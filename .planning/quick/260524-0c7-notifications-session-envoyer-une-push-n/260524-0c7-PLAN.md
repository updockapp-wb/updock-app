---
phase: quick-260524-0c7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/007_session_notif_webhook_and_prefs.sql
  - supabase/functions/notify-session-created/index.ts
  - src/context/ProfileContext.tsx
  - src/context/SessionsContext.tsx
  - src/components/Profile.tsx
  - src/components/SessionForm.tsx
  - src/translations/fr.json
  - src/translations/en.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Session INSERT triggers the notify-session-created Edge Function via database webhook"
    - "Users with notif_session_enabled=false do NOT receive push notifications"
    - "Profile settings show a toggle to enable/disable session notifications"
    - "Creating a session on a spot that already has one the same day shows a conflict message with a join button"
  artifacts:
    - path: "supabase/migrations/007_session_notif_webhook_and_prefs.sql"
      provides: "Database webhook trigger + notif_session_enabled column on profiles"
      contains: "notif_session_enabled"
    - path: "supabase/functions/notify-session-created/index.ts"
      provides: "Edge Function filtering by notification preferences"
      contains: "notif_session_enabled"
    - path: "src/context/ProfileContext.tsx"
      provides: "notif_session_enabled in profile state + toggle function"
      contains: "notif_session_enabled"
    - path: "src/components/Profile.tsx"
      provides: "Notification preferences toggle in settings"
      contains: "notif_session_enabled"
    - path: "src/context/SessionsContext.tsx"
      provides: "Conflict check before session creation"
      contains: "conflict"
    - path: "src/components/SessionForm.tsx"
      provides: "Conflict UI with join button"
      contains: "conflict"
  key_links:
    - from: "supabase/migrations/007_session_notif_webhook_and_prefs.sql"
      to: "supabase/functions/notify-session-created/index.ts"
      via: "database webhook on sessions INSERT"
      pattern: "net.http_post.*notify-session-created"
    - from: "src/context/ProfileContext.tsx"
      to: "src/components/Profile.tsx"
      via: "notif_session_enabled state + toggle"
      pattern: "toggleNotifSession"
    - from: "src/context/SessionsContext.tsx"
      to: "src/components/SessionForm.tsx"
      via: "conflict check result passed to form"
      pattern: "checkSessionConflict"
---

<objective>
Add session creation push notifications (via database webhook), notification preferences toggle in Profile settings, and session conflict detection (same spot + same day).

Purpose: Complete the session notification pipeline (trigger was missing), give users control over notifications, and prevent duplicate sessions on the same spot/day.
Output: Migration SQL, updated Edge Function, updated ProfileContext/SessionsContext, updated Profile and SessionForm UI.
</objective>

<execution_context>
@.planning/quick/260524-0c7-notifications-session-envoyer-une-push-n/260524-0c7-CONTEXT.md
</execution_context>

<context>
@.planning/STATE.md
@supabase/functions/notify-session-created/index.ts
@supabase/migrations/002_notification_triggers.sql
@src/context/SessionsContext.tsx
@src/context/ProfileContext.tsx
@src/components/SessionForm.tsx
@src/components/Profile.tsx
@src/translations/fr.json
@src/translations/en.json

<interfaces>
From src/context/ProfileContext.tsx:
```typescript
interface ProfileData {
    display_name: string | null;
    avatar_url: string | null;
}

interface ProfileContextType {
    profile: ProfileData | null;
    isLoading: boolean;
    updateDisplayName: (name: string) => Promise<void>;
    uploadAvatar: (file: File) => Promise<void>;
}
```

From src/context/SessionsContext.tsx:
```typescript
export interface Session {
  id: string;
  spot_id: string;
  creator_id: string;
  starts_at: string;
  note: string | null;
  is_cancelled: boolean;
  created_at: string;
  creator_profile: { display_name: string | null; avatar_url: string | null; avatar_id: number | null; } | null;
  attendee_count: number;
  user_is_attending: boolean;
}

// createSession signature (line 110):
const createSession = async (spotId: string, startsAt: string, note?: string) => { ... }
```

From src/components/SessionForm.tsx:
```typescript
interface SessionFormProps {
  spotId: string;
  onSessionCreated: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + Edge Function update (backend)</name>
  <files>supabase/migrations/007_session_notif_webhook_and_prefs.sql, supabase/functions/notify-session-created/index.ts</files>
  <action>
  Create migration file `supabase/migrations/007_session_notif_webhook_and_prefs.sql` with two sections:

  **Section 1 — notif_session_enabled column:**
  Add `notif_session_enabled BOOLEAN DEFAULT true` to `public.profiles`. Use `ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS notif_session_enabled BOOLEAN DEFAULT true;`. No backfill needed (DEFAULT true covers existing rows).

  **Section 2 — Database webhook for session creation (commented, manual activation):**
  Follow the exact pattern from `002_notification_triggers.sql` (commented SQL with instructions). Use `pg_net` extension's `net.http_post()` inside a Postgres trigger function. The trigger fires on INSERT into `sessions` table (after insert, for each row). The HTTP call targets the Edge Function URL: `{project_url}/functions/v1/notify-session-created`. Pass the new row as JSON body with `row_to_json(NEW)`. Include Authorization header with anon_key from Vault. Add clear comments explaining:
  1. Enable pg_net extension in Dashboard if not already enabled
  2. Add project_url and anon_key secrets to Vault (same as 002 migration)
  3. Run the commented SQL block manually in Supabase SQL Editor

  The trigger function should be named `notify_session_created_webhook`. The trigger should be named `on_session_insert_notify`.

  **Update Edge Function** `supabase/functions/notify-session-created/index.ts`:
  Between step 2 (fetch creator display name) and step 3 (find favoriting users), add a new step: after fetching `favUsers` (step 3), join with `profiles` to filter out users where `notif_session_enabled = false`. Specifically, after getting `userIds` from favUsers, query `profiles` table: `SELECT id FROM profiles WHERE id IN (userIds) AND notif_session_enabled = true`. Use the filtered list of user IDs to look up push_tokens in step 4 instead of the unfiltered list. If the filtered list is empty, return early with `'no recipients (all opted out)'`.

  Also update the Edge Function to handle the webhook payload format: the webhook sends `row_to_json(NEW)` directly as the body (not wrapped in `{ record: ... }`). Update the payload parsing at the top: try `payload.record` first (for backward compat), fall back to `payload` itself if `payload.record` is undefined. This way the function works whether called by the webhook trigger or manually.
  </action>
  <verify>
    <automated>grep -c "notif_session_enabled" supabase/migrations/007_session_notif_webhook_and_prefs.sql && grep -c "notif_session_enabled" supabase/functions/notify-session-created/index.ts && grep -c "notify_session_created_webhook" supabase/migrations/007_session_notif_webhook_and_prefs.sql</automated>
  </verify>
  <done>Migration adds notif_session_enabled column and contains commented webhook trigger SQL. Edge Function filters recipients by notif preference before sending push tokens.</done>
</task>

<task type="auto">
  <name>Task 2: ProfileContext notif pref + Session conflict check + UI</name>
  <files>src/context/ProfileContext.tsx, src/context/SessionsContext.tsx, src/components/Profile.tsx, src/components/SessionForm.tsx, src/translations/fr.json, src/translations/en.json</files>
  <action>
  **ProfileContext.tsx updates:**
  1. Add `notif_session_enabled: boolean` to `ProfileData` interface (default true).
  2. Update the `select` query in `fetchProfile` to include `notif_session_enabled`.
  3. Map the fetched value into profile state: `notif_session_enabled: data.notif_session_enabled ?? true`.
  4. Add `toggleNotifSession` async function to `ProfileContextType` and implement it: upsert the `profiles` row with the toggled value, optimistic update on `profile` state. Pattern matches `updateDisplayName`.
  5. Expose `toggleNotifSession` in the Provider value.

  **SessionsContext.tsx updates:**
  1. Add a `checkSessionConflict` async function to `SessionsContextType`. Signature: `(spotId: string, startsAt: string) => Promise<Session | null>`. It queries `sessions` table for `spot_id = spotId AND is_cancelled = false` and filters client-side for same calendar day (UTC) as `startsAt`. Compare `new Date(row.starts_at).toISOString().slice(0, 10) === new Date(startsAt).toISOString().slice(0, 10)`. Also filter `starts_at >= now - 1h` (same logic as fetchSessionsForSpot). If a match is found, map the first result to a `Session` object and return it. Otherwise return null.
  2. Expose `checkSessionConflict` in the Provider value.
  3. Do NOT modify `createSession` itself — the conflict check is called from SessionForm before createSession.

  **SessionForm.tsx updates:**
  1. Add state: `conflictSession: Session | null` (import Session type from SessionsContext).
  2. Destructure `checkSessionConflict` and `joinSession` from `useSessions()`.
  3. In `handleSubmit`, before calling `createSession`, call `checkSessionConflict(spotId, isoDate)`. If it returns a session, set `conflictSession` state and return early (do not create).
  4. Render conflict UI when `conflictSession` is not null: a yellow/amber banner below the submit button (or replacing the form area) with:
     - Icon: Calendar from lucide-react
     - Text: translation key `session.conflict_message` (e.g. "Une session existe deja ce jour-la")
     - Creator name and time of existing session (from `conflictSession.creator_profile.display_name` and formatted `conflictSession.starts_at`)
     - A primary button using translation key `session.conflict_join` that calls `joinSession(conflictSession.id)` then calls `onSessionCreated()`
     - A secondary/text button using translation key `session.conflict_create_anyway` that clears `conflictSession` state and calls the original `createSession` flow
  5. Reset `conflictSession` to null when `startsAt` input changes (so the banner disappears if the user picks a different date).

  **Profile.tsx updates:**
  Add a notification preferences toggle inside the existing settings section (the `bg-white rounded-3xl` div). Insert it right after the existing "Notification Status" row (line ~376) and before the Premium row. Structure:
  - A new row with Bell icon (already imported), label from translation key `profile.notif_session_toggle`
  - A toggle switch (styled div with rounded-full, transition, bg-sky-500 when on, bg-slate-200 when off, with a white circle that translates). Controlled by `profile?.notif_session_enabled ?? true`.
  - onClick calls `toggleNotifSession()` from `useProfile()`.
  - Only show this row when `permissionStatus === 'granted'` (no point toggling in-app prefs if system permission is denied).

  **Translations (fr.json and en.json):**
  Add keys:
  - `session.conflict_message`: FR "Une session existe deja sur ce spot ce jour-la", EN "A session already exists at this spot on this day"
  - `session.conflict_join`: FR "Rejoindre cette session", EN "Join this session"
  - `session.conflict_create_anyway`: FR "Creer quand meme", EN "Create anyway"
  - `profile.notif_session_toggle`: FR "Notifications de session", EN "Session notifications"
  - `profile.notif_session_desc`: FR "Recevoir une notification quand une session est creee sur un spot favori", EN "Get notified when a session is created on a favorite spot"
  </action>
  <verify>
    <automated>cd /Users/wandrillebasse/updock-app && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>Profile settings show a toggle for session notifications (only when push permission granted). SessionForm checks for same-day conflict before creating, shows conflict banner with join button and create-anyway option. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no type errors)
2. `npm run build` succeeds
3. Migration file contains both the ALTER TABLE and the commented webhook trigger
4. Edge Function filters by notif_session_enabled before sending
5. Profile settings show notification toggle when permission is granted
6. SessionForm displays conflict banner when a same-day session exists
</verification>

<success_criteria>
- Migration 007 adds notif_session_enabled column and contains commented webhook SQL following 002 pattern
- Edge Function respects notif preferences (filters out opted-out users)
- Profile tab has a working toggle for session notifications
- SessionForm detects same-day conflict and offers join or create-anyway
- TypeScript builds cleanly
</success_criteria>

<output>
Create `.planning/quick/260524-0c7-notifications-session-envoyer-une-push-n/260524-0c7-SUMMARY.md` when done
</output>
