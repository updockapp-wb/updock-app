---
phase: quick-260731-e56
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SpotDetail.tsx
  - src/components/AddSpotForm.tsx
  - src/components/AdminDashboard.tsx
  - src/translations/fr.json
  - src/translations/en.json
autonomous: true
requirements: [QUICK-e56]

must_haves:
  truths:
    - "Le bouton croix de fermeture de SpotDetail reste entièrement visible même quand le nom du spot est long"
    - "Créer un spot sans description affiche une erreur inline et ne soumet pas (AddSpotForm)"
    - "Éditer un spot en vidant sa description affiche une erreur inline et ne sauvegarde pas (SpotDetail édition inline)"
    - "Éditer un spot admin en vidant sa description affiche une erreur inline et ne sauvegarde pas (AdminDashboard)"
    - "Le mot communauté s'affiche avec son accent dans les écrans profil anonyme et stats communauté (fr)"
  artifacts:
    - path: "src/translations/fr.json"
      provides: "clé form.error.desc_required (fr) + accents communauté corrigés"
      contains: "form.error.desc_required"
    - path: "src/translations/en.json"
      provides: "clé form.error.desc_required (en)"
      contains: "form.error.desc_required"
  key_links:
    - from: "src/components/AddSpotForm.tsx"
      to: "form.error.desc_required"
      via: "t() lookup dans handleSubmit"
      pattern: "form\\.error\\.desc_required"
    - from: "src/components/SpotDetail.tsx"
      to: "form.error.desc_required"
      via: "t() lookup dans handleSaveEdit"
      pattern: "form\\.error\\.desc_required"
    - from: "src/components/AdminDashboard.tsx"
      to: "form.error.desc_required"
      via: "t() lookup dans handleSaveEdit"
      pattern: "form\\.error\\.desc_required"
---

<objective>
Corriger 3 écarts UI/i18n indépendants découverts pendant la recette device de la Phase 4 :
(1) le bouton croix de fermeture de SpotDetail coupé quand le nom du spot est long,
(2) rendre la description de spot obligatoire dans les 3 formulaires migrés en Phase 4,
(3) corriger l'accent manquant sur « communauté » dans fr.json.

Purpose: Nettoyer les petits défauts de recette avant de clôturer la Phase 4, sans embarquer de gros chantier.
Output: 5 fichiers touchés (3 composants + 2 fichiers de traduction), aucune régression build/lint.

HORS PÉRIMÈTRE (à laisser en todo) : la migration DS de l'onglet Pending d'AdminDashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Bugs détaillés
@.planning/todos/pending/2026-07-31-spotdetail-close-button-clipped-long-spot-name.md
@.planning/todos/pending/2026-07-31-rendre-la-description-de-spot-obligatoire.md
@.planning/todos/pending/2026-07-31-corriger-accent-communaute-manquant-fr-json.md

# Fichiers touchés
@src/components/SpotDetail.tsx
@src/components/AddSpotForm.tsx
@src/components/AdminDashboard.tsx
@src/translations/fr.json
@src/translations/en.json

<interfaces>
<!-- Pattern de validation client existant à répliquer EXACTEMENT (early return + setError + données préservées). -->

AddSpotForm.tsx handleSubmit (~ligne 135-140) :
  setError(null);
  const trimmed = name.trim();
  if (!trimmed) { setError(t('form.error.name_required')); return; }
  if (trimmed.length > 100) { setError(t('form.error.name_too_long')); return; }
  if (description.length > 2000) { setError(t('form.error.desc_too_long')); return; }
  if (type.length === 0) { setError(t('form.error.type_required')); return; }
  // description est une string state ; erreur affichée dans le bloc rouge en bas de form (ligne ~290)

SpotDetail.tsx handleSaveEdit (~ligne 203-211) :
  setEditError(null);
  const trimmedName = editForm.name.trim();
  if (!trimmedName) { setEditError(t('form.error.name_required')); return; }
  if (trimmedName.length > 100) { setEditError(t('form.error.name_too_long')); return; }
  if ((editForm.description || '').length > 2000) { setEditError(t('form.error.desc_too_long')); return; }
  if (editForm.type.length === 0) { setEditError(t('form.error.type_required')); return; }
  // description peut être optionnelle sur le type Spot -> utiliser (editForm.description || '')

AdminDashboard.tsx handleSaveEdit (~ligne 36-41) :
  setEditError(null);
  const trimmedName = editingSpot.name.trim();
  if (trimmedName.length === 0) { setEditError(t('form.error.name_required')); return; }
  if (trimmedName.length > 100) { setEditError(t('form.error.name_too_long')); return; }
  if (editingSpot.description.length > 2000) { setEditError(t('form.error.desc_too_long')); return; }
  if (editingSpot.type.length === 0) { setEditError(t('form.error.type_required')); return; }

Clés i18n existantes (parité fr/en, même index ~164-166) :
  "form.error.name_required": fr="Le nom du spot est obligatoire." / en="Spot name is required."
  "form.error.desc_too_long": fr / en existants
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Empêcher le bouton croix de SpotDetail d'être coupé sur nom long</name>
  <files>src/components/SpotDetail.tsx</files>
  <action>
Dans le header de SpotDetail (rangée titre/boutons, ~ligne 253), le conteneur du titre `<div className="flex-1">` (ligne 254) refuse de se réduire (min-width: auto flexbox par défaut sur un flex item contenant du texte non tronqué), poussant la rangée de boutons de droite `<div className="flex gap-2">` (ligne 307) hors écran — la croix de fermeture apparaît partiellement coupée.

Deux corrections combinées :
1. Ajouter `min-w-0` à la classe du conteneur titre ligne 254 : `className="flex-1"` -> `className="flex-1 min-w-0"`. Cela autorise le flex item à se réduire sous sa largeur de contenu intrinsèque.
2. Ajouter `shrink-0` à la rangée de boutons ligne 307 : `className="flex gap-2"` -> `className="flex gap-2 shrink-0"`. Garantit que la rangée share/favori/fermer ne se compresse jamais.

NE PAS ajouter `truncate` sur le `<motion.h2 layoutId>` lui-même (ligne 256-261) : le titre est dans un conteneur `flex flex-wrap` (ligne 255) avec un badge de type à côté, et `truncate` sur un enfant de flex-wrap donnerait un résultat imprévisible. Le `min-w-0` sur le parent `flex-1` suffit à laisser le `flex-wrap` gérer le retour à la ligne du nom long. Vérifier après coup que le nom long wrappe proprement plutôt que de déborder.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -n 'flex-1 min-w-0' src/components/SpotDetail.tsx && grep -n 'flex gap-2 shrink-0' src/components/SpotDetail.tsx</automated>
    <human-check>Ouvrir un spot au nom très long (ex. 60+ caractères) : la croix de fermeture (rond gris) est entièrement visible, non coupée par le bord droit.</human-check>
  </verify>
  <done>Le conteneur titre porte `min-w-0`, la rangée de boutons porte `shrink-0`, le build passe. La croix reste entièrement visible quel que soit la longueur du nom.</done>
</task>

<task type="auto">
  <name>Task 2: Rendre la description obligatoire dans les 3 formulaires + clé i18n</name>
  <files>src/components/AddSpotForm.tsx, src/components/SpotDetail.tsx, src/components/AdminDashboard.tsx, src/translations/fr.json, src/translations/en.json</files>
  <action>
Ajouter une validation client « description non vide (après trim) » dans les 3 points de validation, en répliquant EXACTEMENT le pattern `name_required` (early return AVANT mutation, setError avec message traduit, données saisies préservées — aucun reset). Placer le nouveau check juste AVANT le check `desc_too_long` correspondant, par symétrie avec `name_required` qui précède `name_too_long`.

1. Nouvelle clé i18n, parité stricte fr/en, insérée juste après `form.error.name_required` (~ligne 164/165) dans les DEUX fichiers :
   - fr.json : `"form.error.desc_required": "La description du spot est obligatoire.",`
   - en.json : `"form.error.desc_required": "Spot description is required.",`
   Respecter l'indentation existante (4 espaces) et la virgule de fin. Vérifier que les deux fichiers restent du JSON valide.

2. AddSpotForm.tsx handleSubmit (~ligne 139) — insérer avant le check `description.length > 2000` :
   `if (!description.trim()) { setError(t('form.error.desc_required')); return; }`

3. SpotDetail.tsx handleSaveEdit (~ligne 210) — insérer avant le check `(editForm.description || '').length > 2000` :
   `if (!(editForm.description || '').trim()) { setEditError(t('form.error.desc_required')); return; }`

4. AdminDashboard.tsx handleSaveEdit (~ligne 40) — insérer avant le check `editingSpot.description.length > 2000` :
   `if (editingSpot.description.trim().length === 0) { setEditError(t('form.error.desc_required')); return; }`

Ne toucher à aucune contrainte Postgres/RLS (hors périmètre quick — la garde autoritaire reste au niveau existant ; on n'ajoute que la garde UX). Ne pas modifier les composants Input ni l'affichage des erreurs (le bloc rouge existant en bas de form gère déjà l'affichage).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && node -e "JSON.parse(require('fs').readFileSync('src/translations/fr.json','utf8')); JSON.parse(require('fs').readFileSync('src/translations/en.json','utf8')); console.log('JSON OK')" && grep -c 'desc_required' src/translations/fr.json src/translations/en.json && grep -rn 'desc_required' src/components/AddSpotForm.tsx src/components/SpotDetail.tsx src/components/AdminDashboard.tsx</automated>
    <human-check>Dans chacun des 3 formulaires, tenter de créer/sauvegarder avec une description vide : un message d'erreur inline s'affiche, la soumission est bloquée, et les autres champs saisis restent intacts.</human-check>
  </verify>
  <done>La clé `form.error.desc_required` existe avec parité fr/en, les 3 handlers rejettent une description vide via early return avant mutation en préservant les données, build vert, JSON valide.</done>
</task>

<task type="auto">
  <name>Task 3: Corriger l'accent manquant sur « communauté » dans fr.json</name>
  <files>src/translations/fr.json</files>
  <action>
Corriger les occurrences de « communaute » -> « communauté » dans les 4 clés suivantes de src/translations/fr.json (en.json non concerné) :
- ligne 136 `anon_profile.title` : "Rejoins la communaute Updock" -> "Rejoins la communauté Updock"
- ligne 137 `anon_profile.subtitle` : "Decouvre les spots, partage tes sessions et retrouve la communaute." -> "Découvre les spots, partage tes sessions et retrouve la communauté." (corriger AUSSI « Decouvre » -> « Découvre » sur la même ligne — même accent manquant relevé dans le todo)
- ligne 152 `community_stats.title` : "La communaute" -> "La communauté"
- ligne 157 `community_stats.nav_label` : "Statistiques communaute" -> "Statistiques communauté"

Ne modifier que ces valeurs de chaînes, pas les clés. Vérifier que fr.json reste du JSON valide (encodage UTF-8, accents littéraux).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/translations/fr.json','utf8')); console.log('JSON OK')" && grep -c 'communaute' src/translations/fr.json && grep -c 'communauté' src/translations/fr.json</automated>
    <human-check>Ouvrir l'écran profil anonyme et l'onglet stats communauté : le mot « communauté » s'affiche partout avec son accent.</human-check>
  </verify>
  <done>Aucune occurrence de « communaute » sans accent ne subsiste dans les 4 clés visées (grep communaute = 0), « Découvre » corrigé, JSON valide.</done>
</task>

</tasks>

<verification>
- `npm run build` passe (tsc -b + vite build) sans erreur.
- `npm run lint` (eslint .) ne remonte pas de nouvelle erreur sur les 5 fichiers touchés.
- fr.json et en.json parsent en JSON valide.
- Parité i18n : `form.error.desc_required` présent dans fr.json ET en.json.
- Aucune régression sur les validations existantes (name_required, name_too_long, desc_too_long, type_required).
</verification>

<success_criteria>
- Croix de fermeture SpotDetail entièrement visible sur nom long (`min-w-0` + `shrink-0`).
- Description obligatoire appliquée dans AddSpotForm, SpotDetail (édition inline) et AdminDashboard, avec message inline traduit et données préservées.
- `form.error.desc_required` ajoutée avec parité fr/en stricte.
- 4 occurrences « communauté » corrigées dans fr.json (+ « Découvre »).
- Build + lint verts, JSON valide.
</success_criteria>

<output>
Créer `.planning/quick/260731-e56-corriger-les-4-petits-bugs-carts-d-couve/260731-e56-SUMMARY.md` une fois terminé.
</output>
