---
phase: quick-260420-001
plan: 01
subsystem: SpotDetail / i18n
tags: [anonymous-access, ux, blur-overlay, auth-gate]
dependency_graph:
  requires: []
  provides: [blur-photo-gate]
  affects: [SpotDetail.tsx, fr.json, en.json]
tech_stack:
  added: []
  patterns: [conditional-blur-overlay, auth-guard-onclick]
key_files:
  created: []
  modified:
    - src/components/SpotDetail.tsx
    - src/translations/fr.json
    - src/translations/en.json
decisions:
  - blur-is-ux-friction-not-data-protection: Image URLs remain accessible in DOM; acceptable for community content per threat model T-q001-01
metrics:
  duration: ~5 min
  completed: 2026-04-20
---

# Quick Task 260420-001: Photo blur overlay for anonymous users

**One-liner:** Blur overlay with lock icon on spot photos for unauthenticated users; clicking opens AuthModal instead of lightbox.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Blur overlay + auth guard on photo preview | cedcd1e | src/components/SpotDetail.tsx |
| 2 | i18n keys fr + en | 35ae090 | src/translations/fr.json, src/translations/en.json |

## What Was Built

### Task 1 — SpotDetail.tsx

Modified the Photo Preview section (lines ~393-447):

- `<img>` receives `blur-md scale-105` CSS classes when `!user`
- Lock overlay (`bg-white/90` card with `Lock` icon + `spot.photos_login_required` text) rendered when `!user`
- "View N Photos" badge now conditionally rendered only when `user` is truthy
- `onClick` handler now has auth guard: `if (!user) { onOpenAuth?.(); return; }` before `setIsImageOpen(true)`
- Lightbox needs no extra guard — `isImageOpen` can only become `true` via the guarded `onClick`

### Task 2 — Translations

Added `spot.photos_login_required` key:
- French: `"Connectez-vous pour voir les photos"`
- English: `"Sign in to view photos"`

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — the overlay is fully wired with real auth state and i18n translations.

## Threat Flags

None beyond what was documented in the plan's threat model (T-q001-01: image URLs remain accessible in DOM; accepted).

## Self-Check: PASSED

- src/components/SpotDetail.tsx — modified, committed cedcd1e
- src/translations/fr.json — modified, committed 35ae090
- src/translations/en.json — modified, committed 35ae090
- Build: `✓ built in 21.53s` (clean, no TypeScript errors)
