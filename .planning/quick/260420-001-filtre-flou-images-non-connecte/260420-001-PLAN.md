---
phase: quick-260420-001
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SpotDetail.tsx
  - src/translations/fr.json
  - src/translations/en.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Un utilisateur non connecté voit les photos flouées avec un cadenas centré"
    - "Cliquer sur la photo flouée ouvre la modale d'auth (pas la lightbox)"
    - "Un utilisateur connecté voit et peut ouvrir les photos normalement"
  artifacts:
    - path: "src/components/SpotDetail.tsx"
      provides: "Overlay blur + cadenas conditionnel sur la photo principale et la lightbox"
  key_links:
    - from: "Photo principale (div onClick)"
      to: "onOpenAuth()"
      via: "guard !user avant setIsImageOpen"
    - from: "Lightbox (isImageOpen)"
      to: "guard !user"
      via: "isImageOpen ne peut être true si !user (onClick bloqué)"
---

<objective>
Ajouter un overlay flou + cadenas sur les photos du SpotDetail quand l'utilisateur n'est pas connecté. Les photos restent visibles mais flouées; cliquer ouvre la modale d'auth au lieu de la lightbox.

Purpose: Inciter les utilisateurs anonymes à créer un compte pour accéder aux photos en haute qualité.
Output: SpotDetail.tsx modifié avec blur conditionnel + clés i18n ajoutées dans fr.json et en.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- Contrats existants à respecter -->

From src/components/SpotDetail.tsx:
```typescript
interface SpotDetailProps {
    spot: Spot | null;
    onClose: () => void;
    onOpenAuth?: () => void;  // Ouvre la modale d'auth — déjà utilisé pour favorites, reviews, sessions
}
// user = useAuth().user — null si non connecté, User si connecté
// isImageOpen / setIsImageOpen — contrôle la lightbox
// Lock est déjà importé depuis 'lucide-react'
```

Pattern existant (favoris) :
```tsx
onClick={() => {
    if (!user) { onOpenAuth?.(); return; }
    toggleFavorite(spot.id);
}}
```

Traductions : src/translations/fr.json et src/translations/en.json.
Format : `"clé": "valeur"` plat, pas de nesting.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blur overlay sur la photo principale + guard lightbox</name>
  <files>src/components/SpotDetail.tsx</files>
  <action>
Modifier la section "Photo Preview" (ligne ~389-418) et le handler d'ouverture de la lightbox.

**Photo principale — remplacer le contenu conditionnel quand spot.image_urls existe :**

```tsx
{spot.image_urls && spot.image_urls.length > 0 ? (
    <>
        <img
            src={spot.image_urls[0]}
            alt={spot.name}
            className={`w-full h-full object-cover transition-all duration-300 ${!user ? 'blur-md scale-105' : ''}`}
        />
        {/* Overlay cadenas — seulement si non connecté */}
        {!user && (
            <div className="absolute inset-0 flex flex-col items-center justify-center gap-2 bg-black/20">
                <div className="bg-white/90 backdrop-blur-sm rounded-2xl px-5 py-4 flex flex-col items-center gap-2 shadow-lg">
                    <Lock size={22} className="text-slate-700" />
                    <span className="text-xs font-bold text-slate-700 text-center leading-tight">
                        {t('spot.photos_login_required')}
                    </span>
                </div>
            </div>
        )}
        {/* Badge "N photos" — seulement si connecté ET plusieurs photos */}
        {user && spot.image_urls.length > 1 && (
            <div className="absolute bottom-3 right-3 bg-black/60 backdrop-blur-md text-white text-[10px] font-bold px-3 py-1.5 rounded-full border border-white/10 uppercase tracking-widest">
                View {spot.image_urls.length} Photos
            </div>
        )}
    </>
) : (
    <div className="w-full h-full flex flex-col items-center justify-center text-slate-400 text-xs gap-2">
        <div className="p-3 bg-white rounded-full"><Waves size={24} className="opacity-20" /></div>
        No photos available
    </div>
)}
```

**Handler onClick du conteneur photo — remplacer par :**

```tsx
onClick={() => {
    if (!user) {
        onOpenAuth?.();
        return;
    }
    if (spot.image_urls && spot.image_urls.length > 0) {
        setCurrentPhotoIndex(0);
        setIsImageOpen(true);
    }
}}
```

La lightbox n'a pas besoin de guard supplémentaire : `isImageOpen` ne peut être `true` que si l'utilisateur est connecté (le seul endroit où `setIsImageOpen(true)` est appelé est maintenant gardé). Le reste de la lightbox JSX reste inchangé.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>
- Utilisateur non connecté : photo flouée + cadenas visible, clic ouvre AuthModal
- Utilisateur connecté : photo nette, badge "N Photos" présent, clic ouvre lightbox
- Build TypeScript sans erreur
  </done>
</task>

<task type="auto">
  <name>Task 2: Clés i18n fr + en</name>
  <files>src/translations/fr.json, src/translations/en.json</files>
  <action>
Ajouter la clé `spot.photos_login_required` dans les deux fichiers de traduction.

Dans **fr.json**, après la clé `"spot.added_by"` :
```json
"spot.photos_login_required": "Connectez-vous pour voir les photos",
```

Dans **en.json**, même emplacement (après `"spot.added_by"` ou à la fin du bloc spot.*) :
```json
"spot.photos_login_required": "Sign in to view photos",
```

Ne pas modifier les autres clés existantes.
  </action>
  <verify>grep "photos_login_required" src/translations/fr.json src/translations/en.json</verify>
  <done>Les deux fichiers contiennent la clé avec leur traduction respective.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI | Blur est côté client uniquement — les URLs des images restent accessibles dans le DOM. Acceptable : c'est une friction UX, pas une protection de données. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q001-01 | Information Disclosure | img src attribute | accept | Les URLs restent visibles dans le DOM pour un utilisateur anonyme déterminé. Acceptable pour un produit communautaire — les photos ne sont pas du contenu sensible. Si protection réelle requise, implémenter signed URLs côté Supabase Storage (hors scope). |
</threat_model>

<verification>
1. `npm run build` passe sans erreur TypeScript
2. En mode non connecté : photo flouée + cadenas centré visible, clic → AuthModal s'ouvre
3. En mode connecté : photo nette, clic → lightbox s'ouvre normalement
4. Lightbox fonctionne toujours (navigation prev/next, fermeture) pour les utilisateurs connectés
</verification>

<success_criteria>
- Photo principale flouée + overlay cadenas quand `!user`
- Clic photo non connecté → `onOpenAuth?.()` appelé (pas `setIsImageOpen`)
- Lightbox inchangée pour utilisateurs connectés
- Deux clés i18n ajoutées, build propre
</success_criteria>

<output>
After completion, create `.planning/quick/260420-001-filtre-flou-images-non-connecte/260420-001-SUMMARY.md`
</output>
