---
phase: 02-navigation-vue-carte-spots
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md
autonomous: false
requirements: [PERF-01, MAP-01, MAP-02, NAV-01]

must_haves:
  truths:
    - "Une baseline Profiler chiffrée (renders + durée du sous-arbre carte en togglant les filtres) existe AVANT tout refactor"
    - "Une baseline mémoire (rétention Blob/object URLs après 5 cycles open/close d'AddSpotForm) existe AVANT tout refactor"
    - "La liste des consommateurs du master src/ui/Modal est établie (AuthModal seul, glass) avant d'étendre Modal"
  artifacts:
    - path: ".planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md"
      provides: "Baseline Profiler + mémoire + audit consommateurs Modal"
      contains: "Profiler"
  key_links:
    - from: "02-BASELINE.md"
      to: "PERF-01 / MAP-01 verification"
      via: "chiffres avant/après comparables"
      pattern: "renders"
---

<objective>
Capturer la **baseline de performance et de mémoire AVANT tout refactor** de la vue
Carte, et auditer les consommateurs actuels de `src/ui/Modal` avant de l'étendre.

Purpose : PERF-01 et MAP-01 exigent une comparaison **avant/après** (success criteria
ROADMAP + Blocker STATE.md sur les baselines chiffrées). Sans baseline capturée avant
le premier changement de code, l'élimination des re-renders et de la fuite mémoire
n'est pas mesurable — la phase deviendrait invérifiable. L'audit des consommateurs de
`Modal` conditionne l'extension sûre de ce master en Plan 03 (Pitfall 1 / Open Q1 du
RESEARCH).

Output : `02-BASELINE.md` contenant les chiffres Profiler, les snapshots mémoire et la
liste des consommateurs de `Modal`.

**Ce plan est un GATE Wave 0 : il DOIT être complété avant que les Plans 02 et 03
(Wave 1) ne modifient le moindre fichier de code.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation-vue-carte-spots/02-VALIDATION.md
@.planning/phases/02-navigation-vue-carte-spots/02-RESEARCH.md

<interfaces>
<!-- Contrat de mesure — extrait de 02-VALIDATION.md (Protocole PERF-01) et 02-RESEARCH.md. -->
<!-- L'exécuteur applique ce protocole tel quel, aucune exploration nécessaire. -->

Protocole Profiler (RESEARCH § Protocole de mesure PERF-01) :
1. `npm run dev`, ouvrir React DevTools → onglet Profiler.
2. Démarrer l'enregistrement, toggler CHAQUE filtre 2× (All → Dockstart → … → All), arrêter.
3. Noter : nombre de renders du composant `MapComponent` + durée, et si `<Source>`/layers re-render.

Protocole mémoire (RESEARCH § Protocole, étape 6 ; Pitfall 3) :
- Onglet Memory : snapshot → ajouter 5 images dans AddSpotForm → fermer → rouvrir ×5 → snapshot.
- Comparer la rétention des Blob / object URLs entre les snapshots.

Consommateur unique confirmé par RESEARCH (A2, Open Q1) : `grep -rn "ui/Modal" src/`
→ AuthModal.tsx (surface glass) seul. À re-confirmer par la commande dans ce plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Auditer les consommateurs de Modal et créer le scaffold 02-BASELINE.md</name>
  <files>.planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md</files>
  <read_first>
    - src/ui/Modal.tsx (état actuel du master, pour documenter la surface glass par défaut)
    - .planning/phases/02-navigation-vue-carte-spots/02-VALIDATION.md (protocole Wave 0 exact)
    - .planning/phases/02-navigation-vue-carte-spots/02-RESEARCH.md § Protocole de mesure PERF-01
  </read_first>
  <action>
    Exécuter `grep -rn "ui/Modal" src/` et consigner le résultat brut. Créer le fichier
    `.planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md` avec quatre sections
    prêtes à remplir : (1) « Consommateurs de src/ui/Modal » — coller le résultat du grep
    et la conclusion attendue (AuthModal seul, surface glass → défaut glass rétro-compatible
    pour l'extension du Plan 03) ; (2) « Baseline Profiler (AVANT refactor) » — tableau vide
    avec colonnes : filtre togglé, nombre de renders de `MapComponent`, durée (ms),
    `<Source>`/layers re-render (oui/non) ; (3) « Baseline mémoire AVANT refactor » —
    emplacements pour snapshot initial, snapshot après 5 cycles open/close d'AddSpotForm,
    et delta de rétention Blob/object URLs ; (4) « Protocole exact » — copier verbatim les
    6 étapes du protocole PERF-01 de RESEARCH.md pour que le dev les rejoue à l'identique
    après refactor. Ne pas remplir les chiffres Profiler/mémoire (Task 2, mesure humaine).
    Aucune modification de code source dans cette tâche.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md && grep -q "Profiler" .planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md && grep -rn "ui/Modal" src/ | grep -v '^Binary' | grep -c "AuthModal"</automated>
  </verify>
  <done>02-BASELINE.md existe avec les 4 sections ; le grep confirme AuthModal comme unique consommateur de src/ui/Modal.</done>
  <acceptance_criteria>
    - `test -f .planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md` retourne 0.
    - `grep -rn "ui/Modal" src/` liste exactement une occurrence : `src/components/AuthModal.tsx`.
    - 02-BASELINE.md contient les sections « Baseline Profiler », « Baseline mémoire » et « Protocole exact » (assertion source : les 3 titres présents).
    - Aucun fichier sous `src/` n'a été modifié (assertion : `git status --short src/` vide).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 : Capturer les chiffres Profiler + mémoire dans 02-BASELINE.md (mesure humaine)</name>
  <files>.planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md</files>
  <read_first>
    - .planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md (scaffold créé en Task 1)
    - .planning/phases/02-navigation-vue-carte-spots/02-RESEARCH.md § Protocole de mesure PERF-01
  </read_first>
  <action>
    Étape humaine (aucun outil CLI ne peut mesurer les renders/mémoire runtime) : le dev
    lance `npm run dev`, capture via React DevTools Profiler et l'onglet Memory les chiffres
    AVANT refactor, puis les reporte dans 02-BASELINE.md. Voir <how-to-verify> pour les étapes.
  </action>
  <what-built>
    Le scaffold 02-BASELINE.md et le protocole de mesure sont prêts (Task 1). La capture des
    chiffres Profiler et mémoire nécessite React DevTools + l'onglet Memory du navigateur —
    action qu'aucun outil CLI ne peut réaliser (mesure runtime interactive).
  </what-built>
  <how-to-verify>
    1. Lancer `npm run dev` et ouvrir l'app dans Chrome avec l'extension React DevTools installée.
    2. Onglet Profiler : démarrer l'enregistrement, toggler chaque filtre de la carte 2× (All → Dockstart → Rockstart → … → All), arrêter. Reporter dans 02-BASELINE.md le nombre de renders de `MapComponent` + la durée, et si `<Source>`/layers re-render.
    3. Onglet Memory : prendre un snapshot, ouvrir « ajouter un spot » depuis la carte, ajouter 5 images dans AddSpotForm, fermer, rouvrir/refermer le formulaire ×5, prendre un second snapshot. Reporter le delta de rétention des Blob / object URLs.
    4. Sauvegarder 02-BASELINE.md avec toutes les valeurs remplies.
  </how-to-verify>
  <verify>
    <human-check>02-BASELINE.md contient des chiffres Profiler (renders + durée) et un delta mémoire non vides pour l'état AVANT refactor.</human-check>
  </verify>
  <done>02-BASELINE.md rempli avec les valeurs Profiler et mémoire AVANT refactor.</done>
  <acceptance_criteria>
    - 02-BASELINE.md contient des valeurs numériques Profiler (renders + durée) renseignées.
    - 02-BASELINE.md contient un delta de rétention mémoire renseigné après 5 cycles open/close.
  </acceptance_criteria>
  <resume-signal>Taper « baseline capturée » une fois 02-BASELINE.md rempli, ou décrire tout blocage (React DevTools absent, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Plan de mesure uniquement — aucun code exécuté, aucune entrée non fiable, aucun fichier source modifié. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-A | Information Disclosure | 02-BASELINE.md | accept | Ne contient que des métriques de perf/mémoire locales — aucune donnée sensible, aucun secret. |
| T-02-SC | Tampering (supply chain) | npm installs | accept (N/A) | Aucun `npm install` dans cette phase (RESEARCH § Package Legitimacy Audit = non applicable). |
</threat_model>

<verification>
- 02-BASELINE.md existe et contient les 4 sections attendues.
- Le grep confirme AuthModal comme unique consommateur de src/ui/Modal (débloque l'extension du Plan 03).
- Les chiffres Profiler et mémoire AVANT refactor sont renseignés (checkpoint humain).
- Aucun fichier `src/` modifié (ce plan ne touche pas au code).
</verification>

<success_criteria>
- Une baseline chiffrée Profiler + mémoire existe AVANT tout changement de code, comparable après refactor.
- La liste des consommateurs de Modal est établie, autorisant une extension rétro-compatible (défaut glass).
</success_criteria>

<output>
Créer `.planning/phases/02-navigation-vue-carte-spots/02-01-SUMMARY.md` une fois terminé,
en y référençant 02-BASELINE.md et ses chiffres clés.
</output>
