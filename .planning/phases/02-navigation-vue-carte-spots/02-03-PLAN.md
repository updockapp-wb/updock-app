---
phase: 02-navigation-vue-carte-spots
plan: 03
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/ui/Modal.tsx
  - src/components/FiltersModal.tsx
  - src/components/NavBar.tsx
  - src/App.tsx
autonomous: true
requirements: [NAV-01, PERF-01]

must_haves:
  truths:
    - "src/ui/Modal accepte une variante claire/bottom-sheet en plus de la glass par défaut, sans régresser AuthModal (glass) (D-02)"
    - "FiltersModal s'affiche exactement comme avant (bottom-sheet clair) mais consomme src/ui/Modal + src/ui/Header (D-02)"
    - "AuthModal (autre consommateur de Modal) reste en surface glass, inchangé (D-02)"
    - "La NavBar mobile active rend identiquement après wiring de text-sky-500 → text-primary (NAV-01)"
    - "Les boutons d'onglet NavBar (mobile et desktop) restent des <button> natifs, non migrés vers src/ui/Button — aucun variant Button ne correspond à leur forme (D-04)"
    - "Le nav-shell (NavBar mobile + desktop) ne se re-render plus quand un état NON-nav change (React.memo + callbacks App.tsx stabilisés via useCallback), rendu strictement inchangé (PERF-01)"
    - "Les gradients/ombres/sky-50/600 sans token restent littéraux (non modifiés) (D-01)"
  artifacts:
    - path: "src/ui/Modal.tsx"
      provides: "Modal avec prop surface/layout (défaut glass rétro-compatible)"
      contains: "surface"
    - path: "src/components/FiltersModal.tsx"
      provides: "FiltersModal migré vers Modal + Header (surface light/sheet)"
      contains: "Modal"
    - path: "src/components/NavBar.tsx"
      provides: "text-sky-500 → text-primary sur les 4 états actifs mobiles + export React.memo"
      contains: "memo"
    - path: "src/App.tsx"
      provides: "Callbacks NavBar (onTabChange/onAddSpotClick/onOpenAuth) stabilisés via useCallback"
      contains: "useCallback"
  key_links:
    - from: "src/components/FiltersModal.tsx"
      to: "src/ui/Modal + src/ui/Header"
      via: "composition <Modal surface=light><Header surface=light .../></Modal>"
      pattern: "from '../ui/Modal'"
    - from: "src/components/NavBar.tsx"
      to: "--color-primary (src/index.css)"
      via: "classe text-primary (alias var(--color-sky-500))"
      pattern: "text-primary"
    - from: "src/App.tsx"
      to: "src/components/NavBar.tsx (React.memo)"
      via: "callbacks stabilisés useCallback → props NavBar référentiellement stables"
      pattern: "useCallback"
---

<objective>
Migrer le chrome de navigation vers le design system Phase 1, byte-identique : (Pitfall 1)
étendre `src/ui/Modal` avec une variante claire/bottom-sheet ; (D-02) migrer `FiltersModal`
vers `Modal` + `Header` + garder ses lignes de filtre custom ; (NAV-01) câbler les
occurrences `text-sky-500` de la NavBar mobile vers `text-primary` ; (PERF-01) éliminer les
re-renders inutiles du nav-shell en mémoïsant `NavBar` (`React.memo`) et en stabilisant ses
callbacks dans `App.tsx` (`useCallback`).

Purpose : supprimer le shell modal dupliqué et les littéraux sky wirables, en réutilisant
les masters existants — cohérence DS sans changer un pixel du rendu. Le master `Modal` est
aujourd'hui codé en dur en glass ; l'étendre AVANT de migrer `FiltersModal` est obligatoire,
sinon régression visuelle majeure (surface, z-index, animation, backdrop, radius tous différents).

Output : `Modal.tsx` avec prop `surface`/`layout` (défaut glass), `FiltersModal.tsx` migré,
`NavBar.tsx` avec tokens câblés + export `React.memo`, `App.tsx` avec callbacks NavBar stabilisés.

**Contrainte cardinale : byte-identique. AuthModal (seul autre consommateur de Modal, glass)
ne doit pas régresser — défaut glass obligatoire.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-navigation-vue-carte-spots/02-CONTEXT.md
@.planning/phases/02-navigation-vue-carte-spots/02-RESEARCH.md
@.planning/phases/02-navigation-vue-carte-spots/02-PATTERNS.md
@.planning/phases/02-navigation-vue-carte-spots/02-UI-SPEC.md
@.planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md

<interfaces>
<!-- Contrats existants extraits du code. L'exécuteur les applique verbatim. -->

Modal ACTUEL (glass, à garder comme défaut) — src/ui/Modal.tsx l.11-32 :
- container: "fixed inset-0 z-[5000] flex items-center justify-center p-4 bg-black/60 backdrop-blur-md"
- panel: motion.div, initial/animate/exit sur `scale` (0.95↔1),
  "bg-white/10 backdrop-blur-xl border border-white/20 w-full max-w-sm rounded-4xl p-8 shadow-2xl relative overflow-hidden"
- close: <Button variant="ghost" iconOnly aria-label="Close"> avec <X> text-white/70

Variante LIGHT/sheet à AJOUTER — classes verbatim depuis FiltersModal.tsx l.29-37 :
- container: "fixed inset-0 z-[3000] flex items-end sm:items-center justify-center pointer-events-none"
- backdrop: "absolute inset-0 bg-black/20 backdrop-blur-sm pointer-events-auto" (onClick={onClose})
- panel: "relative z-10 bg-white w-full max-w-sm sm:rounded-3xl rounded-t-3xl p-6 shadow-2xl pointer-events-auto"
- animation: initial={{y:"100%"}} animate={{y:0}} exit={{y:"100%"}}
- PAS de bouton close interne (le close vit dans le <Header> de FiltersModal)

Header (src/ui/Header.tsx) — forme rangée-avec-close déjà disponible :
`<Header surface="light" title={...} onClose={...} />` → rend text-text (slate-800) + close bg-slate-100/text-slate-600 (identique aux littéraux de FiltersModal l.38-43).

AuthModal (src/components/AuthModal.tsx l.83-89) — SEUL autre consommateur de Modal, forme de composition de référence, surface glass :
`<Modal isOpen onClose><Header surface="glass" title subtitle />{body}</Modal>`

FiltersModalProps (contrat conservé, Map.tsx l.368-373 ne change pas) :
`{ isOpen, onClose, selectedFilter: StartType|'All', onFilterChange }`

NavBar — occurrences wirables (mobile actif l.82, 93, 125, 133) : `text-sky-500` → `text-primary`.
NON wirables (restent littéraux, Pitfall 5) : desktop `bg-sky-50 text-sky-600`, gradients
`from-sky-500 to-blue-600` / `from-sky-400 to-blue-500`, `shadow-sky-500/25`, `shadow-sky-500/30`.
Exceptions legacy à PRÉSERVER (UI-SPEC) : labels mobiles `text-[10px] font-medium`, `p-3`/`gap-3`.
Boutons d'onglet = `<button>` natifs, NON migrés vers `src/ui/Button` (D-02 CONTEXT §D-04 :
aucun variant Button ne matche la forme d'un onglet ; miroir du précédent A4 / CTA custom). NAV-01
satisfait pour la NavBar par le wiring de tokens, pas par une migration structurelle vers Button.

App.tsx — instances NavBar (desktop l.101-108 `isVertical`, mobile l.227-233) recevant des callbacks
INLINE recréés à chaque render : `onTabChange={(tab)=>{setActiveTab(tab);setSelectedSpot(null);}}`,
`onAddSpotClick={()=>setIsAddingSpotMode(true)}`, `onOpenAuth={()=>setIsAuthModalOpen(true)}`.
Ces callbacks sont à stabiliser via `useCallback` (setters React stables → deps `[]`) pour que
`React.memo(NavBar)` évite les re-renders sur changement d'état non-nav (PERF-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Étendre src/ui/Modal avec une variante claire/bottom-sheet (Pitfall 1)</name>
  <files>src/ui/Modal.tsx</files>
  <read_first>
    - src/ui/Modal.tsx (master actuel, glass codé en dur)
    - src/ui/Header.tsx lignes 3-26 (précédent exact de la prop `surface` avec défaut glass rétro-compatible)
    - src/components/FiltersModal.tsx lignes 27-43 (source verbatim des classes light/sheet)
    - src/components/AuthModal.tsx lignes 83-89 (consommateur glass à ne pas régresser)
    - .planning/phases/02-navigation-vue-carte-spots/02-RESEARCH.md § Pitfall 1
    - .planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md (confirme AuthModal seul consommateur)
  </read_first>
  <action>
    Ajouter à `ModalProps` une prop `surface?: 'glass' | 'light'` (défaut `'glass'`) et si utile
    `layout?: 'center' | 'sheet'` (défaut `'center'`), en répliquant EXACTEMENT le pattern de
    `Header.tsx` (prop optionnelle + défaut = rendu actuel → rétro-compatible). Le rendu `glass`
    par défaut DOIT rester byte-identique à l'actuel (container `z-[5000] bg-black/60 backdrop-blur-md`,
    panel `bg-white/10 backdrop-blur-xl rounded-4xl p-8` animé en `scale`, close Button ghost). Ajouter
    la branche `light`/`sheet` avec les classes extraites verbatim de FiltersModal l.29-37 : container
    `z-[3000] items-end sm:items-center pointer-events-none`, backdrop `bg-black/20 backdrop-blur-sm`
    cliquable (`onClose`), panel `bg-white sm:rounded-3xl rounded-t-3xl p-6`, animation slide `y:100%↔0`.
    En surface `light`, NE PAS rendre le bouton close interne (il vit dans le `<Header>` du consommateur)
    — conditionner le Button close au cas `glass`. Ne changer AUCUN autre comportement de Modal.
    Vérifier que le défaut glass laisse AuthModal strictement inchangé.
  </action>
  <verify>
    <automated>npx tsc -b && grep -q "surface" src/ui/Modal.tsx && grep -q "z-\[3000\]" src/ui/Modal.tsx && grep -q "z-\[5000\]" src/ui/Modal.tsx && grep -q "rounded-t-3xl" src/ui/Modal.tsx</automated>
    <human-check>Ouvrir AuthModal (flux auth) : surface glass strictement inchangée par rapport à avant. Le défaut glass ne régresse pas le seul consommateur existant.</human-check>
  </verify>
  <done>Modal expose surface (défaut glass) ; branche light/sheet présente avec les classes verbatim de FiltersModal ; glass inchangé ; typecheck OK ; AuthModal non régressé.</done>
  <acceptance_criteria>
    - `npx tsc -b` réussit (exit 0).
    - `grep "surface" src/ui/Modal.tsx` matche ; défaut = `'glass'` (assertion source : `surface = 'glass'`).
    - Le rendu glass conserve `z-[5000]`, `bg-white/10 backdrop-blur-xl`, `rounded-4xl p-8` (assertion source : ces classes présentes dans la branche glass).
    - La branche light contient `z-[3000]`, `bg-black/20 backdrop-blur-sm`, `bg-white`, `rounded-t-3xl p-6`, animation `y:"100%"` (assertion source verbatim FiltersModal).
    - AuthModal.tsx n'est PAS modifié et rend identiquement (human-check visuel : ouvrir l'auth, surface glass inchangée).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 : Migrer FiltersModal vers Modal + Header (D-02)</name>
  <files>src/components/FiltersModal.tsx</files>
  <read_first>
    - src/components/FiltersModal.tsx (fichier cible complet, l.1-87)
    - src/ui/Modal.tsx (variante light/sheet ajoutée en Task 1)
    - src/ui/Header.tsx (forme rangée-avec-close, surface light)
    - src/components/AuthModal.tsx lignes 83-89 (forme de composition de référence)
    - .planning/phases/02-navigation-vue-carte-spots/02-PATTERNS.md § FiltersModal.tsx (mapping exact + ce qui reste custom)
  </read_first>
  <action>
    Remplacer le shell de `FiltersModal` (le `<AnimatePresence>` + container + backdrop + `motion.div`
    panel, l.27-37 et l.82-85) par `<Modal isOpen={isOpen} onClose={onClose} surface="light" layout="sheet">`.
    Remplacer le header custom (l.38-43) par `<Header surface="light" title={t('filters.title')} onClose={onClose} />`
    (byte-identique par construction : Header rend `text-text`=slate-800 + close `bg-slate-100`/`text-slate-600`).
    CONSERVER TEL QUEL, à l'intérieur du Modal : le label `t('filters.start_type')` (l.45), les lignes de
    filtre custom (l.48-71, état sélectionné `border-sky-500 bg-sky-50`, chips, pastille check — AUCUN
    variant Button ne matche, NE PAS forcer) et le bouton « Voir les résultats » `bg-slate-900` (l.75-80 —
    aucun variant Button ne matche `bg-slate-900`, le garder custom pour minimiser le scope). Le contrat de
    props `FiltersModalProps` reste identique (Map.tsx l.368-373 inchangé). Retirer l'import framer-motion
    devenu inutile si le shell passe entièrement par Modal.
  </action>
  <verify>
    <automated>npx tsc -b && grep -q "from '../ui/Modal'" src/components/FiltersModal.tsx && grep -q "surface=\"light\"" src/components/FiltersModal.tsx && grep -q "bg-slate-900" src/components/FiltersModal.tsx && grep -q "border-sky-500" src/components/FiltersModal.tsx</automated>
    <human-check>Ouvrir le filtre carte : bottom-sheet clair identique à avant (position bas d'écran, backdrop bg-black/20, slide vertical, radius haut). Sélection de filtre fonctionne comme avant. AuthModal non régressé.</human-check>
  </verify>
  <done>FiltersModal consomme Modal (surface light) + Header ; lignes de filtre et CTA slate-900 conservés custom ; contrat de props inchangé ; typecheck OK ; rendu bottom-sheet identique.</done>
  <acceptance_criteria>
    - `npx tsc -b` réussit (exit 0).
    - `grep "from '../ui/Modal'" src/components/FiltersModal.tsx` et `grep "surface=\"light\"" ...` matchent.
    - Les lignes de filtre custom sont préservées (assertion source : `border-sky-500`, `bg-sky-50`, pastille `bg-sky-500` présentes).
    - Le CTA `bg-slate-900` est préservé (assertion source : `bg-slate-900` présent).
    - `FiltersModalProps` inchangé ; Map.tsx l.368-373 non modifié (assertion : `git status` ne liste pas Map.tsx pour ce plan).
    - Behavior (human-check) : bottom-sheet clair identique (position, backdrop, slide-y, radius top) ; sélection de filtre OK ; AuthModal non régressé.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 : NavBar — wiring tokens (NAV-01) + mémoïsation nav-shell (PERF-01)</name>
  <files>src/components/NavBar.tsx, src/App.tsx</files>
  <read_first>
    - src/components/NavBar.tsx (fichier cible, l.76-140 pour la bottom bar mobile ; export l.13/141)
    - src/App.tsx l.101-108 (instance NavBar desktop) + l.227-233 (instance NavBar mobile) + l.28-41 (états/setters)
    - src/ui/Button.tsx lignes 16-30 (précédent canonique du wiring byte-identique avec commentaire de justification)
    - src/index.css lignes 13-19 (--color-primary: var(--color-sky-500))
    - .planning/phases/02-navigation-vue-carte-spots/02-PATTERNS.md § NavBar.tsx (occurrences exactes, exceptions à préserver)
    - .planning/phases/02-navigation-vue-carte-spots/02-UI-SPEC.md § Color + exceptions typographiques
    - .planning/phases/02-navigation-vue-carte-spots/02-CONTEXT.md §D-04 (boutons natifs conservés — rationale)
    - .planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md (baseline nav-shell AVANT refactor à comparer)
  </read_first>
  <action>
    **Partie A — wiring tokens (NAV-01).** Remplacer les 4 occurrences `text-sky-500` des états actifs de la
    bottom bar mobile (l.82, 93, 125, 133) par `text-primary` — `--color-primary` aliase `var(--color-sky-500)`,
    rendu strictement identique. Ajouter un commentaire de justification bref sur le modèle de Button.tsx l.22.
    Les boutons d'onglet RESTENT des `<button>` natifs (par D-04 : aucun variant `src/ui/Button` ne matche la
    forme d'un onglet — `Button` est `flex items-center justify-center`, un onglet est `flex flex-col items-center`
    (mobile) / `flex items-center gap-3 text-left` (desktop) ; forcer un variant violerait le principe DS D-06
    « variants widen the API, never the appearance »). NE PAS migrer vers `Button`, NE PAS inventer un variant
    nav-tab. NE PAS toucher aux occurrences NON wirables (Pitfall 5) : desktop actif `bg-sky-50 text-sky-600`
    (l.21, 32, 43, 51), gradients CTA `from-sky-500 to-blue-600` (l.63), `from-sky-400 to-blue-500` (l.110),
    ombres `shadow-sky-500/25` (l.63) / `shadow-sky-500/30` (l.110) — RESTENT littéraux. PRÉSERVER les exceptions
    legacy (UI-SPEC) : labels `text-[10px] font-medium`, `p-3`/`gap-3` — NE PAS les « corriger ». Le libellé
    « Add Spot » codé en dur reste tel quel (hors scope i18n).

    **Partie B — mémoïsation nav-shell (PERF-01, byte-identique).** Envelopper l'export de `NavBar` dans
    `React.memo` (importer `memo` de `react` ; renommer la fonction en `NavBarComponent` ou similaire et
    `export default memo(NavBarComponent)`) — `NavBar` est purement présentationnel, `React.memo` ne change AUCUN
    rendu. Dans `App.tsx`, stabiliser les 3 callbacks passés aux DEUX instances de NavBar via `useCallback` (deps
    `[]`, les setters `setActiveTab`/`setSelectedSpot`/`setIsAddingSpotMode`/`setIsAuthModalOpen` sont stables) :
    `handleTabChange = useCallback((tab)=>{ setActiveTab(tab); setSelectedSpot(null); }, [])`,
    `handleAddSpotClick = useCallback(()=>setIsAddingSpotMode(true), [])`,
    `handleOpenAuth = useCallback(()=>setIsAuthModalOpen(true), [])`. Câbler ces callbacks stables sur les deux
    `<NavBar>` (desktop l.101-108 et mobile l.227-233) à la place des arrow-functions inline. Objectif : après
    ce changement, `NavBar` ne se re-render QUE quand `activeTab`/`user` (props légitimes) ou la langue changent,
    et PLUS lors d'un changement d'état non-nav (spots, favoris, selectedSpot, isAdminOpen, showWelcome…).
    **Aucun comportement observable ne change** — memo + useCallback sont des optimisations référentielles pures.
    Ne PAS mémoïser d'autres composants ni modifier d'autres props/handlers d'App.tsx.
  </action>
  <verify>
    <automated>npx tsc -b && test "$(grep -c 'text-primary' src/components/NavBar.tsx)" -eq 4 && test "$(grep -c 'text-sky-500' src/components/NavBar.tsx)" -eq 0 && grep -q "from-sky-500 to-blue-600" src/components/NavBar.tsx && grep -Eq "memo\(" src/components/NavBar.tsx && test "$(grep -c 'useCallback' src/App.tsx)" -ge 3</automated>
    <human-check>1) Diff DevTools computed styles `text-sky-500` (avant) vs `text-primary` (après) : identique sur les 4 onglets mobiles actifs ; nav desktop + mobile inchangées visuellement. 2) Rejouer le protocole nav-shell de 02-BASELINE.md APRÈS refactor : les 3 actions non-nav (ouvrir/fermer SpotDetail, toggle filtre, favori) ne déclenchent plus de render de `NavBar` (mobile+desktop) ; comparer aux chiffres AVANT (cible : 0 render superflu). Naviguer entre onglets + auth : NavBar re-render toujours correctement quand activeTab/user changent.</human-check>
  </verify>
  <done>4 text-sky-500 mobiles → text-primary ; boutons d'onglet natifs conservés (D-04) ; littéraux non wirables + exceptions legacy préservés ; NavBar exporté en React.memo ; 3 callbacks App.tsx stabilisés via useCallback ; typecheck OK ; rendu identique ; 0 render nav-shell superflu sur état non-nav.</done>
  <acceptance_criteria>
    - `npx tsc -b` réussit (exit 0).
    - `grep -c "text-primary" src/components/NavBar.tsx` == 4 ; `grep -c "text-sky-500" src/components/NavBar.tsx` == 0.
    - `grep "from-sky-500 to-blue-600" src/components/NavBar.tsx` matche (gradient préservé).
    - Boutons d'onglet restent `<button>` natifs (assertion source : aucun `import ... from '../ui/Button'` ajouté dans NavBar.tsx ; D-04 respecté).
    - `grep -E "memo\(" src/components/NavBar.tsx` matche (export React.memo) ; `import` de `memo` présent.
    - `grep -c "useCallback" src/App.tsx` ≥ 3 (les 3 callbacks NavBar stabilisés) ; les 2 instances `<NavBar>` reçoivent les callbacks stables.
    - Exceptions legacy préservées (assertion source : `text-[10px] font-medium`, `p-3`, `gap-3` toujours présents).
    - Behavior (human-check) : rendu byte-identique ; nav re-render sur activeTab/user OK ; 0 render nav-shell superflu sur état non-nav (vs 02-BASELINE.md).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Refactor de chrome UI (Modal/FiltersModal/NavBar) — aucun nouveau flux d'auth, d'entrée serveur ni de crypto. Surface d'attaque inchangée. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | src/ui/Modal (consommé par AuthModal) | accept | Défaut `surface='glass'` rétro-compatible ; AuthModal (flux auth) non modifié — aucune régression de comportement de sécurité. Vérifié : AuthModal seul autre consommateur (02-BASELINE.md). |
| T-02-05 | Elevation of Privilege | FiltersModal / NavBar | accept | Composants de présentation sans logique d'autorisation ; le gating favoris/add-spot (`user`/`onOpenAuth`) reste inchangé. |
| T-02-06 | Tampering | App.tsx callbacks NavBar (useCallback) + React.memo | accept | Optimisation référentielle pure — mêmes setters, mêmes props effectives, aucun changement de flux ni de contrôle d'accès ; rendu byte-identique. |
| T-02-SC | Tampering (supply chain) | npm installs | accept (N/A) | Aucun `npm install` dans cette phase (RESEARCH § Package Legitimacy Audit = non applicable). |
</threat_model>

<verification>
- `npx tsc -b` passe après chaque tâche.
- Modal expose surface/layout, défaut glass ; AuthModal non régressé.
- FiltersModal consomme Modal+Header (surface light), rendu bottom-sheet identique ; filtres/CTA custom conservés.
- NavBar : 4 text-sky-500 → text-primary ; boutons d'onglet natifs conservés (D-04) ; littéraux non wirables et exceptions legacy préservés.
- Nav-shell mémoïsé : NavBar en React.memo + callbacks App.tsx stabilisés (useCallback) ; 0 render superflu sur état non-nav (Profiler vs 02-BASELINE.md, PERF-01).
- Recette manuelle nav (desktop + mobile) + ouverture filtre + ouverture auth : 0 régression.
</verification>

<success_criteria>
- Navigation globale + bottom bar + FiltersModal consomment composants et tokens du DS (NAV-01), sans changement de comportement utilisateur.
- Re-renders inutiles du nav-shell éliminés (NavBar mémoïsé, callbacks stabilisés), mesure Profiler avant/après (PERF-01 « nav » — ROADMAP success criterion 4).
- Aucun consommateur existant de Modal (AuthModal) régressé.
</success_criteria>

<output>
Créer `.planning/phases/02-navigation-vue-carte-spots/02-03-SUMMARY.md` une fois terminé.
</output>
