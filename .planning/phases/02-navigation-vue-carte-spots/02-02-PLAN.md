---
phase: 02-navigation-vue-carte-spots
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/config/mapbox.ts
  - src/components/Map.tsx
  - src/components/AddSpotForm.tsx
autonomous: true
requirements: [MAP-01, MAP-02, PERF-01]

must_haves:
  truths:
    - "Les couleurs des markers/clusters sont centralisées dans une constante MAP_COLORS, rendu strictement identique à avant"
    - "Toggler un filtre carte ne ré-alloue plus les objets Feature quand seul le filtre change (Profiler avant/après)"
    - "Les compteurs de cluster restent corrects après filtrage (chemin setData conservé, pas de setFilter natif)"
    - "Le set de markers visibles pour chaque filtre est identique à avant le refactor (spots multi-type inclus)"
    - "Ajouter/retirer/réajouter des photos dans AddSpotForm ne casse aucun aperçu encore affiché"
    - "La rétention des Blob/object URLs reste stable après plusieurs cycles open/close du formulaire"
  artifacts:
    - path: "src/config/mapbox.ts"
      provides: "Constante MAP_COLORS (couleurs markers, valeurs hex verbatim)"
      contains: "MAP_COLORS"
    - path: "src/components/Map.tsx"
      provides: "Split-memoization allFeatures/filtered + refs MAP_COLORS dans les layers"
      contains: "allFeatures"
    - path: "src/components/AddSpotForm.tsx"
      provides: "Révocation des blob URLs scopée au reset + garde au démontage"
      contains: "revokeAll"
  key_links:
    - from: "src/components/Map.tsx"
      to: "src/config/mapbox.ts MAP_COLORS"
      via: "import + injection dans les expressions paint"
      pattern: "MAP_COLORS"
    - from: "src/components/Map.tsx"
      to: "Mapbox source 'spots'"
      via: "<Source data={spotsGeoJson} cluster> → setData (conservé)"
      pattern: "data=\\{spotsGeoJson\\}"
    - from: "src/components/AddSpotForm.tsx resetForm"
      to: "URL.revokeObjectURL"
      via: "revokeAll() appelé avant de vider imagePreviews"
      pattern: "revokeAll"
---

<objective>
Optimiser la vue Carte — l'écran le plus critique de l'app — sur trois axes sans **aucune
régression fonctionnelle ni visuelle** : (D-01) centraliser les 17 occurrences hex des
layers Mapbox dans une constante `MAP_COLORS` ; (MAP-01/PERF-01) séparer la génération
coûteuse des objets `Feature` de leur filtrage bon marché (split-memoization) ; (MAP-02/D-03)
corriger la fuite mémoire des aperçus photo d'`AddSpotForm`.

Purpose : réduire les allocations/GC au toggle de filtre et supprimer la révocation
prématurée des blob URLs, tout en conservant des compteurs de cluster corrects. La valeur
du refactor est de **supprimer** du code fragile (hex épars, cleanup mal scopé), pas d'en
ajouter.

Output : `MAP_COLORS` dans `src/config/mapbox.ts`, `Map.tsx` refactoré (couleurs + mémoïsation),
`AddSpotForm.tsx` avec révocation blob URL correctement scopée.

**Contrainte cardinale (héritée Phase 1) : byte-identique. Aucun rendu utilisateur ne change.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-navigation-vue-carte-spots/02-CONTEXT.md
@.planning/phases/02-navigation-vue-carte-spots/02-RESEARCH.md
@.planning/phases/02-navigation-vue-carte-spots/02-PATTERNS.md
@.planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md

<interfaces>
<!-- Contrats existants extraits du code. L'exécuteur les utilise directement. -->

src/config/mapbox.ts (état actuel — module-level config object `as const`-style) :
```typescript
export const mapboxConfig = {
    accessToken: 'pk.eyJ1Ijoi...',
    styleUrl: 'mapbox://styles/mapbox/satellite-streets-v12'
};
```

Map.tsx — mémoïsation ACTUELLE à scinder (lignes 141-159) :
```typescript
const spotsGeoJson: FeatureCollection = useMemo(() => {
  const filtered = filter === 'All' ? spots : spots.filter(s => s.type.includes(filter as StartType));
  return { type: 'FeatureCollection', features: filtered.map(spot => ({
    type: 'Feature',
    geometry: { type: 'Point', coordinates: [spot.position[1], spot.position[0]] },
    properties: { id: spot.id, name: spot.name, type: spot.type[0], is_approved: spot.is_approved },
  })) };
}, [spots, filter]);
```

Valeurs hex à centraliser (Map.tsx lignes 25-45, 58-68, 77-95 — verbatim, D-01) :
clusterLayer circle-color step: '#22d3ee' (<5), '#38bdf8' (5-20), '#ffffff' (>20) ; stroke '#fff'
clusterCountLayer text-color step: '#ffffff', '#ffffff', '#0f172a'
unclusteredPoint case: pending '#f97316' ; match type: Dockstart '#38bdf8', Rockstart '#f472b6',
  Dropstart '#2dd4bf', Deadstart '#818cf8', Rampstart '#fbbf24', Beachstart '#f59e0b', default '#38bdf8' ; stroke '#fff'

AddSpotForm.tsx — pattern CORRECT déjà présent (handleRemoveImage l.56-60, KEEP as-is) ;
resetForm (l.27-35, appelé à l'ouverture l.39 et après submit) ; bug à remplacer (l.62-66).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Centraliser les couleurs markers dans MAP_COLORS (D-01)</name>
  <files>src/config/mapbox.ts, src/components/Map.tsx</files>
  <read_first>
    - src/config/mapbox.ts (fichier cible, pattern config object existant)
    - src/components/Map.tsx lignes 19-97 (les 3 constantes LayerProps contenant les hex)
    - .planning/phases/02-navigation-vue-carte-spots/02-PATTERNS.md § src/config/mapbox.ts (valeurs verbatim + garde byte-identique)
  </read_first>
  <action>
    Ajouter dans `src/config/mapbox.ts` un export `MAP_COLORS` (objet `as const`) regroupant
    exactement les valeurs hex actuelles : `clusterSmall: '#22d3ee'`, `clusterMedium: '#38bdf8'`,
    `clusterLarge: '#ffffff'`, `clusterTextLight: '#ffffff'`, `clusterTextDark: '#0f172a'`,
    `pending: '#f97316'`, `Dockstart: '#38bdf8'`, `Rockstart: '#f472b6'`, `Dropstart: '#2dd4bf'`,
    `Deadstart: '#818cf8'`, `Rampstart: '#fbbf24'`, `Beachstart: '#f59e0b'`, `markerStroke: '#fff'`.
    Dans `src/components/Map.tsx`, importer `MAP_COLORS` depuis `../config/mapbox` et remplacer
    chaque littéral hex des expressions `paint` des 3 layers (`clusterLayer`, `clusterCountLayer`,
    `unclusteredPointLayer`) par la ref `MAP_COLORS.*` correspondante. Les layers restent des
    constantes module `LayerProps` — ne changer QUE les valeurs de couleur. **Byte-identique
    strict** : conserver `markerStroke` en `'#fff'` (3 caractères, comme l'actuel `circle-stroke-color`),
    NE PAS normaliser en `'#ffffff'`. NE wirer AUCUNE couleur marker vers un token CSS : `#38bdf8`
    est sky-400 (v3-era) ≠ `--color-primary` (sky-500 v4 ≈ #00a6f4), donc toutes restent des hex
    dans MAP_COLORS (D-01). La couleur de fog `#242B4B` (Map.tsx l.292) N'EST PAS un marker et
    reste littérale hors MAP_COLORS.
  </action>
  <verify>
    <automated>npx tsc -b && grep -q "export const MAP_COLORS" src/config/mapbox.ts && grep -c "MAP_COLORS\." src/components/Map.tsx && ! grep -E "'#(22d3ee|f472b6|2dd4bf|818cf8|fbbf24|f59e0b|f97316)'" src/components/Map.tsx</automated>
  </verify>
  <done>MAP_COLORS exporté avec les 13 clés ; les 3 layers de Map.tsx référencent MAP_COLORS.* ; plus aucun hex de type-de-spot littéral dans Map.tsx ; typecheck OK.</done>
  <acceptance_criteria>
    - `npx tsc -b` réussit (exit 0).
    - `grep "export const MAP_COLORS" src/config/mapbox.ts` matche.
    - `grep -c "MAP_COLORS\." src/components/Map.tsx` ≥ 13 (chaque hex des 3 layers remplacé).
    - `grep -E "'#(22d3ee|f472b6|2dd4bf|818cf8|fbbf24|f59e0b|f97316)'" src/components/Map.tsx` ne matche RIEN (assertion source : plus de hex marker inline).
    - `MAP_COLORS.markerStroke === '#fff'` (byte-identique, pas `#ffffff`).
    - Vérif visuelle rapide (`npm run dev`) : clusters et markers rendent avec exactement les mêmes couleurs qu'avant.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 : Split-memoization de la source GeoJSON (MAP-01 / PERF-01)</name>
  <files>src/components/Map.tsx</files>
  <read_first>
    - src/components/Map.tsx lignes 141-159 (mémoïsation actuelle à scinder) + 300-311 (<Source>/<Layer>)
    - .planning/phases/02-navigation-vue-carte-spots/02-RESEARCH.md § Pattern 1 + Pitfalls 2 & 4
    - .planning/phases/02-navigation-vue-carte-spots/02-PATTERNS.md § Map.tsx (garde de régression)
    - .planning/phases/02-navigation-vue-carte-spots/02-BASELINE.md (chiffres avant refactor)
  </read_first>
  <action>
    Scinder le `useMemo` unique `spotsGeoJson` (l.141-159) en deux : (1) `allFeatures = useMemo(..., [spots])`
    qui construit le tableau `Feature[]` (géométrie + properties) une seule fois par changement de
    `spots` ; (2) `spotsGeoJson = useMemo(..., [allFeatures, filter])` qui assemble la FeatureCollection
    en filtrant. Conserver `<Source data={spotsGeoJson} cluster>` INCHANGÉ — NE PAS remplacer par
    `map.setFilter()` (casse les compteurs de cluster sur source clusterisée, Pitfall 2). La
    mémoïsation est purement une optimisation d'allocation côté React. **Garde de régression
    critique (Pitfall 4)** : le filtre actuel teste `s.type.includes(filter)` sur le tableau COMPLET
    des types du spot, alors que chaque feature ne stocke que `type: spot.type[0]`. Pour rester
    byte-identique côté ensemble de markers, filtrer sur l'accès au spot complet — soit conserver le
    filtrage sur `spots` puis mapper, soit stocker le tableau `spot.type` complet dans les properties
    et filtrer dessus avec `.includes(filter)`. NE PAS filtrer sur `properties.type` seul (ferait
    disparaître les spots multi-type). Cas `filter === 'All'` : retourner `allFeatures` sans `.filter()`.
    Aucun autre comportement (handlers, clic cluster, fly-to) ne change.
  </action>
  <verify>
    <automated>npx tsc -b && grep -q "allFeatures" src/components/Map.tsx && grep -q "data={spotsGeoJson}" src/components/Map.tsx && ! grep -q "setFilter" src/components/Map.tsx</automated>
    <human-check>Rejouer le protocole Profiler de 02-BASELINE.md après refactor : moins d'allocations/renders au toggle de filtre, compteurs de cluster corrects, comportement identique. Comparer aux chiffres AVANT. Vérifier que pour CHAQUE filtre, les markers visibles sont exactement les mêmes qu'avant (spots multi-type inclus).</human-check>
  </verify>
  <done>Deux useMemo distincts (allFeatures sur [spots], spotsGeoJson sur [allFeatures, filter]) ; <Source data={spotsGeoJson}> conservé ; aucun setFilter ; typecheck OK ; ensemble de markers identique par filtre.</done>
  <acceptance_criteria>
    - `npx tsc -b` réussit (exit 0).
    - `grep "allFeatures" src/components/Map.tsx` matche (nouveau useMemo mémoïsé sur [spots]).
    - `grep "data={spotsGeoJson}" src/components/Map.tsx` matche (chemin setData conservé).
    - `grep "setFilter" src/components/Map.tsx` ne matche RIEN (Pitfall 2 respecté).
    - Recette manuelle (behavior) : pour CHAQUE filtre, les markers visibles sont EXACTEMENT les mêmes qu'avant refactor (spots multi-type inclus).
    - Profiler (human-check) : toggler les filtres ne ré-alloue plus `allFeatures` ; renders du sous-arbre carte ≤ baseline 02-BASELINE.md ; compteurs de cluster corrects.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 : Corriger la fuite mémoire des aperçus photo d'AddSpotForm (MAP-02 / D-03)</name>
  <files>src/components/AddSpotForm.tsx</files>
  <read_first>
    - src/components/AddSpotForm.tsx lignes 15-66 (états, resetForm, handleRemoveImage, useEffect fautif)
    - .planning/phases/02-navigation-vue-carte-spots/02-RESEARCH.md § Pattern 3 + Pitfall 3
    - .planning/phases/02-navigation-vue-carte-spots/02-PATTERNS.md § AddSpotForm.tsx
  </read_first>
  <action>
    Remplacer le `useEffect` fautif (l.62-66) qui révoque `imagePreviews` à CHAQUE changement de
    `imagePreviews` (donc révoque des URLs d'images encore affichées dès qu'une nouvelle est ajoutée).
    Cible : (1) un `useRef` miroir de `imagePreviews` alimenté par un `useEffect(..., [imagePreviews])`
    SANS cleanup ; (2) un callback `revokeAll` (mémoïsé `useCallback`) qui parcourt le ref et appelle
    `URL.revokeObjectURL` sur chaque URL ; (3) appeler `revokeAll()` DANS `resetForm` AVANT de vider
    (`setImagePreviews([])`) — `resetForm` est déclenché à l'ouverture (l.39) et après submit, ce qui
    couvre les fermetures ; (4) une garde de démontage réel `useEffect(() => () => revokeAll(), [revokeAll])`.
    **Pitfall 3 critique** : `AddSpotForm` est rendu INCONDITIONNELLEMENT dans Map.tsx (l.358) — le
    composant n'est jamais démonté en usage normal, donc un cleanup au seul démontage ne s'exécuterait
    JAMAIS. La révocation DOIT passer par `resetForm`/reset, pas uniquement par le démontage. CONSERVER
    `handleRemoveImage` (l.56-60) tel quel — il révoque déjà correctement l'URL retirée. **AUCUNE
    modification visuelle** du formulaire (D-03 = fix de bug, migration DS = Phase 4).
  </action>
  <verify>
    <automated>npx tsc -b && grep -q "revokeAll" src/components/AddSpotForm.tsx && ! grep -Pzo "useEffect\(\s*\(\)\s*=>\s*\{\s*return\s*\(\)\s*=>\s*\{\s*imagePreviews\.forEach" src/components/AddSpotForm.tsx</automated>
    <human-check>Rejouer le protocole mémoire de 02-BASELINE.md : après 5 cycles open/close du formulaire, la rétention Blob/object URLs n'augmente plus de façon monotone (comparer au snapshot AVANT). Ajouter/retirer/réajouter des photos : aucune image affichée cassée.</human-check>
  </verify>
  <done>useEffect fautif remplacé ; revokeAll appelé dans resetForm avant reset + garde au démontage ; handleRemoveImage inchangé ; aucun changement visuel ; typecheck OK.</done>
  <acceptance_criteria>
    - `npx tsc -b` réussit (exit 0).
    - `grep "revokeAll" src/components/AddSpotForm.tsx` matche.
    - L'ancien cleanup `useEffect(..., [imagePreviews])` révoquant tout `imagePreviews` n'existe plus (assertion source : plus de `imagePreviews.forEach(url => URL.revokeObjectURL` dans un cleanup dépendant de `[imagePreviews]`).
    - `handleRemoveImage` (l.56-60) est inchangé (assertion : révoque toujours `imagePreviews[index]`).
    - Aucun élément JSX/classe du formulaire n'a changé (assertion : `git diff src/components/AddSpotForm.tsx` ne touche pas au `return`/markup visuel, seulement la logique blob URL).
    - Behavior (human-check) : ajouter 5 images → en retirer une → en réajouter → aucun aperçu encore affiché n'est cassé.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ ressources navigateur (blob URLs) | Cycle de vie `URL.createObjectURL`/`revokeObjectURL` — doit être piloté explicitement par React pour éviter la fuite de ressource. |
| React ↔ Mapbox GL (WebGL) | `source.setData()` — frontière de rendu ; couleurs statiques, aucune entrée non fiable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service (ressource browser) | AddSpotForm blob URL lifecycle | mitigate | Révocation scopée au reset + garde démontage (D-03) — supprime la croissance mémoire monotone après cycles open/close ; vérifiée par snapshots mémoire vs baseline. |
| T-02-02 | Tampering | MAP_COLORS / expressions paint Mapbox | accept | Constantes statiques, aucune entrée utilisateur ni externe n'alimente les couleurs — surface d'attaque nulle. |
| T-02-03 | Information Disclosure | Map.tsx properties GeoJSON | accept | Ne contient que id/name/type/is_approved déjà publics (source Supabase inchangée, hors scope). |
| T-02-SC | Tampering (supply chain) | npm installs | accept (N/A) | Aucun `npm install` dans cette phase (RESEARCH § Package Legitimacy Audit = non applicable). |
</threat_model>

<verification>
- `npx tsc -b` passe après chaque tâche.
- MAP_COLORS centralise toutes les couleurs des 3 layers ; aucun hex marker littéral résiduel dans Map.tsx.
- Split-memoization en place, `setData` conservé, pas de `setFilter`.
- Ensemble de markers identique par filtre (recette manuelle, spots multi-type inclus).
- Fix blob URL scopé au reset ; handleRemoveImage inchangé ; aucun changement visuel du formulaire.
- Profiler avant/après : moins de renders/allocations au toggle filtre (vs 02-BASELINE.md).
- Mémoire avant/après : rétention Blob/object URLs stable après cycles open/close.
</verification>

<success_criteria>
- Couleurs markers centralisées, rendu byte-identique (MAP-01/D-01).
- Re-renders/allocations inutiles de la carte éliminés, compteurs de cluster corrects (MAP-01/PERF-01).
- Fuite mémoire des aperçus photo corrigée sans régression visuelle (MAP-02/D-03).
</success_criteria>

<output>
Créer `.planning/phases/02-navigation-vue-carte-spots/02-02-SUMMARY.md` une fois terminé,
avec les chiffres Profiler/mémoire APRÈS comparés à 02-BASELINE.md.
</output>
