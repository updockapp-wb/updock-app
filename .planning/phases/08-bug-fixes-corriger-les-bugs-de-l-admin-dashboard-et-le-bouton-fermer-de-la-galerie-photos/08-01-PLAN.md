---
phase: 08-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Profile.tsx
  - src/context/ProfileContext.tsx
  - src/components/ReviewList.tsx
  - src/components/SessionCard.tsx
  - src/components/SpotDetail.tsx
autonomous: true
requirements: [BUG-06, BUG-07, BUG-08, BUG-09]

must_haves:
  truths:
    - "Profile screen shows 'Updock v1.1.3 (Beta)' in both authenticated and anonymous views"
    - "No ROOKIE/Pro/Expert badge appears anywhere in the profile"
    - "Spots Added card shows the real count of spots created by the user from Supabase"
    - "Default avatar everywhere is a Lucide User icon, not an SVG file"
    - "Preset avatar picker grid is removed from Profile"
    - "selectPresetAvatar function is removed from ProfileContext"
  artifacts:
    - path: "src/components/Profile.tsx"
      provides: "Fixed version, no badge, real spots count, no preset avatar grid"
      contains: "v1.1.3"
    - path: "src/context/ProfileContext.tsx"
      provides: "Cleaned ProfileContext without selectPresetAvatar"
    - path: "src/components/ReviewList.tsx"
      provides: "Default avatar with Lucide User icon"
    - path: "src/components/SessionCard.tsx"
      provides: "Default avatar with Lucide User icon"
    - path: "src/components/SpotDetail.tsx"
      provides: "Default avatar with Lucide User icon for uploader"
  key_links:
    - from: "src/components/Profile.tsx"
      to: "src/context/ProfileContext.tsx"
      via: "useProfile() hook — selectPresetAvatar removed from both"
      pattern: "useProfile"
---

<objective>
Fix Profile screen bugs: correct version number, remove gamification badge, fix spots count stat, and replace preset SVG avatars with a neutral Lucide icon everywhere.

Purpose: Address user-reported bugs D-06, D-07, D-08, D-09, D-10 from CONTEXT.md.
Output: Cleaned Profile.tsx, ProfileContext.tsx, and avatar fallback updates in ReviewList, SessionCard, SpotDetail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/Profile.tsx
@src/context/ProfileContext.tsx
@src/components/ReviewList.tsx
@src/components/SessionCard.tsx
@src/components/SpotDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Profile.tsx — version, badge, spots count, avatar picker removal</name>
  <files>src/components/Profile.tsx, src/context/ProfileContext.tsx</files>
  <read_first>src/components/Profile.tsx, src/context/ProfileContext.tsx, package.json</read_first>
  <action>
**In `src/components/Profile.tsx`:**

1. **D-06 — Version number (2 places):**
   - Line 120 (anonymous view): change `Updock v1.1.0 (Beta)` to `Updock v1.1.3 (Beta)`
   - Line 443 (authenticated view): change `Updock v1.1.0 (Beta)` to `Updock v1.1.3 (Beta)`

2. **D-07 — Remove ROOKIE badge entirely:**
   - Delete the `getLevel` function (lines 130-134)
   - Delete the `const level = getLevel(spotsCount);` line (line 136)
   - Delete the badge `<span>` element showing `level.name` (lines 179-181: the span with `level.bg`, `level.color`, `level.name`)
   - Keep the email display `<p>` below it

3. **D-08 — Fix spotsCount:**
   - Remove `const spotsCount = favorites.length;` (line 32)
   - Add a new state: `const [spotsCount, setSpotsCount] = useState(0);`
   - Add a `useEffect` that runs when `user` changes to fetch the count:
     ```
     useEffect(() => {
       if (!user) { setSpotsCount(0); return; }
       supabase
         .from('spots')
         .select('id', { count: 'exact', head: true })
         .eq('user_id', user.id)
         .then(({ count }) => setSpotsCount(count ?? 0));
     }, [user]);
     ```
   - Add `import { supabase } from '../lib/supabase';` at the top
   - The "Spots Added" card already displays `spotsCount` on line 310 — but currently hardcodes `0`. Remove the hardcoded `0` and replace with `{spotsCount}` (it was already using `spotsCount` for Favorites, but Spots Added was hardcoded). Actually re-reading: line 310 shows `0` literally. Replace the `0` on that line with `{spotsCount}`.
   - The "Favorites" card on line 314 already shows `{spotsCount}` but that was `favorites.length`. Since we renamed, update the Favorites card to show `{favorites.length}` directly instead of `{spotsCount}`.

4. **D-09/D-10 — Remove preset avatars, use Lucide User icon:**
   - Delete the entire `AVATARS` array constant (lines 13-19)
   - Delete the `const currentAvatar = ...` line (line 127)
   - Replace the avatar display logic in the header (lines 145-159): Instead of the current if/else chain with `profile?.avatar_url` and `currentAvatar.src`, use:
     - If `profile?.avatar_url` exists: show `<img src={profile.avatar_url} ...>`
     - Else: show `<User size={40} className="text-slate-300" />` (the Lucide User icon, already imported)
   - Delete the entire "Avatar Picker Modal/Popover" block (lines 201-263): the `{isAvatarPickerOpen && (...)}` conditional block
   - Remove `isAvatarPickerOpen`/`setIsAvatarPickerOpen` state (line 36)
   - Remove `isUpdating`/`setIsUpdating` state (line 37)
   - Remove `selectPresetAvatar` from the `useProfile()` destructure (line 31)
   - Change the edit avatar button (lines 165-170) to only trigger `fileInputRef.current?.click()` directly instead of toggling `isAvatarPickerOpen`. Replace:
     ```tsx
     <button onClick={() => setIsAvatarPickerOpen(!isAvatarPickerOpen)} ...>
       <Edit2 size={14} />
     </button>
     ```
     with:
     ```tsx
     <button onClick={() => fileInputRef.current?.click()} ...>
       <Camera size={14} />
     </button>
     ```
   - Move the hidden file input (currently inside the avatar picker at line 228-240) to right after the avatar edit button, since the picker is being deleted. The onChange handler stays the same (calls `uploadAvatar(file)`).

5. **Remove unused imports:** Remove `Edit2` from lucide-react import if no longer used. Keep `Camera` (already imported). Remove `X` only if not used elsewhere in the file (it's not used elsewhere after picker removal — check carefully, the display name section doesn't use X).

**In `src/context/ProfileContext.tsx`:**

6. **D-09/D-10 — Remove selectPresetAvatar:**
   - Remove `selectPresetAvatar` from `ProfileContextType` interface (line 16)
   - Remove the `selectPresetAvatar` function body (lines 102-116)
   - Remove `selectPresetAvatar` from the Provider value object (line 119)
   - Remove `avatar_id` from `ProfileData` interface (line 8) — no longer needed since no preset selection
   - Remove `avatar_id` from the select query (line 37): change to `select('display_name, avatar_url')`
   - Remove `avatar_id: data.avatar_id ?? 1` from the profile state setter (line 45)
  </action>
  <verify>
    <automated>cd C:/Users/wandr/updock-app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Profile.tsx contains `Updock v1.1.3 (Beta)` in exactly 2 places (grep returns 2 matches)
    - Profile.tsx does NOT contain `getLevel` or `level.name` or `Rookie` or `Expert`
    - Profile.tsx does NOT contain `AVATARS` array
    - Profile.tsx does NOT contain `isAvatarPickerOpen`
    - Profile.tsx does NOT contain `selectPresetAvatar`
    - Profile.tsx contains `supabase` import and `.from('spots')` query with `eq('user_id'`
    - Profile.tsx Spots Added card displays `{spotsCount}` not hardcoded `0`
    - Profile.tsx Favorites card displays `{favorites.length}` not `{spotsCount}`
    - ProfileContext.tsx does NOT contain `selectPresetAvatar` or `avatar_id`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Version shows v1.1.3, no gamification badge, spots count fetched from DB, preset avatars removed, Lucide User icon as default avatar, selectPresetAvatar removed from context, TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Replace AVATARS fallback with Lucide User icon in ReviewList, SessionCard, SpotDetail</name>
  <files>src/components/ReviewList.tsx, src/components/SessionCard.tsx, src/components/SpotDetail.tsx</files>
  <read_first>src/components/ReviewList.tsx, src/components/SessionCard.tsx, src/components/SpotDetail.tsx</read_first>
  <action>
**In `src/components/ReviewList.tsx`:**

1. Delete the `AVATARS` array (lines 6-12).
2. Add `User` to the lucide-react import: `import { Star, User } from 'lucide-react';`
3. Replace the avatar resolution logic (lines 40-43):
   - Current: `const avatarSrc = review.profiles?.avatar_url || AVATARS.find(...)?.src || AVATARS[0].src;`
   - New: determine if there's a custom avatar_url:
     ```tsx
     const hasAvatar = !!review.profiles?.avatar_url;
     ```
4. Replace the `<img src={avatarSrc} ...>` element (around line 57-60) with a conditional:
   ```tsx
   {hasAvatar ? (
     <img src={review.profiles!.avatar_url!} alt={displayName} className="w-8 h-8 rounded-full object-cover" />
   ) : (
     <div className="w-8 h-8 rounded-full bg-slate-100 flex items-center justify-center">
       <User size={16} className="text-slate-400" />
     </div>
   )}
   ```

**In `src/components/SessionCard.tsx`:**

1. Delete the `AVATARS` array (lines 9-15).
2. Add `User` to the lucide-react import (line 3): add `User` to `{ Calendar, Users, Bell, User }`.
   Note: `Users` (plural) is already imported. `User` (singular) is the icon we need.
3. Replace the avatar resolution logic (lines 37-40):
   - Current: `const avatarSrc = session.creator_profile?.avatar_url || AVATARS.find(...)?.src || AVATARS[0].src;`
   - New: `const hasCreatorAvatar = !!session.creator_profile?.avatar_url;`
4. Find the `<img>` element that uses `avatarSrc` for the session creator and replace with conditional rendering:
   ```tsx
   {hasCreatorAvatar ? (
     <img src={session.creator_profile!.avatar_url!} alt="" className="w-8 h-8 rounded-full object-cover" />
   ) : (
     <div className="w-8 h-8 rounded-full bg-slate-100 flex items-center justify-center">
       <User size={16} className="text-slate-400" />
     </div>
   )}
   ```

**In `src/components/SpotDetail.tsx`:**

1. Delete the `AVATARS` array (lines 20-26).
2. Add `User as UserIcon` to the lucide-react import to avoid conflict with `user` variable. Actually, Lucide imports are PascalCase and the `user` variable is lowercase, so `User` from lucide won't conflict. But to be safe and explicit, import as: add `UserCircle` to lucide-react import (line 1). Actually, better to use `User` since that's what the other files use. There's no conflict — `User` (component) vs `user` (variable) are different cases. Add `User as UserIcon` to the import to be extra safe.
3. Replace the uploader avatar display (around line 254):
   - Current: `<img src={uploaderProfile.avatar_url || AVATARS.find(a => a.id === (uploaderProfile.avatar_id || 1))?.src || AVATARS[0].src} ...>`
   - New:
     ```tsx
     {uploaderProfile.avatar_url ? (
       <img src={uploaderProfile.avatar_url} className="w-5 h-5 rounded-full object-cover" alt="" />
     ) : (
       <div className="w-5 h-5 rounded-full bg-slate-100 flex items-center justify-center">
         <UserIcon size={10} className="text-slate-400" />
       </div>
     )}
     ```
4. Remove `avatar_id` from the `uploaderProfile` state type (line 61): change from `{ display_name: string | null; avatar_url: string | null; avatar_id: number | null; }` to `{ display_name: string | null; avatar_url: string | null; }`.
5. Update the uploader profile fetch query (line 137): change `.select('display_name, avatar_url, avatar_id')` to `.select('display_name, avatar_url')`.
  </action>
  <verify>
    <automated>cd C:/Users/wandr/updock-app && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ReviewList.tsx does NOT contain `AVATARS` array
    - ReviewList.tsx contains `import { Star, User } from 'lucide-react'` (or similar with User)
    - ReviewList.tsx contains `<User size={16}` as fallback avatar
    - SessionCard.tsx does NOT contain `AVATARS` array
    - SessionCard.tsx contains `User` in its lucide-react import
    - SessionCard.tsx contains `<User size={16}` as fallback avatar
    - SpotDetail.tsx does NOT contain `AVATARS` array
    - SpotDetail.tsx does NOT contain `avatar_id` in uploaderProfile type
    - SpotDetail.tsx contains `UserIcon` (or `User`) as fallback avatar
    - `npm run build` succeeds with exit code 0
  </acceptance_criteria>
  <done>All AVATARS arrays removed from ReviewList, SessionCard, SpotDetail. Default avatar is a Lucide User icon everywhere. Build passes cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `grep -r "AVATARS" src/components/` returns no matches
3. `grep -r "avatar.*svg" src/components/` returns no matches
4. `grep -c "v1.1.3" src/components/Profile.tsx` returns 2
5. `grep "getLevel\|Rookie\|Expert" src/components/Profile.tsx` returns no matches
6. `grep "selectPresetAvatar" src/context/ProfileContext.tsx` returns no matches
</verification>

<success_criteria>
- Version displays v1.1.3 in both authenticated and anonymous profile views
- Gamification badge (Rookie/Pro/Expert) is completely removed
- Spots Added count is fetched from Supabase `spots` table filtered by user_id
- All 5 SVG preset avatars are no longer referenced anywhere in components
- Default avatar is a Lucide User icon in Profile, ReviewList, SessionCard, SpotDetail
- selectPresetAvatar removed from ProfileContext
- Full build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-bug-fixes-corriger-les-bugs-de-l-admin-dashboard-et-le-bouton-fermer-de-la-galerie-photos/08-01-SUMMARY.md`
</output>
