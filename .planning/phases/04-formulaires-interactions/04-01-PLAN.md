---
phase: 04-formulaires-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/Input.tsx
  - src/translations/fr.json
  - src/translations/en.json
autonomous: true
requirements: [UI-03, ROBUST-01]
must_haves:
  truths:
    - "Le composant Input rend une variante fond clair (surface=light) visuellement identique aux champs actuels d'AddSpotForm (bg-slate-50, border-slate-100, focus sky-500)"
    - "Input sans prop surface (défaut glass) reste byte-identique pour AuthModal (fond sombre, label uppercase)"
    - "Input peut rendre un <textarea> via multiline et limiter la saisie via maxLength"
    - "Toutes les nouvelles clés i18n de validation/confirmation/favori existent en français ET en anglais avec parité exacte"
    - "fr.json spot.edit_save vaut 'Enregistrer les modifications'"
    - "L'extension du composant Input suit le précédent établi en Phase 2/3 (étendre Modal/Header plutôt que créer un nouveau composant), conformément à D-01"
  artifacts:
    - path: "src/ui/Input.tsx"
      provides: "Primitive DS Input étendue (surface | multiline | maxLength | error)"
      contains: "surface"
    - path: "src/translations/fr.json"
      provides: "Clés form.error.*, form.confirm.no_photo.*, fav.error.revert, fav.add, fav.remove"
      contains: "form.error.name_required"
    - path: "src/translations/en.json"
      provides: "Mêmes clés que fr.json (parité)"
      contains: "form.error.name_required"
  key_links:
    - from: "src/ui/Input.tsx surface=light"
      to: "AddSpotForm.tsx L206/L218 (classes verbatim)"
      via: "extraction verbatim des classes bg-slate-50 border-2 border-slate-100"
      pattern: "bg-slate-50"
---

<objective>
Poser la couche de contrat de la phase 4 : étendre la primitive DS `Input` (surface claire + textarea + maxLength + message d'erreur inline) et ajouter toutes les clés i18n consommées par les formulaires migrés en aval, avec parité fr/en stricte.

Purpose: Ces deux artefacts sont les fondations partagées consommées par les plans 04-03 (AddSpotForm), 04-04 (SpotDetail) et 04-05 (AdminDashboard). Les définir en premier évite que chaque exécuteur réinvente le contrat (D-01).
Output: `src/ui/Input.tsx` étendu (défaut `glass` inchangé) + nouvelles clés dans `fr.json` et `en.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-formulaires-interactions/04-RESEARCH.md
@.planning/phases/04-formulaires-interactions/04-PATTERNS.md
@.planning/phases/04-formulaires-interactions/04-UI-SPEC.md

<interfaces>
<!-- Contrat que les plans 04-03/04/05 consommeront directement — aucune exploration requise. -->

État actuel de `src/ui/Input.tsx` (props L4-13) : `label, value, onChange (HTMLInputElement seul), icon?, type?, placeholder?, required?, minLength?`. Rendu unique fond sombre glass. Label glass = `text-xs font-bold text-white/70 uppercase tracking-wider ml-1` (L28). Champ glass = `w-full bg-black/20 border border-white/10 rounded-xl py-3 ... text-white ... focus:ring-2 focus:ring-primary` (L39).

Convention `surface` à répliquer (verbatim `src/ui/Modal.tsx` L28 et `src/ui/Header.tsx` L16-26) : prop `surface?: 'glass' | 'light'`, défaut `'glass'`, branchement ternaire des classes selon la surface. Les consommateurs existants ne passent aucun prop → restent byte-identiques.

Classes light à EXTRAIRE VERBATIM de `AddSpotForm.tsx` : champ input L206 / textarea L218 = `w-full p-4 bg-slate-50 border-2 border-slate-100 rounded-xl focus:border-sky-500 focus:outline-none transition-colors` ; le textarea ajoute `min-h-[100px]`. Label light L200 = `block text-sm text-slate-700 mb-2` avec graisse normalisée `font-normal` (UI-SPEC Typography § 2 graisses). Message d'erreur inline sous le champ (surface light) = `bg-red-50 border border-red-200 text-red-600 text-sm` (UI-SPEC L105).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Étendre la primitive Input (surface + multiline + maxLength + error)</name>
  <files>src/ui/Input.tsx</files>
  <read_first>
    - src/ui/Input.tsx (état actuel à étendre)
    - src/ui/Header.tsx (convention `surface` à répliquer verbatim — ternaires de couleur L16-26)
    - src/ui/Modal.tsx (défaut `surface='glass'` L28, même stratégie de non-régression)
    - src/components/AddSpotForm.tsx (L200/L206/L218 — source verbatim des classes light label/input/textarea)
    - .planning/phases/04-formulaires-interactions/04-UI-SPEC.md (Component Contract L149-150, Typography § graisses, Color L105)
  </read_first>
  <action>
    Ajouter à `InputProps` : `surface?: 'glass' | 'light'` (défaut `'glass'`), `multiline?: boolean`, `maxLength?: number`, `error?: string`. Élargir le type de `onChange` à `React.ChangeEventHandler<HTMLInputElement | HTMLTextAreaElement>`.
    Brancher les classes par `surface` avec un ternaire (modèle `Header.tsx` L16-26) — NE PAS changer le défaut `glass` (Pitfall 2 RESEARCH : casserait AuthModal). Sur `surface='glass'`, conserver strictement les classes actuelles (label `text-xs font-bold text-white/70 uppercase tracking-wider ml-1`, champ `bg-black/20 border border-white/10 ... focus:ring-2 focus:ring-primary`).
    Sur `surface='light'` : label = `block text-sm font-normal text-slate-700 mb-2` (NE PAS rendre le label uppercase glass — régression directe), champ = `w-full p-4 bg-slate-50 border-2 border-slate-100 rounded-xl focus:border-sky-500 focus:outline-none transition-colors` extrait verbatim d'AddSpotForm L206.
    Sur `multiline={true}` : rendre un `<textarea>` au lieu de `<input>`, en ajoutant `min-h-[100px]` aux classes light (verbatim AddSpotForm L218). Passer `maxLength` à l'élément rendu.
    Sur `error` défini : rendre un `<div>` sous le champ avec la classe `bg-red-50 border border-red-200 text-red-600 text-sm` (surface light) — UI-SPEC L105. Ne pas inventer de classe sombre sur fond clair.
    Ne pas invente de valeur de design : toute classe light provient verbatim d'AddSpotForm ou de l'UI-SPEC.
  </action>
  <verify>
    <automated>npm run lint && grep -c "surface" src/ui/Input.tsx && grep -q "text-white/70 uppercase tracking-wider" src/ui/Input.tsx && grep -q "bg-slate-50 border-2 border-slate-100" src/ui/Input.tsx && grep -q "min-h-\[100px\]" src/ui/Input.tsx && echo OK</automated>
  </verify>
  <done>Input accepte surface/multiline/maxLength/error ; le défaut glass conserve les classes originales verbatim ; la variante light reproduit les classes AddSpotForm ; lint vert.</done>
  <acceptance_criteria>
    - `grep -q "surface = 'glass'" src/ui/Input.tsx` (défaut glass présent)
    - `grep -q "HTMLTextAreaElement" src/ui/Input.tsx` (onChange élargi)
    - Le rendu `surface='glass'` conserve les classes L28/L39 originales verbatim (aucune classe glass supprimée/modifiée)
    - Le label light ne contient PAS `uppercase` (source assertion : `grep "text-slate-700 mb-2" src/ui/Input.tsx` présent, `uppercase` absent du bloc light)
    - `npm run lint` sort sans erreur
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 : Ajouter les clés i18n (parité fr/en) + correctif spot.edit_save</name>
  <files>src/translations/fr.json, src/translations/en.json</files>
  <read_first>
    - src/translations/fr.json (structure JSON, clés add.*/spot.edit_*/fav.* existantes)
    - src/translations/en.json (parité)
    - .planning/phases/04-formulaires-interactions/04-UI-SPEC.md (Copywriting Contract L115-136 — copy exact fr/en)
  </read_first>
  <action>
    Ajouter dans `fr.json` ET `en.json` les 12 clés NOUVELLES avec la copy exacte de l'UI-SPEC Copywriting Contract (L121-132) : `form.error.name_required`, `form.error.name_too_long`, `form.error.desc_too_long`, `form.error.type_required`, `form.error.submit_failed`, `fav.error.revert`, `form.confirm.no_photo.title`, `form.confirm.no_photo.body`, `form.confirm.no_photo.confirm`, `form.confirm.no_photo.cancel`, `fav.remove`, `fav.add`.
    Valeurs fr (verbatim UI-SPEC) : name_required = « Le nom du spot est obligatoire. » ; name_too_long = « Le nom est trop long (100 caractères max). » ; desc_too_long = « La description est trop longue (2000 caractères max). » ; type_required = « Sélectionne au moins un type de spot. » ; submit_failed = « Échec de l'envoi. Tes informations sont conservées, réessaie. » ; fav.error.revert = « Échec, réessaie. » ; no_photo.title = « Publier sans photo ? » ; no_photo.body = « Une photo aide les autres riders à repérer le spot en un coup d'œil. Tu pourras en ajouter une plus tard en modifiant le spot. » ; no_photo.confirm = « Publier quand même » ; no_photo.cancel = « Ajouter une photo » ; fav.remove = « Retirer des favoris » ; fav.add = « Ajouter aux favoris ».
    Valeurs en (verbatim UI-SPEC) : "Spot name is required." / "Name is too long (100 characters max)." / "Description is too long (2000 characters max)." / "Select at least one spot type." / "Submission failed. Your details are saved, try again." / "Failed, try again." / "Publish without a photo?" / "A photo helps other riders spot it at a glance. You can add one later by editing the spot." / "Publish anyway" / "Add a photo" / "Remove from favorites" / "Add to favorites".
    Correctif copy existant : dans `fr.json` L144, remplacer la valeur de `spot.edit_save` par « Enregistrer les modifications » (en.json « Save Changes » déjà conforme, sert de référence de parité).
    Respecter le format JSON plat existant (clés à points, pas d'objets imbriqués) et l'échappement des apostrophes.
  </action>
  <verify>
    <automated>node -e "const fr=require('./src/translations/fr.json'),en=require('./src/translations/en.json');const k=['form.error.name_required','form.error.name_too_long','form.error.desc_too_long','form.error.type_required','form.error.submit_failed','fav.error.revert','form.confirm.no_photo.title','form.confirm.no_photo.body','form.confirm.no_photo.confirm','form.confirm.no_photo.cancel','fav.remove','fav.add'];const miss=k.filter(x=>!(x in fr)||!(x in en));if(miss.length){console.error('MANQUANT',miss);process.exit(1)};if(fr['spot.edit_save']!=='Enregistrer les modifications'){console.error('edit_save KO');process.exit(1)};console.log('OK parite')"</automated>
  </verify>
  <done>Les 12 clés existent dans fr.json ET en.json ; spot.edit_save fr = « Enregistrer les modifications » ; les deux fichiers restent du JSON valide.</done>
  <acceptance_criteria>
    - Le script node de vérification sort `OK parite` (les 12 clés présentes des deux côtés, aucune manquante)
    - `fr['spot.edit_save'] === 'Enregistrer les modifications'`
    - `JSON.parse` réussit sur les deux fichiers (pas de virgule pendante / apostrophe non échappée)
    - Aucune clé ajoutée dans un seul des deux fichiers (parité stricte)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Utilisateur → champ de saisie (React) | La saisie texte entre dans l'app ; rendu par React (échappement par défaut) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-I1 | Information Disclosure / Tampering | Input (rendu de valeur) | mitigate | React échappe le rendu par défaut ; ne PAS introduire `dangerouslySetInnerHTML` dans Input. Le prop `error` rend du texte simple, jamais du HTML. |
| T-04-V1 | Tampering (V5 Input Validation) | prop `maxLength` de Input | accept | `maxLength` est un garde-fou UX côté client, PAS une frontière de sécurité (rappel RESEARCH Security Domain). La garde autoritaire reste les contraintes RLS/Postgres Supabase — non modifiées ici. Ne pas présenter maxLength comme contrôle de sécurité suffisant. |

Aucune installation de package dans ce plan (RESEARCH § Package Legitimacy Audit : non applicable) → pas de checkpoint T-04-SC requis.
</threat_model>

<verification>
- `npm run lint` vert.
- `npm run build` (typecheck TS) vert — la nouvelle signature `onChange` élargie ne casse aucun consommateur existant (AuthModal ne passe pas de textarea).
- Vérification manuelle rapide (au device en 04-06) : AuthModal rend byte-identique (fond sombre, label uppercase).
</verification>

<success_criteria>
- Input expose surface/multiline/maxLength/error, défaut glass inchangé (AuthModal non régressé).
- Les 12 clés i18n existent avec parité fr/en ; spot.edit_save fr corrigé.
- lint + build verts.
</success_criteria>

<output>
Créer `.planning/phases/04-formulaires-interactions/04-01-SUMMARY.md` à la fin.
</output>
