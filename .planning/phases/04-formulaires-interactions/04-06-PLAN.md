---
phase: 04-formulaires-interactions
plan: 06
type: execute
wave: 3
depends_on: ["04-03", "04-04", "04-05"]
files_modified:
  - .planning/phases/04-formulaires-interactions/04-QA-CHECKLIST.md
autonomous: false
requirements: [UI-03, ROBUST-01, ROBUST-02]
must_haves:
  truths:
    - "Un document de recette manuelle dédié couvre ajout, édition user, édition admin, favori add/remove, favori offline, validation champ vide/trop long, soumission sans photo"
    - "Le build TS (npm run build) et le lint passent sur l'ensemble de la phase"
    - "La recette manuelle mobile passe à 100% — aucune régression fonctionnelle (critère de succès #4)"
    - "AuthModal et FiltersModal restent visuellement byte-identiques (non-régression des consommateurs Input non touchés)"
  artifacts:
    - path: ".planning/phases/04-formulaires-interactions/04-QA-CHECKLIST.md"
      provides: "Checklist de recette manuelle de la phase 4"
      contains: "Recette"
  key_links:
    - from: "recette manuelle"
      to: "critère de succès #4 ROADMAP"
      via: "checklist device 100%"
      pattern: "100"
---

<objective>
Valider la non-régression et le durcissement de la phase 4 : produire la checklist de recette manuelle dédiée (sur le modèle de 03-QA-CHECKLIST.md) et faire passer la recette device à 100% + build/lint verts (critère de succès #4, QA-01).

Purpose: Phase gate. Aucune infra de test automatisé (contrainte projet) → la recette manuelle mobile est le filet de sécurité. Ce plan consolide le filet automatique (tsc/eslint) et la recette humaine.
Output: `04-QA-CHECKLIST.md` + validation device 100%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-formulaires-interactions/04-RESEARCH.md
@.planning/phases/04-formulaires-interactions/04-UI-SPEC.md
@.planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Rédiger la checklist de recette manuelle 04-QA-CHECKLIST.md + gate build/lint</name>
  <files>.planning/phases/04-formulaires-interactions/04-QA-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md (modèle de format et de rigueur)
    - .planning/phases/04-formulaires-interactions/04-RESEARCH.md (Validation Architecture, Wave 0 Gaps — liste des scénarios à couvrir)
    - .planning/phases/04-formulaires-interactions/04-UI-SPEC.md (Interaction & States Contract — états attendus)
  </read_first>
  <action>
    Exécuter d'abord le filet automatique et consigner le résultat en tête de document : `npm run lint` puis `npm run build` (tsc -b && vite build) — les deux DOIVENT être verts avant la recette humaine.
    Créer `04-QA-CHECKLIST.md` avec frontmatter (phase, plan: 06, type: qa-checklist, status: draft, created) sur le modèle de 03-QA-CHECKLIST.md. Documenter, chaque scénario avec étapes + résultat attendu + case à cocher :
    1. Ajout de spot : formulaire ouvert → champs rendus par Input (fond clair) ; submit avec état loading ; toast « Envoi du spot en cours… » (comportement addSpot existant).
    2. Validation ajout : nom vide/espaces → message inline « Le nom du spot est obligatoire. » ; nom >100 → message trop long ; description >2000 → message trop long ; désélectionner tous les types → « Sélectionne au moins un type de spot. » ; dans chaque cas addSpot NON déclenché et données conservées.
    3. Confirmation pas-de-photo (D-04) : soumettre sans photo → dialogue doux Modal light+center (2 actions « Publier quand même » / « Ajouter une photo ») ; « Publier quand même » publie ; « Ajouter une photo » referme le dialogue sans publier.
    4. Édition user (SpotDetail) : champs via Input ; Save via Button loading ; validation identique ; échec réseau → erreur inline « Échec de l'envoi… » + overlay ouvert + données conservées.
    5. Édition admin (AdminDashboard, D-09) : champs via Input ; libellés en FR et EN corrects ; validation ; échec réseau → toast ; succès → overlay fermé.
    6. Favori add/remove : bouton coeur (SpotDetail + liste favoris App) = Button iconOnly, cible tactile ≥44px, aria-label ; non connecté (SpotDetail) → ouvre l'auth + badge cadenas.
    7. Favori offline (D-08) : couper le réseau, toggler un favori → état visuel se met à jour puis revert + toast discret « Échec, réessaie. ».
    8. Non-régression : AuthModal (fond sombre, label uppercase) et FiltersModal inchangés ; aucun `alert()` natif ne surgit sur les chemins migrés.
    Vérifier la parité i18n : chaque message ci-dessus s'affiche correctement en français ET en anglais.
  </action>
  <verify>
    <automated>npm run lint && npm run build && test -f .planning/phases/04-formulaires-interactions/04-QA-CHECKLIST.md && grep -qi "recette" .planning/phases/04-formulaires-interactions/04-QA-CHECKLIST.md && echo OK</automated>
  </verify>
  <done>04-QA-CHECKLIST.md existe avec les 8 blocs de scénarios ; lint + build verts et consignés.</done>
  <acceptance_criteria>
    - `npm run lint` sort sans erreur
    - `npm run build` (tsc + vite) sort sans erreur
    - Le fichier `04-QA-CHECKLIST.md` existe et couvre les 8 scénarios (ajout, validation, confirm pas-de-photo, édition user, édition admin, favori add/remove, favori offline, non-régression)
    - Aucun `alert(` restant sur les chemins migrés (`grep -rc "alert(" src/context/SpotsContext.tsx` == 0)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migration DS des formulaires (ajout/édition user/édition admin) + boutons favori + validation client + gestion d'erreur inline/toast. Le build TS et le lint sont verts (Task 1). La checklist 04-QA-CHECKLIST.md liste tous les scénarios à valider sur device.</what-built>
  <how-to-verify>
    Sur un device iOS physique (cible QA-01), dérouler `.planning/phases/04-formulaires-interactions/04-QA-CHECKLIST.md` scénario par scénario :
    1. Ajouter un spot : vérifier le rendu clair des champs (Input), le bouton submit avec spinner, le toast d'envoi.
    2. Tester chaque validation invalide (nom vide, nom >100, description >2000, 0 type) → message inline clair, données conservées.
    3. Soumettre sans photo → dialogue doux (Modal), tester les 2 actions.
    4. Éditer un spot (fiche user) : champs Input, Save loading, couper le réseau et sauvegarder → erreur inline + données conservées.
    5. Éditer un spot en admin : libellés corrects (basculer FR/EN), validation, échec réseau → toast.
    6. Favori : ajouter/retirer depuis la fiche ET depuis la liste favoris ; vérifier la cible tactile confortable ; non connecté → auth + cadenas.
    7. Couper le réseau, toggler un favori → revert + toast discret.
    8. Vérifier qu'AuthModal (auth) et FiltersModal (filtres) sont visuellement inchangés et qu'aucune alerte native ne surgit.
    Cocher chaque item. Toute divergence de rendu ou régression fonctionnelle = retour en gap closure (`/gsd:plan-phase 04 --gaps`), pas un arbitrage.
  </how-to-verify>
  <resume-signal>Taper « recette 100% » si tous les scénarios passent, ou décrire les régressions/écarts constatés.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Utilisateur mobile → app compilée | La recette valide le comportement réel sur device (cible QA-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-V5 | Tampering (V5 Input Validation) | validation bout-en-bout | mitigate | La recette confirme que la validation client s'affiche correctement ; rappel : ce n'est pas une frontière de sécurité, RLS/Postgres reste la garde autoritaire (non modifiée par la phase). |
| T-04-Q1 | Repudiation (feedback) | feedback erreur/toast | mitigate | La recette offline vérifie que tout échec produit un feedback visible (inline/toast) — aucun échec silencieux. |

Aucune installation de package sur toute la phase 4 (RESEARCH § Package Legitimacy Audit : non applicable) → aucun checkpoint T-04-SC.
</threat_model>

<verification>
- `npm run lint` + `npm run build` verts (filet automatique).
- Recette manuelle device à 100% (critère de succès #4).
- Non-régression AuthModal/FiltersModal confirmée visuellement.
</verification>

<success_criteria>
- 04-QA-CHECKLIST.md produite et déroulée à 100% sur device.
- lint + build verts ; aucune régression fonctionnelle ; aucun alert() natif sur les chemins migrés.
</success_criteria>

<output>
Créer `.planning/phases/04-formulaires-interactions/04-06-SUMMARY.md` à la fin (statut de la recette, écarts éventuels).
</output>
