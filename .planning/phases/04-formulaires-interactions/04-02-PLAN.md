---
phase: 04-formulaires-interactions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/context/SpotsContext.tsx
  - src/context/FavoritesContext.tsx
autonomous: true
requirements: [ROBUST-02]
must_haves:
  truths:
    - "updateSpot propage (throw) son erreur pour qu'un formulaire d'édition puisse l'afficher inline, au lieu de l'avaler via alert()"
    - "approveSpot et deleteSpot affichent un Toast Capacitor natif en cas d'échec (plus aucun alert() natif sur ces chemins)"
    - "Le préfixe [DEBUG] est retiré du message d'échec de deleteSpot"
    - "toggleFavorite rejette (throw) après le revert optimiste en cas d'échec, pour que le composant appelant déclenche un toast traduit"
    - "Le mécanisme de revert optimiste des favoris reste inchangé (comportement existant conservé)"
  artifacts:
    - path: "src/context/SpotsContext.tsx"
      provides: "updateSpot qui rethrow ; approve/deleteSpot avec Toast au lieu d'alert"
      contains: "throw"
    - path: "src/context/FavoritesContext.tsx"
      provides: "toggleFavorite: (spotId) => Promise<void> qui rejette après revert"
      contains: "Promise<void>"
  key_links:
    - from: "src/context/SpotsContext.tsx approveSpot/deleteSpot catch"
      to: "@capacitor/toast Toast.show"
      via: "pattern existant addSpot L205-215"
      pattern: "Toast.show"
    - from: "src/context/FavoritesContext.tsx toggleFavorite catch"
      to: "composant appelant (App.tsx / SpotDetail.tsx)"
      via: "rethrow après setFavorites revert"
      pattern: "throw"
---

<objective>
Fiabiliser la couche d'orchestration des données (ROBUST-02) : faire remonter les échecs API vers l'UI au lieu de les avaler par des `alert()` natifs bloquants, et permettre au revert favori de déclencher un feedback côté composant.

Purpose: Fondation consommée par 04-04 (SpotDetail : édition inline + toast favori) et 04-05 (AdminDashboard : édition + App.tsx favori). `updateSpot` doit `throw` pour que les formulaires affichent l'erreur inline (D-07 garde les données) ; `toggleFavorite` doit rejeter pour que le composant affiche un toast traduit (D-08, Résolution Q2 UI-SPEC).
Output: `SpotsContext.tsx` (updateSpot rethrow + Toast sur approve/delete) et `FavoritesContext.tsx` (toggleFavorite Promise<void> rejetante).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-formulaires-interactions/04-RESEARCH.md
@.planning/phases/04-formulaires-interactions/04-PATTERNS.md

<interfaces>
<!-- Contrats consommés par 04-04 (SpotDetail) et 04-05 (AdminDashboard + App.tsx). -->

`SpotsContext.updateSpot` (L266-287) AVALE aujourd'hui l'erreur (catch → `alert('Failed to update spot.')`, pas de rethrow). Les `handleSaveEdit` de SpotDetail/AdminDashboard ne voient jamais l'échec. NOUVEAU contrat : `updateSpot` doit `throw` dans son catch après log, pour que le composant fasse `try { await updateSpot(...) } catch { setError(...) }`. La signature publique reste `(updatedSpot: Spot) => Promise<void>`.

`SpotsContext` importe déjà `Toast` de `@capacitor/toast` (L5, déjà utilisé dans addSpot L128-215). Pattern verbatim d'erreur toast : `Toast.show({ text: 'Erreur : ...', duration: 'long' })`.

`FavoritesContext.toggleFavorite` est aujourd'hui typé `(spotId: string) => void` (interface L9) mais l'implémentation est `async` avec revert silencieux (catch L95-103, pas de rethrow). NOUVEAU contrat : type `(spotId: string) => Promise<void>` ; après le `setFavorites(...)` de revert existant, `throw err` pour que le composant appelant réagisse. NE PAS changer le mécanisme de revert lui-même.

Périmètre net (Pitfall 5 RESEARCH) : NE PAS partir en chasse à tous les `alert()` du repo. Seuls updateSpot (édition = formulaire), approveSpot et deleteSpot (actions admin, D-09) sont dans le périmètre. Le `confirm('Delete this spot?')` L235 RESTE (comportement conservé). NE PAS toucher `type: JSON.stringify(...)` (L273, hors scope).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : SpotsContext — rethrow updateSpot + Toast sur approve/delete + retrait [DEBUG]</name>
  <files>src/context/SpotsContext.tsx</files>
  <read_first>
    - src/context/SpotsContext.tsx (approveSpot L223-232, deleteSpot L234-264, updateSpot L266-287, pattern Toast addSpot L205-215, import Toast L5)
    - .planning/phases/04-formulaires-interactions/04-RESEARCH.md (Pitfall 5, Code Examples toast)
    - .planning/phases/04-formulaires-interactions/04-UI-SPEC.md (Copywriting Contract L138-140 : suppression admin conserve confirm(), retirer [DEBUG])
  </read_first>
  <action>
    Dans `approveSpot` (catch L228-231) : remplacer `alert('Failed to approve.')` par `Toast.show({ text: 'Échec de l'approbation.', duration: 'long' })` (pattern verbatim addSpot). Conserver le log console.
    Dans `deleteSpot` (catch L259-263) : remplacer `alert(\`[DEBUG] Failed to delete spot: ${errorMessage}\`)` par `Toast.show({ text: \`Échec de la suppression : ${errorMessage}\`, duration: 'long' })` — RETIRER le préfixe `[DEBUG]` (UI-SPEC L140, string de debug en prod). Conserver le `confirm('Delete this spot?')` L235 inchangé.
    Dans `updateSpot` (catch L283-286) : remplacer `alert('Failed to update spot.')` par un `throw error` (après le `console.error` existant) afin que l'appelant (SpotDetail/AdminDashboard handleSaveEdit) puisse capter l'échec et l'afficher inline. NE PAS ajouter de Toast ici — le feedback d'édition est inline côté composant (D-07).
    NE PAS toucher : addSpot, le `confirm()` L235, `type: JSON.stringify(...)` L273, lat/lng.
  </action>
  <verify>
    <automated>npm run lint && ! grep -q "alert(" src/context/SpotsContext.tsx && ! grep -q "\[DEBUG\]" src/context/SpotsContext.tsx && grep -q "throw error" src/context/SpotsContext.tsx && grep -c "Toast.show" src/context/SpotsContext.tsx</automated>
  </verify>
  <done>Aucun `alert(` ni `[DEBUG]` ne subsiste dans SpotsContext ; updateSpot rethrow ; approve/delete utilisent Toast ; confirm() de suppression conservé ; lint vert.</done>
  <acceptance_criteria>
    - `grep -c "alert(" src/context/SpotsContext.tsx` retourne 0
    - `grep -c "\[DEBUG\]" src/context/SpotsContext.tsx` retourne 0
    - `updateSpot` contient `throw` dans son bloc catch (source assertion)
    - `grep -c "confirm('Delete this spot?')" src/context/SpotsContext.tsx` retourne 1 (confirm conservé)
    - `type: JSON.stringify` toujours présent (chemin fragile non touché)
    - `npm run lint` vert
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 : FavoritesContext — toggleFavorite rejette après revert (Promise<void>)</name>
  <files>src/context/FavoritesContext.tsx</files>
  <read_first>
    - src/context/FavoritesContext.tsx (interface L7-11, toggleFavorite L61-104, revert catch L95-103)
    - .planning/phases/04-formulaires-interactions/04-PATTERNS.md (section FavoritesContext L214-227, décision i18n)
    - .planning/phases/04-formulaires-interactions/04-UI-SPEC.md (Résolution Q2 L181 : toast déclenché depuis le composant, pas le context)
  </read_first>
  <action>
    Changer la signature dans `FavoritesContextType` (L9) de `toggleFavorite: (spotId: string) => void` à `toggleFavorite: (spotId: string) => Promise<void>`.
    Dans le bloc catch de `toggleFavorite` (L95-103) : conserver STRICTEMENT le `setFavorites(...)` de revert existant (mécanisme inchangé), puis ajouter `throw err` APRÈS le revert, pour que le composant appelant (App.tsx, SpotDetail.tsx) puisse déclencher `Toast.show({ text: t('fav.error.revert') })` avec la bonne langue.
    NE PAS coder de chaîne en dur en français dans le context (Pitfall 4 : le context n'a pas accès à `t()`). NE PAS appeler Toast.show ici — c'est le rôle du composant (04-04 / 04-05).
    Conserver le `if (!user) return;` L62 et l'update optimiste L67-71 inchangés.
  </action>
  <verify>
    <automated>npm run lint && grep -q "Promise<void>" src/context/FavoritesContext.tsx && grep -q "throw err" src/context/FavoritesContext.tsx && ! grep -qi "Toast" src/context/FavoritesContext.tsx && echo OK</automated>
  </verify>
  <done>toggleFavorite typé Promise<void>, rejette après le revert existant ; aucun Toast ni chaîne FR codée en dur dans le context ; revert optimiste inchangé ; lint vert.</done>
  <acceptance_criteria>
    - `grep -q "toggleFavorite: (spotId: string) => Promise<void>" src/context/FavoritesContext.tsx` (contrat mis à jour)
    - Le catch contient `setFavorites(` (revert conservé) SUIVI de `throw err` (source assertion : le revert précède le throw)
    - `grep -ci "Toast" src/context/FavoritesContext.tsx` retourne 0 (aucun toast dans le context)
    - `npm run lint` vert et `npm run build` typecheck vert (le changement de type Promise<void> ne casse pas les appelants existants qui n'attendent pas le retour)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (context) → API Supabase | Mutations spot/favori franchissent le pont réseau ; l'échec doit être géré sans crash ni état bloqué (ROBUST-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-D1 | Denial of Service (état bloqué) | updateSpot / toggleFavorite catch | mitigate | L'erreur est propagée (throw) et gérée en aval (inline/toast) — jamais d'`alert()` bloquant ni de promesse silencieusement avalée. Le revert favori garantit un état cohérent après échec. |
| T-04-I2 | Information Disclosure | message d'erreur deleteSpot | mitigate | Retrait du préfixe `[DEBUG]` (fuite d'info de debug en prod). Le message toast reste générique côté utilisateur. |
| T-04-R1 | Repudiation (feedback absent) | revert favori silencieux | mitigate | Le rethrow permet au composant d'informer l'utilisateur (toast D-08) que l'action a échoué — plus de revert muet. |

Aucune installation de package (Toast déjà lié) → pas de checkpoint T-04-SC.
</threat_model>

<verification>
- `npm run lint` + `npm run build` verts.
- Aucun `alert(` restant dans SpotsContext ; aucun Toast/chaîne FR dans FavoritesContext.
- Contrat `Promise<void>` compatible avec les appelants actuels (typecheck).
</verification>

<success_criteria>
- updateSpot rethrow ; approve/delete via Toast sans [DEBUG] ; confirm() conservé.
- toggleFavorite Promise<void> rejetante après revert inchangé.
- lint + build verts.
</success_criteria>

<output>
Créer `.planning/phases/04-formulaires-interactions/04-02-SUMMARY.md` à la fin.
</output>
