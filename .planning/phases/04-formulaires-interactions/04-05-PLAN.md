---
phase: 04-formulaires-interactions
plan: 05
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/components/AdminDashboard.tsx
  - src/App.tsx
autonomous: true
requirements: [UI-03, ROBUST-01, ROBUST-02]
must_haves:
  truths:
    - "Les champs nom/description du formulaire d'édition admin passent par Input surface=light (description multiline, maxLength), le bouton Save par Button loading (D-09)"
    - "Les libellés en dur du formulaire admin (Name/Type/Description/Difficulty/Edit Spot/Save) passent par t() (parité i18n)"
    - "Une édition admin invalide affiche un message inline et n'appelle pas updateSpot ; un échec API affiche un feedback (toast) sans crash"
    - "Le bouton favori de la liste favoris d'App.tsx est le composant DS Button (iconOnly + aria-label), cible tactile 44px"
    - "En cas d'échec du toggle favori depuis la liste, l'état visuel se revert ET un toast discret traduit s'affiche (D-08)"
  artifacts:
    - path: "src/components/AdminDashboard.tsx"
      provides: "Formulaire d'édition admin migré DS + i18n + validation + feedback"
      contains: "surface=\"light\""
    - path: "src/App.tsx"
      provides: "Bouton favori liste migré Button iconOnly + toast revert"
      contains: "iconOnly"
  key_links:
    - from: "AdminDashboard handleSaveEdit"
      to: "updateSpot (rethrow) → toast/inline"
      via: "try/catch"
      pattern: "Toast.show"
    - from: "App.tsx bouton favori"
      to: "toggleFavorite (Promise rejetante) → Toast.show(t('fav.error.revert'))"
      via: "catch côté composant (D-08)"
      pattern: "fav.error.revert"
---

<objective>
Migrer le formulaire d'édition inline de l'AdminDashboard (D-09) et le bouton favori de la liste favoris d'App.tsx (D-10) vers le design system, avec validation, i18n et feedback d'échec cohérent (UI-03, ROBUST-01, ROBUST-02).

Purpose: Ferme le périmètre de migration des formulaires (édition admin = même logique que SpotDetail, D-09) et des boutons favori (le second des deux boutons coeur, D-10). Consomme les fondations 04-01 (Input) et 04-02 (updateSpot rethrow, toggleFavorite rejetante).
Output: `src/components/AdminDashboard.tsx` + `src/App.tsx` migrés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-formulaires-interactions/04-RESEARCH.md
@.planning/phases/04-formulaires-interactions/04-PATTERNS.md
@.planning/phases/04-formulaires-interactions/04-UI-SPEC.md

<interfaces>
<!-- Fondations livrées par 04-01 et 04-02. -->
`Input` : `surface="light"`, `multiline`, `maxLength`, `label`, `value`, `onChange`. (04-01)
`Button` : `variant`, `size`, `loading`, `iconOnly` (impose `aria-label`), `className`. `variant="ghost"` = fond sombre → override className pour fond clair.
`updateSpot(spot): Promise<void>` (04-02) : rethrow en cas d'échec → handleSaveEdit doit try/catch et afficher un feedback (toast admin, Pattern 4 : action transitoire dans une liste). `Toast` importé de `@capacitor/toast`.
`toggleFavorite(spotId): Promise<void>` (04-02) : rejette après revert → App.tsx doit `.catch()` + `Toast.show({ text: t('fav.error.revert'), duration: 'short' })`.

Clés i18n : réutiliser les clés EXISTANTES `spot.edit_name` (« Nom »), `spot.edit_type` (« Type »), `spot.edit_description` (« Description »), `spot.edit_difficulty` (« Difficulté »), `spot.edit_title` (« Modifier le spot »), `spot.edit_save` (« Enregistrer les modifications », corrigé par 04-01). Favori : `fav.remove`, `fav.error.revert`, `form.error.*` (04-01). Aucune NOUVELLE clé i18n à créer dans ce plan (donc ne PAS modifier les fichiers de traduction — évite tout conflit avec 04-01).

État actuel AdminDashboard : overlay édition L198-282 (input nom L217-222, textarea desc L249-253, pills type L227-244, pills difficulté L258-266, bouton Save custom L272-278) ; libellés « Edit Spot » L208, « Name » L216, « Type » L225, « Description » L248, « Difficulty » L256, « Save Changes » L277 EN DUR ; handleSaveEdit L29-33. `useLanguage().t` déjà importé (L16).
État actuel App.tsx : bouton favori liste L174-182 (`<button className="p-2 text-rose-500 hover:bg-rose-50 rounded-full">` + Heart fill-rose-500).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : AdminDashboard — édition inline → Input/Button + i18n + validation + feedback</name>
  <files>src/components/AdminDashboard.tsx</files>
  <behavior>
    - editingSpot.name vide/espaces → setEditError(t('form.error.name_required')), updateSpot NON appelé
    - editingSpot.name.trim().length > 100 → setEditError(t('form.error.name_too_long')), updateSpot NON appelé
    - editingSpot.description.length > 2000 → setEditError(t('form.error.desc_too_long')), updateSpot NON appelé
    - editingSpot.type.length === 0 → setEditError(t('form.error.type_required')), updateSpot NON appelé
    - updateSpot rejette → Toast.show(t('form.error.submit_failed')), overlay reste ouvert (données conservées)
    - saisie valide + updateSpot OK → overlay se ferme (setEditingSpot(null))
  </behavior>
  <read_first>
    - src/components/AdminDashboard.tsx (overlay édition L198-282, handleSaveEdit L29-33, imports L1-6)
    - src/components/SpotDetail.tsx (analog : même logique d'édition, une fois 04-04 fait)
    - src/ui/Input.tsx (contrat 04-01)
    - .planning/phases/04-formulaires-interactions/04-PATTERNS.md (section AdminDashboard L173-187)
  </read_first>
  <action>
    Importer `Input`, `Button` (`../ui/Input`, `../ui/Button`) et `Toast` (`@capacitor/toast`). Ajouter `const [editError, setEditError] = useState<string | null>(null)`.
    Remplacer l'`<input>` nom (L217-222) par `<Input surface="light" label={t('spot.edit_name')} value={editingSpot.name} onChange={e => setEditingSpot({ ...editingSpot, name: e.target.value })} maxLength={100} />` et le `<textarea>` desc (L249-253) par `<Input surface="light" multiline label={t('spot.edit_description')} value={editingSpot.description} onChange={e => setEditingSpot({ ...editingSpot, description: e.target.value })} maxLength={2000} />`. Retirer les `<label>` manuels devenus redondants.
    Remplacer les libellés en dur restants par `t()` : titre « Edit Spot » L208 → `t('spot.edit_title')` ; label « Type » L225 → `t('spot.edit_type')` ; label « Difficulty » L256 → `t('spot.edit_difficulty')`.
    Remplacer le bouton Save custom (L272-278) par `<Button variant="primary" size="lg" loading={?} onClick={handleSaveEdit} className="w-full">{t('spot.edit_save')}</Button>` — ajouter un état `isSaving` local si absent pour piloter `loading`.
    Dans `handleSaveEdit` (L29-33) : `setEditError(null)` d'abord ; ajouter la validation (nom trim non vide ≤100, desc ≤2000, type ≥1) avec `setEditError(t('form.error.*))` + return AVANT updateSpot ; envelopper `await updateSpot(editingSpot)` dans try/catch — sur succès `setEditingSpot(null)` ; sur échec (updateSpot rethrow, 04-02) `Toast.show({ text: t('form.error.submit_failed'), duration: 'long' })` et laisser l'overlay ouvert (données conservées, D-07).
    Ajouter l'affichage inline `{editError && <div className="p-3 rounded-xl bg-red-50 border border-red-200 text-red-600 text-sm">{editError}</div>}` près du bouton Save.
    CONSERVER VERBATIM : pills type L227-244, pills difficulté L258-266. NE PAS modifier les fichiers de traduction (réutilisation de clés existantes uniquement).
  </action>
  <verify>
    <automated>npm run lint && grep -c "surface=\"light\"" src/components/AdminDashboard.tsx && grep -q "spot.edit_title" src/components/AdminDashboard.tsx && ! grep -q ">Edit Spot<\|>Save Changes<\|>Difficulty<\|>Description<" src/components/AdminDashboard.tsx && grep -q "form.error.submit_failed" src/components/AdminDashboard.tsx && echo OK</automated>
  </verify>
  <done>Champs édition admin via Input surface=light ; libellés via t() ; Save via Button ; validation inline + feedback toast sur échec updateSpot ; pills verbatim ; lint vert.</done>
  <acceptance_criteria>
    - `grep -c "surface=\"light\"" src/components/AdminDashboard.tsx` >= 2 (nom + desc)
    - Aucun libellé en dur « Edit Spot » / « Save Changes » / « Difficulty » / « Description » (grep retourne 0)
    - handleSaveEdit contient validation + return AVANT updateSpot et un catch appelant `Toast.show` (submit_failed)
    - `<Button variant="primary"` remplace le `<button ... bg-sky-500 ... py-4` custom
    - Pills type/difficulté verbatim conservées
    - `src/translations/*.json` NON modifiés par ce plan
    - `npm run lint` + `npm run build` verts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 : App.tsx — bouton favori liste → Button iconOnly + toast de revert (D-08, D-10)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (bouton favori liste L174-182, imports en tête, accès à toggleFavorite/t)
    - src/ui/Button.tsx (iconOnly + aria-label, variant ghost fond sombre)
    - .planning/phases/04-formulaires-interactions/04-PATTERNS.md (section App.tsx L231-243)
    - .planning/phases/04-formulaires-interactions/04-UI-SPEC.md (Component Contract L154, cible 44px)
  </read_first>
  <action>
    S'assurer que `Button` (`./ui/Button`) et `Toast` (`@capacitor/toast`) sont importés dans App.tsx (les ajouter si absents ; `t` via useLanguage déjà disponible dans ce scope).
    Remplacer le `<button>` favori (L174-182) par `<Button variant="ghost" iconOnly aria-label={t('fav.remove')} className="!p-2 text-rose-500 hover:!bg-rose-50 w-11 h-11" onClick={(e) => { e.stopPropagation(); toggleFavorite(spot.id).catch(() => Toast.show({ text: t('fav.error.revert'), duration: 'short' })); }}><Heart size={20} className="fill-rose-500 text-rose-500" /></Button>`.
    Justification classes : `variant="ghost"` (fond sombre `bg-white/5`) est overridé pour reproduire le rendu clair actuel (`text-rose-500 hover:bg-rose-50 rounded-full`) ; `w-11 h-11` (44px) satisfait la cible tactile WCAG (UI-SPEC L56) alors que l'ancien `p-2` autour d'une icône 20px ne faisait que 36px.
    Contexte : cette liste ne montre que des favoris déjà actifs → `aria-label={t('fav.remove')}` est correct (l'action retire). Conserver `e.stopPropagation()` (le conteneur parent ouvre la fiche au clic).
  </action>
  <verify>
    <automated>npm run lint && grep -q "iconOnly" src/App.tsx && grep -q "fav.error.revert" src/App.tsx && grep -q "from '@capacitor/toast'" src/App.tsx && echo OK</automated>
  </verify>
  <done>Bouton favori liste = Button iconOnly + aria-label, rendu rose clair préservé, cible 44px ; échec toggle → revert (context) + toast discret ; lint vert.</done>
  <acceptance_criteria>
    - `grep -q "iconOnly" src/App.tsx` avec `aria-label={t('fav.remove')}`
    - Le onClick appelle `toggleFavorite(spot.id).catch(` avec `Toast.show` (`text: t('fav.error.revert')`)
    - Le `<button ... p-2 text-rose-500` custom supprimé au profit de `<Button`
    - Cible tactile ≥44px (`w-11 h-11` ou padding équivalent)
    - `npm run lint` + `npm run build` verts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → édition spot (React state) | Modification de spot par l'admin (accès réservé) |
| Composant → updateSpot/toggleFavorite → Supabase | Mutations franchissent le réseau |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-V4 | Tampering (V5 Input Validation) | validation édition admin | mitigate | Même validation client (longueur, ≥1 type) que les autres formulaires. UX ; garde RLS/Postgres inchangée. |
| T-04-A2 | Elevation of Privilege (V4) | accès AdminDashboard | accept | L'ouverture de l'AdminDashboard reste conditionnée à l'identité admin en amont (non modifiée ici) ; RLS Supabase reste la garde serveur. Ne pas affaiblir. |
| T-04-D3 | Denial of Service (état bloqué) | handleSaveEdit / toggleFavorite catch | mitigate | updateSpot rethrow → toast, overlay non bloqué ; revert favori + toast. Aucun alert() bloquant. |

Aucune installation de package (Toast déjà lié).
</threat_model>

<verification>
- `npm run lint` + `npm run build` verts.
- Recette manuelle (04-06) : édition admin invalide → inline ; échec réseau édition admin → toast + données conservées ; favori liste offline → revert + toast ; libellés admin en FR/EN corrects.
</verification>

<success_criteria>
- Édition admin via Input/Button + i18n + validation + feedback toast (D-09).
- Bouton favori liste DS (iconOnly, 44px) + toast revert (D-10, D-08).
- Pills verbatim ; garde admin conservée ; lint + build verts.
</success_criteria>

<output>
Créer `.planning/phases/04-formulaires-interactions/04-05-SUMMARY.md` à la fin.
</output>
