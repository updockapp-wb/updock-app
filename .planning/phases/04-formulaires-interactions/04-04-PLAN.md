---
phase: 04-formulaires-interactions
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/components/SpotDetail.tsx
autonomous: true
requirements: [UI-03, ROBUST-01, ROBUST-02]
must_haves:
  truths:
    - "Le bouton favori de SpotDetail est le composant DS Button (iconOnly + aria-label), cible tactile 44px, badge cadenas conservé pour l'utilisateur non connecté"
    - "En cas d'échec du toggle favori, l'état visuel se revert ET un toast discret traduit s'affiche (D-08)"
    - "Les champs nom/description du formulaire d'édition inline passent par Input surface=light (description multiline, maxLength)"
    - "Le bouton Enregistrer de l'édition est le composant DS Button (loading)"
    - "Une édition invalide (nom vide/trop long, desc trop longue, 0 type) affiche un message inline et n'appelle pas updateSpot ; un échec API affiche l'erreur inline en conservant les données"
  artifacts:
    - path: "src/components/SpotDetail.tsx"
      provides: "Bouton favori DS + toast revert + formulaire d'édition migré + validation/inline error"
      contains: "iconOnly"
  key_links:
    - from: "SpotDetail bouton favori"
      to: "toggleFavorite (Promise rejetante) → Toast.show(t('fav.error.revert'))"
      via: "catch côté composant (D-08)"
      pattern: "fav.error.revert"
    - from: "SpotDetail handleSaveEdit"
      to: "updateSpot (rethrow) → setError inline"
      via: "try/catch, données conservées (D-07)"
      pattern: "form.error.submit_failed"
---

<objective>
Migrer le bouton favori (D-10) et le formulaire d'édition inline de SpotDetail vers le design system, avec toast de revert favori (D-08) et validation + gestion d'erreur inline de l'édition (ROBUST-01/02, D-07).

Purpose: SpotDetail contient deux surfaces du périmètre : l'action favori (coeur) et le formulaire d'édition de spot. Les deux réutilisent les fondations livrées par 04-01 (Input) et 04-02 (updateSpot rethrow, toggleFavorite rejetante).
Output: `src/components/SpotDetail.tsx` migré (favori + édition).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-formulaires-interactions/04-RESEARCH.md
@.planning/phases/04-formulaires-interactions/04-PATTERNS.md
@.planning/phases/04-formulaires-interactions/04-UI-SPEC.md

<interfaces>
<!-- Fondations livrées par 04-01 et 04-02 — à consommer directement. -->
`Input` : `surface="light"`, `multiline`, `maxLength`, `error`, `label`, `value`, `onChange` (accepte HTMLTextAreaElement). Livré par 04-01.
`Button` : `variant="ghost"` = `bg-white/5 hover:bg-white/10 rounded-full` (pensé fond SOMBRE — sur fond clair, override via className `bg-slate-100 hover:bg-slate-200`). `iconOnly` impose `aria-label`. `variant="primary" loading` pour le submit.
`toggleFavorite(spotId): Promise<void>` (04-02) : rejette après revert en cas d'échec → le composant doit `.catch()` et appeler `Toast.show({ text: t('fav.error.revert'), duration: 'short' })`.
`updateSpot(spot): Promise<void>` (04-02) : rethrow en cas d'échec → handleSaveEdit doit `try/catch` et `setError(t('form.error.submit_failed'))`.

Clés i18n disponibles (04-01) : `fav.add`, `fav.remove`, `fav.error.revert`, `form.error.*`, `form.error.submit_failed`.
`@capacitor/toast` déjà dans le repo : `import { Toast } from '@capacitor/toast'`.

État actuel : bouton favori L300-319 (`<button className="w-10 h-10 rounded-full bg-slate-100 hover:bg-slate-200 ...">` + Heart + badge Lock !user) ; formulaire d'édition L557-677 (input nom L561-566, textarea desc L596-600, pills type L573-590, pills difficulté L607-616, bouton Save L669-676) ; handleSaveEdit L197-230.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Bouton favori → Button iconOnly + toast de revert (D-08, D-10)</name>
  <files>src/components/SpotDetail.tsx</files>
  <read_first>
    - src/components/SpotDetail.tsx (bouton favori L300-319, imports)
    - src/ui/Button.tsx (iconOnly + aria-label, variant ghost = fond sombre)
    - .planning/phases/04-formulaires-interactions/04-PATTERNS.md (section SpotDetail (A) L140-161)
    - .planning/phases/04-formulaires-interactions/04-UI-SPEC.md (Component Contract L154, cible 44px L56)
  </read_first>
  <action>
    Importer `Button` (`../ui/Button`) et `Toast` (`@capacitor/toast`).
    Remplacer le `<button>` favori (L300-319) par `<Button variant="ghost" iconOnly aria-label={user && isFavorite(spot.id) ? t('fav.remove') : t('fav.add')} className="...">`. Comme `variant="ghost"` est pensé fond sombre (`bg-white/5`), override via `className` pour reproduire le rendu clair actuel : `w-10 h-10 !bg-slate-100 hover:!bg-slate-200 shadow-sm relative overflow-visible` (cible tactile ≥44px déjà satisfaite par `w-10 h-10` = 40px → porter à `w-11 h-11` (44px) OU ajouter padding pour atteindre 44px, UI-SPEC L56).
    Conserver à l'intérieur le `<Heart size={20} className={user && isFavorite(spot.id) ? 'fill-accent text-accent' : 'text-slate-600'} />` et le badge cadenas `{!user && <span ...><Lock/></span>}` (L315-319) verbatim.
    Adapter le onClick : conserver la logique existante `e.stopPropagation(); if (!user) { onOpenAuth?.(); return; }` puis, au lieu d'un simple `toggleFavorite(spot.id)`, faire `toggleFavorite(spot.id).catch(() => Toast.show({ text: t('fav.error.revert'), duration: 'short' }))` (D-08 : toast sur revert).
    NE PAS toucher le bouton Share adjacent (L290-299).
  </action>
  <verify>
    <automated>npm run lint && grep -q "iconOnly" src/components/SpotDetail.tsx && grep -q "fav.error.revert" src/components/SpotDetail.tsx && grep -q "from '@capacitor/toast'" src/components/SpotDetail.tsx && grep -q "aria-label={user && isFavorite" src/components/SpotDetail.tsx && echo OK</automated>
  </verify>
  <done>Bouton favori = Button iconOnly + aria-label dynamique, rendu clair préservé, cible 44px, badge cadenas conservé ; échec toggle → revert (context) + toast discret ; lint vert.</done>
  <acceptance_criteria>
    - `grep -q "iconOnly" src/components/SpotDetail.tsx` et `aria-label` dynamique fav.add/fav.remove présent
    - Le onClick appelle `toggleFavorite(spot.id).catch(` avec `Toast.show` (`text: t('fav.error.revert')`) — source assertion
    - Le badge `Lock` (`!user`) toujours présent
    - Cible tactile ≥44px (source : `w-11 h-11` ou padding équivalent — pas `w-10 h-10` seul)
    - `npm run lint` vert
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Formulaire d'édition inline → Input/Button + validation + erreur inline</name>
  <files>src/components/SpotDetail.tsx</files>
  <behavior>
    - editForm.name vide/espaces → setEditError(t('form.error.name_required')), updateSpot NON appelé
    - editForm.name.trim().length > 100 → setEditError(t('form.error.name_too_long')), updateSpot NON appelé
    - editForm.description.length > 2000 → setEditError(t('form.error.desc_too_long')), updateSpot NON appelé
    - editForm.type.length === 0 → setEditError(t('form.error.type_required')), updateSpot NON appelé
    - updateSpot rejette → setEditError(t('form.error.submit_failed')), overlay reste ouvert avec données
    - saisie valide + updateSpot OK → overlay se ferme
  </behavior>
  <read_first>
    - src/components/SpotDetail.tsx (formulaire édition L557-677, handleSaveEdit L197-230)
    - src/components/AddSpotForm.tsx (référence de migration une fois 04-03 fait — même validation)
    - src/ui/Input.tsx (contrat étendu 04-01)
    - .planning/phases/04-formulaires-interactions/04-PATTERNS.md (section SpotDetail (B) L163-169)
  </read_first>
  <action>
    Importer `Input`. Ajouter `const [editError, setEditError] = useState<string | null>(null)`.
    Remplacer l'`<input>` nom (L561-566) par `<Input surface="light" label={t('spot.edit_name')} value={editForm.name} onChange={e => setEditForm({ ...editForm, name: e.target.value })} maxLength={100} />` et le `<textarea>` desc (L596-600) par `<Input surface="light" multiline label={t('spot.edit_description')} value={editForm.description} onChange={e => setEditForm({ ...editForm, description: e.target.value })} maxLength={2000} />`. Retirer les `<label>` manuels devenus redondants.
    Remplacer le bouton Save custom (L669-676) par `<Button variant="primary" size="lg" loading={isSaving} onClick={handleSaveEdit} className="w-full">{t('spot.edit_save')}</Button>` (conserver l'icône Save si souhaité en children).
    Dans `handleSaveEdit` (L197-230) : au début, `setEditError(null)` ; ajouter la même validation que AddSpotForm (nom trim non vide, ≤100 ; desc ≤2000 ; type.length ≥1) — en cas d'échec `setEditError(t('form.error.*'))` puis return AVANT l'upload/updateSpot. Comme `updateSpot` rethrow désormais (04-02), le catch existant doit faire `setEditError(t('form.error.submit_failed'))` (au lieu d'avaler l'erreur) — les données du formulaire restent (D-07), l'overlay ne se ferme pas sur échec.
    Ajouter l'affichage inline dans l'overlay (près du bouton Save) : `{editError && <div className="p-3 rounded-xl bg-red-50 border border-red-200 text-red-600 text-sm">{editError}</div>}`.
    CONSERVER VERBATIM : pills type L573-590 et pills difficulté L607-616 (A4/A5 — toggles, pas des Button), grille photos.
    Dette optionnelle (Pitfall 3, NON bloquant) : `URL.createObjectURL(file)` inline L638 fuit ; si cette zone est réécrite, appliquer le pattern ref+revoke d'AddSpotForm ; sinon laisser tel quel (bug préexistant, pas une régression introduite). À signaler dans le SUMMARY, ne pas forcer.
  </action>
  <verify>
    <automated>npm run lint && grep -c "surface=\"light\"" src/components/SpotDetail.tsx && grep -q "form.error.submit_failed" src/components/SpotDetail.tsx && grep -q "editError" src/components/SpotDetail.tsx && grep -q "loading={isSaving}" src/components/SpotDetail.tsx && echo OK</automated>
  </verify>
  <done>Champs édition via Input surface=light (desc multiline, maxLength) ; Save via Button loading ; validation inline + affichage erreur ; updateSpot rethrow capté → submit_failed inline, données conservées ; pills verbatim ; lint vert.</done>
  <acceptance_criteria>
    - `grep -c "surface=\"light\"" src/components/SpotDetail.tsx` >= 2 (nom + desc d'édition)
    - handleSaveEdit contient les branches de validation avec return AVANT updateSpot (source assertion)
    - Le catch de handleSaveEdit appelle `setEditError(t('form.error.submit_failed'))` (plus d'erreur avalée silencieusement)
    - `<Button variant="primary" ... loading={isSaving}` présent ; le `<button ... bg-sky-500 ... py-3` custom supprimé
    - Pills type/difficulté (`border-sky-500 bg-sky-50 text-sky-600`) toujours présentes verbatim
    - `npm run lint` + `npm run build` verts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Utilisateur → édition spot (React state) | Modification nom/description/type |
| Composant → updateSpot/toggleFavorite → Supabase | Mutations franchissent le réseau ; contrôle d'accès propriétaire/admin en amont (V4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-V3 | Tampering (V5 Input Validation) | validation édition | mitigate | Même validation client que AddSpotForm (longueur, ≥1 type). UX uniquement ; garde autoritaire RLS/Postgres inchangée. |
| T-04-A1 | Elevation of Privilege (V4 Access Control) | garde édition/favori | accept | La condition existante `user?.id === spot.user_id || email admin` (L268) et la garde `!user → onOpenAuth` (favori) sont CONSERVÉES — ne pas les affaiblir. RLS reste la garde serveur. |
| T-04-D2 | Denial of Service (état bloqué) | handleSaveEdit catch | mitigate | updateSpot rethrow capté → erreur inline, overlay non bloqué, données conservées (D-07). Pas d'alert() bloquant. |

Aucune installation de package (Toast déjà lié).
</threat_model>

<verification>
- `npm run lint` + `npm run build` verts.
- Recette manuelle (04-06) : favori offline → revert + toast ; édition invalide → inline ; édition échec réseau → submit_failed inline + données conservées.
</verification>

<success_criteria>
- Bouton favori DS (iconOnly, 44px, badge cadenas) + toast revert.
- Champs édition via Input surface=light ; Save via Button loading ; validation + erreur inline.
- Pills verbatim ; garde d'accès conservée ; lint + build verts.
</success_criteria>

<output>
Créer `.planning/phases/04-formulaires-interactions/04-04-SUMMARY.md` à la fin (mentionner l'état de la dette Pitfall 3 createObjectURL).
</output>
