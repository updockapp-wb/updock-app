---
phase: 04-formulaires-interactions
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/AddSpotForm.tsx
autonomous: true
requirements: [UI-03, ROBUST-01, ROBUST-02]
must_haves:
  truths:
    - "Les champs nom et description d'AddSpotForm sont rendus par le composant DS Input (surface=light), la description via multiline"
    - "Le bouton de soumission est le composant DS Button (variant primary, size lg, loading)"
    - "Une soumission avec nom vide/espaces, nom >100, description >2000 ou 0 type sélectionné affiche un message d'erreur inline clair et n'appelle pas addSpot (limites D-02 : nom 100 / description 2000 ; message dédié 0 type sélectionné, D-03)"
    - "Une soumission valide sans photo ouvre un dialogue de confirmation doux (Modal light+center) avant publication, non bloquant"
    - "Les données saisies sont conservées après une erreur (pas de reset du formulaire sur échec)"
    - "Aucune validation de type MIME ou de taille n'est ajoutée sur les photos uploadées dans cette phase — explicitement hors périmètre (D-05)"
  artifacts:
    - path: "src/components/AddSpotForm.tsx"
      provides: "Formulaire d'ajout migré DS + validation client + confirmation pas-de-photo"
      contains: "surface=\"light\""
  key_links:
    - from: "AddSpotForm handleSubmit"
      to: "validation client (nom/desc/type)"
      via: "fonction de validation avant addSpot"
      pattern: "form.error.type_required"
    - from: "AddSpotForm (0 photo)"
      to: "Modal surface=light layout=center"
      via: "confirmation douce D-04"
      pattern: "form.confirm.no_photo"
---

<objective>
Migrer le formulaire d'ajout de spot vers le design system (Input/Button/Modal) et ajouter la validation client + la confirmation douce « pas de photo » (UI-03, ROBUST-01, D-04).

Purpose: AddSpotForm est le formulaire principal de création. Il sert de référence de migration pour SpotDetail (04-04) et AdminDashboard (04-05). Le shell `max-w-lg` propre est CONSERVÉ (Pitfall 1 : le master Modal light+sheet est max-w-sm et rétrécirait le formulaire) ; seuls les champs, le bouton submit et le dialogue de confirmation passent au DS.
Output: `src/components/AddSpotForm.tsx` migré.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-formulaires-interactions/04-RESEARCH.md
@.planning/phases/04-formulaires-interactions/04-PATTERNS.md
@.planning/phases/04-formulaires-interactions/04-UI-SPEC.md

<interfaces>
<!-- Input étendu (livré par 04-01) — à consommer directement. -->
`Input` (src/ui/Input.tsx) : props `label, value, onChange, surface?: 'glass'|'light', multiline?, maxLength?, error?, placeholder?, required?, type?`. Défaut `surface='glass'` → passer explicitement `surface="light"` sur ce formulaire clair.
`Button` (src/ui/Button.tsx) : props `variant ('primary'|'secondary'|'ghost'|'danger'), size ('sm'|'md'|'lg'), loading, disabled, type ('button'|'submit'), className`. Gère déjà spinner Loader2 + disabled.
`Modal` (src/ui/Modal.tsx) : `isOpen, onClose, children, surface, layout`. `surface="light" layout="center"` = dialogue centré blanc (max-w-sm, p-8, rounded-3xl).

<!-- Analog de référence : AuthModal (seul form déjà migré) — pattern handleSubmit try/catch + inline error. -->
Clés i18n disponibles (livrées par 04-01) : `form.error.name_required`, `form.error.name_too_long`, `form.error.desc_too_long`, `form.error.type_required`, `form.error.submit_failed`, `form.confirm.no_photo.{title,body,confirm,cancel}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Migrer champs + bouton submit vers Input/Button + validation client inline</name>
  <files>src/components/AddSpotForm.tsx</files>
  <behavior>
    - name vide ou espaces uniquement → setError(t('form.error.name_required')), addSpot NON appelé
    - name.trim().length > 100 → setError(t('form.error.name_too_long')), addSpot NON appelé
    - description.length > 2000 → setError(t('form.error.desc_too_long')), addSpot NON appelé
    - type.length === 0 → setError(t('form.error.type_required')), addSpot NON appelé
    - saisie valide → error effacé, addSpot appelé
  </behavior>
  <read_first>
    - src/components/AddSpotForm.tsx (état complet — handleSubmit L95-122, champs L201-219, submit L262-268, pills type L167-179)
    - src/components/AuthModal.tsx (analog : handleSubmit try/catch + état error + affichage inline L149-153)
    - src/ui/Input.tsx (contrat étendu livré par 04-01)
    - src/ui/Button.tsx (loading/disabled/variant)
    - .planning/phases/04-formulaires-interactions/04-PATTERNS.md (section AddSpotForm L65-132)
  </read_first>
  <action>
    Importer `Input` et `Button` (`../ui/Input`, `../ui/Button`). Ajouter `const [error, setError] = useState<string | null>(null)`.
    Remplacer le `<input>` nom (L201-208) par `<Input surface="light" label={t('add.name')} value={name} onChange={e => setName(e.target.value)} placeholder={t('add.placeholder_name')} maxLength={100} />`. Remplacer le `<textarea>` description (L214-219) par `<Input surface="light" multiline label={t('add.desc')} value={description} onChange={e => setDescription(e.target.value)} placeholder={t('add.placeholder_desc')} maxLength={2000} />`. Retirer les `<label>` manuels correspondants (le label est désormais rendu par Input).
    Remplacer le `<button type="submit">` custom (L262-268) par `<Button type="submit" variant="primary" size="lg" loading={isSending} className="w-full">{isSending ? t('add.sending') : t('add.submit')}</Button>`.
    Dans `handleSubmit`, AVANT `if (!position) return`, exécuter la validation (D-02 = 100/2000, D-03) : `setError(null)` d'abord ; `const trimmed = name.trim()` ; si `!trimmed` → `setError(t('form.error.name_required'))` puis return ; si `trimmed.length > 100` → `setError(t('form.error.name_too_long'))` puis return ; si `description.length > 2000` → `setError(t('form.error.desc_too_long'))` puis return ; si `type.length === 0` → `setError(t('form.error.type_required'))` puis return.
    Ajouter l'affichage inline dans le `<form>` (au-dessus du Button submit), classe surface claire : `{error && <div className="p-3 rounded-xl bg-red-50 border border-red-200 text-red-600 text-sm">{error}</div>}`.
    CONSERVER VERBATIM : les pills de type (L167-179 — A4 RESEARCH, Button n'a pas de variante pill), la grille photos, le `<select>` de difficulté (harmonisation en pills = Task hors périmètre de ce plan, laisser le select). Ajuster uniquement le bouton de suppression de photo `p-1.5` → `p-2` (UI-SPEC L60).
    NE PAS reformuler les classes Tailwind conservées (anti-régression).
  </action>
  <verify>
    <automated>npm run lint && grep -c "surface=\"light\"" src/components/AddSpotForm.tsx && grep -q "multiline" src/components/AddSpotForm.tsx && grep -q "form.error.type_required" src/components/AddSpotForm.tsx && grep -q "bg-red-50 border border-red-200 text-red-600" src/components/AddSpotForm.tsx && grep -q "loading={isSending}" src/components/AddSpotForm.tsx && echo OK</automated>
  </verify>
  <done>Nom/description via Input surface=light (description multiline, maxLength) ; submit via Button loading ; validation inline des 4 règles avant addSpot ; données conservées sur erreur ; pills/grille photos verbatim ; lint vert.</done>
  <acceptance_criteria>
    - `grep -c "surface=\"light\"" src/components/AddSpotForm.tsx` >= 2 (nom + description)
    - `grep -q "maxLength={100}"` et `grep -q "maxLength={2000}"` présents
    - handleSubmit contient les 4 branches de validation avec return AVANT l'appel `addSpot` (source assertion)
    - `<Button type="submit"` présent ; le `<button ... py-4 bg-sky-500` custom supprimé (`grep -c "py-4 bg-sky-500 hover:bg-sky-600"` retourne 0)
    - Les pills de type (`border-sky-500 bg-sky-50 text-sky-600`) toujours présentes verbatim
    - `npm run lint` vert
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 : Confirmation douce « pas de photo » via Modal light+center (D-04)</name>
  <files>src/components/AddSpotForm.tsx</files>
  <read_first>
    - src/components/AddSpotForm.tsx (handleSubmit après Task 1, imageFiles state L20)
    - src/ui/Modal.tsx (surface="light" layout="center" L37-63)
    - .planning/phases/04-formulaires-interactions/04-UI-SPEC.md (Copywriting Contract L127-130, Component Contract L157, D-04)
    - .planning/phases/04-formulaires-interactions/04-PATTERNS.md (Pattern 3 confirmation pas-de-photo L127-132)
  </read_first>
  <action>
    Importer `Modal` (`../ui/Modal`). Ajouter `const [showNoPhotoConfirm, setShowNoPhotoConfirm] = useState(false)`.
    Extraire la logique de soumission effective (upload + addSpot + onSubmit + onClose + resetForm) dans une fonction `doSubmit()` async réutilisable.
    Dans `handleSubmit` : après la validation OK (Task 1) et `if (!position) return`, si `imageFiles.length === 0` → `setShowNoPhotoConfirm(true)` et return (ne PAS appeler addSpot tout de suite) ; sinon appeler `doSubmit()`.
    Rendre un `<Modal isOpen={showNoPhotoConfirm} onClose={() => setShowNoPhotoConfirm(false)} surface="light" layout="center">` contenant : titre `t('form.confirm.no_photo.title')`, corps `t('form.confirm.no_photo.body')`, et deux actions — un `Button variant="primary"` « Publier quand même » (`t('form.confirm.no_photo.confirm')`) qui fait `setShowNoPhotoConfirm(false)` puis `doSubmit()`, et un `Button variant="secondary"` « Ajouter une photo » (`t('form.confirm.no_photo.cancel')`) qui ferme simplement le dialogue (`setShowNoPhotoConfirm(false)`).
    Ton : dialogue doux, non bloquant, jamais culpabilisant (copy déjà validée dans les clés i18n). NE PAS utiliser `confirm()` natif.
  </action>
  <verify>
    <automated>npm run lint && grep -q "surface=\"light\" layout=\"center\"" src/components/AddSpotForm.tsx && grep -q "showNoPhotoConfirm" src/components/AddSpotForm.tsx && grep -q "form.confirm.no_photo.title" src/components/AddSpotForm.tsx && grep -q "imageFiles.length === 0" src/components/AddSpotForm.tsx && ! grep -q "window.confirm\|[^.]confirm(" src/components/AddSpotForm.tsx && echo OK</automated>
  </verify>
  <done>Soumission valide sans photo → Modal light+center non bloquant (2 actions positives) ; avec photo → soumission directe ; aucun confirm() natif ; lint vert.</done>
  <acceptance_criteria>
    - `grep -q "surface=\"light\" layout=\"center\"" src/components/AddSpotForm.tsx` (Modal DS utilisé pour la confirmation)
    - Les 4 clés `form.confirm.no_photo.{title,body,confirm,cancel}` référencées via `t()` dans le fichier
    - `handleSubmit` branche sur `imageFiles.length === 0` pour ouvrir le dialogue au lieu d'appeler addSpot directement
    - Aucun `confirm(` natif dans le fichier
    - `npm run lint` + `npm run build` verts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Utilisateur → formulaire (React state) | Saisie nom/description/type avant soumission |
| Formulaire → SpotsContext.addSpot → Supabase | Données franchissent le pont réseau |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-V2 | Tampering (V5 Input Validation) | validation nom/description | mitigate | Validation client longueur (nom ≤100, desc ≤2000) + trim + ≥1 type. UX uniquement — rappel : la garde autoritaire reste RLS/Postgres (non modifiée). Ne pas présenter comme contrôle sécurité suffisant. |
| T-04-X1 | Tampering (XSS stocké) | rendu description | mitigate | React échappe par défaut ; aucun `dangerouslySetInnerHTML` introduit. `whitespace-pre-line` reste sûr. |
| T-04-U1 | (hors scope) upload fichier | validation MIME/taille | accept | Explicitement hors scope (D-05) — NE PAS implémenter de validation de fichier dans ce plan. |

Aucune installation de package.
</threat_model>

<verification>
- `npm run lint` + `npm run build` verts.
- Recette manuelle (04-06) : saisie invalide → message inline ; soumission sans photo → dialogue doux ; données conservées après erreur.
</verification>

<success_criteria>
- Champs nom/description via Input surface=light (desc multiline) ; submit via Button loading.
- Validation inline des 4 règles avant addSpot ; confirmation douce pas-de-photo via Modal light+center.
- Pills/grille photos verbatim ; select difficulté inchangé ; aucun confirm() natif.
</success_criteria>

<output>
Créer `.planning/phases/04-formulaires-interactions/04-03-SUMMARY.md` à la fin.
</output>
