---
phase: 01-audit-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-audit-design-system/audit/PREFLIGHT.md
  - .planning/phases/01-audit-design-system/audit/screenshots/before/
autonomous: false
requirements: [DS-02, DS-03]
must_haves:
  truths:
    - "An audit/ artifact directory exists to receive all Phase 1 measurement + parity outputs"
    - "Chrome/Lighthouse availability is recorded, with the fallback decision documented"
    - "Before-change AuthModal screenshots (login, signup, error) exist as the pixel-parity oracle"
  artifacts:
    - path: ".planning/phases/01-audit-design-system/audit/PREFLIGHT.md"
      provides: "Chrome availability result + Lighthouse fallback decision"
      contains: "Chrome"
    - path: ".planning/phases/01-audit-design-system/audit/screenshots/before/"
      provides: "Pristine-state AuthModal screenshots (login/signup/error) — the parity oracle"
  key_links:
    - from: "audit/screenshots/before/"
      to: "Plan 01-06 AuthModal migration"
      via: "before/after pixel-identity comparison"
      pattern: "before"
---

<objective>
Establish the pristine-state prerequisites that MUST exist before any token or
component change touches the app: the audit artifact directory, a Chrome/
Lighthouse environment preflight (with documented fallback), and the "before"
AuthModal screenshots that serve as the pixel-parity oracle for the D-09 proof
migration.

Purpose: This phase's cardinal constraint is pixel-identity (harmonisation, not
rebranding). Parity can only be proven against a snapshot of the CURRENT render.
This plan captures that snapshot BEFORE any change, and confirms the perf-audit
environment.
Output: `audit/` directory, `audit/PREFLIGHT.md`, `audit/screenshots/before/*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-design-system/01-VALIDATION.md
@.planning/phases/01-audit-design-system/01-RESEARCH.md
@.planning/phases/01-audit-design-system/01-UI-SPEC.md
@src/components/AuthModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit/ directory + Chrome/Lighthouse preflight</name>
  <files>.planning/phases/01-audit-design-system/audit/PREFLIGHT.md</files>
  <read_first>
    - .planning/phases/01-audit-design-system/01-RESEARCH.md §Environment Availability + §Open Questions Q2 (Chrome-availability preflight + documented fallback: Vite compressed-size report + React Profiler)
    - .planning/phases/01-audit-design-system/01-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
    Create the directory `.planning/phases/01-audit-design-system/audit/` and the
    subdirectory `audit/screenshots/before/` (use `mkdir -p`; idempotent — plan
    01-02 also ensures `audit/`). Probe for a Chrome/Chromium binary with
    `command -v google-chrome chromium chromium-browser chrome` and also check the
    macOS default path `/Applications/Google Chrome.app`. Write `audit/PREFLIGHT.md`
    recording: (a) Node/npm/Vite/Capacitor versions from RESEARCH §Environment
    Availability, (b) whether Chrome was found, (c) the resulting Lighthouse
    decision — if Chrome present, Lighthouse mobile runs in plan 01-02; if ABSENT,
    record the documented fallback (Vite compressed-size report + React Profiler)
    and mark Lighthouse as manual/deferred per RESEARCH Open Question 2 and D-08
    (perf is directional, so absence downgrades but does not block). Do NOT install
    any test framework (locked project constraint).
  </action>
  <verify>
    <automated>test -d .planning/phases/01-audit-design-system/audit/screenshots/before && test -f .planning/phases/01-audit-design-system/audit/PREFLIGHT.md && grep -qi chrome .planning/phases/01-audit-design-system/audit/PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - `audit/` and `audit/screenshots/before/` directories exist.
    - `audit/PREFLIGHT.md` exists and contains a Chrome-availability result plus an
      explicit Lighthouse run-or-fallback decision.
    - No test-runner dependency was added to package.json.
  </acceptance_criteria>
  <done>Audit directory scaffold + preflight note committed; Lighthouse path decided.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture "before" AuthModal screenshots (parity oracle)</name>
  <action>Human runs the app and captures the three pristine-state AuthModal screenshots (login/signup/error) into audit/screenshots/before/. See <how-to-verify> for exact steps. No source file is changed.</action>
  <what-built>
    The audit/ scaffold and screenshot target folder are ready. Now the
    pristine-state AuthModal screenshots must be captured by a human running the
    app — there is no CLI to screenshot the auth modal, and this snapshot is the
    pixel-parity oracle for the D-09 migration (plan 01-06).
  </what-built>
  <read_first>
    - src/components/AuthModal.tsx (the screen being snapshotted — login, signup, and error states)
    - .planning/phases/01-audit-design-system/01-PATTERNS.md §AuthModal (parity oracle note)
  </read_first>
  <how-to-verify>
    1. Run the app: `npm run dev` (or `npm run build && npm run preview`).
    2. Open the AuthModal and capture THREE screenshots at the same viewport:
       - Login state (default).
       - Signup state (tap the "no account / sign up" toggle — shows first/last/display-name fields).
       - Error state (submit invalid login credentials to show the red error banner).
    3. Save them as PNGs into
       `.planning/phases/01-audit-design-system/audit/screenshots/before/`
       named `login.png`, `signup.png`, `error.png`.
    4. Do NOT change any source file yet — these must reflect the CURRENT render.
  </how-to-verify>
  <acceptance_criteria>
    - Three files exist: `before/login.png`, `before/signup.png`, `before/error.png`.
    - They were captured on unmodified source (no token/component change made yet).
  </acceptance_criteria>
  <resume-signal>Type "captured" once the three before/*.png screenshots are saved, or describe any blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | No installs in this plan (setup + screenshots only) — boundary not crossed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-A | Tampering | Parity oracle (before-screenshots) | mitigate | Screenshots captured on unmodified source BEFORE any change; any later source edit is compared against them. If captured post-change, the oracle is invalid. |
| T-01-01-B | Information Disclosure | Screenshots may show a test email | accept | Use a throwaway test credential; no real PII committed. |
</threat_model>

<verification>
- `audit/` and `audit/screenshots/before/` exist.
- `audit/PREFLIGHT.md` records Chrome availability + Lighthouse decision.
- Three `before/*.png` screenshots captured on pristine source.
- No test framework installed.
</verification>

<success_criteria>
The pixel-parity oracle and audit scaffold exist, captured before any code change,
so that DS-01/DS-02 work in later waves can be proven pixel-identical.
</success_criteria>

<output>
Create `.planning/phases/01-audit-design-system/01-01-SUMMARY.md` when done.
</output>
