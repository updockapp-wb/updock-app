---
phase: 01-audit-design-system
plan: 06
type: execute
wave: 4
depends_on: [01-01, 01-03, 01-04, 01-05]
files_modified:
  - src/components/AuthModal.tsx
autonomous: false
requirements: [DS-02]
must_haves:
  truths:
    - "AuthModal is re-authored to consume src/ui/{Modal, Header, Input, Button}"
    - "AuthModal renders pixel-identical to the before-screenshots (login, signup, error)"
    - "Auth logic (handleSubmit, mapAuthError, all supabase.auth.* / profiles calls) is byte-identical"
    - "HTML validation (required, minLength=6) is unchanged"
    - "The sky-500 primary submit remains the sole visual anchor"
  artifacts:
    - path: "src/components/AuthModal.tsx"
      provides: "D-09 proof screen migrated onto the src/ui master components"
      contains: "supabase.auth"
  key_links:
    - from: "src/components/AuthModal.tsx"
      to: "src/ui/Modal, Header, Input, Button"
      via: "imports + JSX composition replacing inline markup"
      pattern: "from '../ui/"
    - from: "src/components/AuthModal.tsx"
      to: "supabase.auth (unchanged)"
      via: "handleSubmit byte-identical"
      pattern: "supabase\\.auth\\."
---

<objective>
Prove the design system holds by migrating `AuthModal.tsx` (D-09) onto the
`src/ui/` master components — Modal + Header + Input + Button — the one screen that
exercises all of them plus error handling. Appearance strictly pixel-identical to
the before-screenshots; auth behavior byte-identical (no regression).

Purpose: The proof-screen validates DS-01/DS-02 before phases 2-4 adopt the design
system app-wide.
Output: re-authored `src/components/AuthModal.tsx`.

CONSTRAINT (cardinal): pixel-identity is the acceptance oracle, verified by
before/after screenshot — not by reading code. `mapAuthError`, `handleSubmit`, and
every `supabase.auth.*` / `supabase.from('profiles')` call stay byte-identical
(Security Domain: auth regression is the primary threat). Any class string that was
"cleaned up" during extraction is a defect (Pitfall 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-audit-design-system/01-PATTERNS.md
@.planning/phases/01-audit-design-system/01-UI-SPEC.md
@.planning/phases/01-audit-design-system/01-RESEARCH.md
@src/components/AuthModal.tsx
@src/ui/Modal.tsx
@src/ui/Header.tsx
@src/ui/Input.tsx
@src/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-author AuthModal JSX onto src/ui components (logic untouched)</name>
  <files>src/components/AuthModal.tsx</files>
  <read_first>
    - src/components/AuthModal.tsx (full file: L30-35 mapAuthError, L37-78 handleSubmit/supabase calls — KEEP byte-identical; L80-221 JSX — re-author)
    - src/ui/Modal.tsx, src/ui/Header.tsx, src/ui/Input.tsx, src/ui/Button.tsx (the master components + their props)
    - .planning/phases/01-audit-design-system/01-PATTERNS.md §`src/components/AuthModal.tsx` (migration checklist: what stays byte-identical)
    - .planning/phases/01-audit-design-system/01-UI-SPEC.md §Visual Hierarchy (primary CTA sole anchor) + §Copywriting Contract (i18n keys unchanged)
  </read_first>
  <action>
    Re-author ONLY the JSX (current L80-221) to compose the master components:
      - Replace the backdrop + motion shell + close button (L81-96, L218-220) with
        `<Modal isOpen={isOpen} onClose={onClose}>…</Modal>`.
      - Replace the `<h2>`/`<p>` title+subtitle (L98-103) with
        `<Header surface="glass" title={isLogin ? t('auth.title_login') :
        t('auth.title_signup')} subtitle={isLogin ? t('auth.subtitle_login') :
        t('auth.subtitle_signup')} />`.
      - Replace each field block (first name, last name [icon-less], display name,
        email, password) with `<Input label icon value onChange placeholder
        required [minLength]/>` — pass `icon={User|Mail|Lock}`; the last-name field
        passes NO icon (renders `pl-4`); keep `minLength={6}` on password.
      - Keep the error banner div (L191-195) verbatim (it is not a master component
        this phase — inherited).
      - Replace the submit `<button>` (L197-204) with `<Button type="submit"
        variant="primary" size="lg" loading={loading}>{isLogin ? t('auth.btn_login')
        : t('auth.btn_signup')}</Button>` — the sole sky-500 visual anchor.
      - Keep the login/signup toggle button (L207-217) as-is (inherited).
    KEEP BYTE-IDENTICAL: imports of `supabase`/`useLanguage`, all state hooks,
    `mapAuthError` (L30-35), `handleSubmit` + all `supabase.auth.*` /
    `supabase.from('profiles')` (L37-78), the display-name `useEffect`/`useRef`
    logic, and `required`/`minLength={6}`. Remove now-unused imports (e.g. `X`,
    `motion`, `AnimatePresence`) only if fully replaced by the master components.
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -q "from '../ui/Modal'" src/components/AuthModal.tsx && grep -q "supabase.auth.signInWithPassword" src/components/AuthModal.tsx && grep -q "supabase.auth.signUp" src/components/AuthModal.tsx && grep -q "minLength={6}" src/components/AuthModal.tsx && grep -q "mapAuthError" src/components/AuthModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AuthModal imports and composes `src/ui/{Modal,Header,Input,Button}`.
    - `handleSubmit`, `mapAuthError`, all `supabase.auth.*`/`profiles` calls, and
      `required`/`minLength={6}` are unchanged (grep-confirmed present + diff-verified).
    - Primary submit is the `primary`/`lg` Button (sky-500 anchor).
    - `npm run lint && npm run build` green.
  </acceptance_criteria>
  <done>AuthModal is re-authored onto the master components with auth logic untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pixel-parity + auth no-regression gate</name>
  <action>Human captures after-screenshots (login/signup/error), diffs against before/, and exercises the auth flow to confirm pixel-identity and zero auth regression. See <how-to-verify>.</action>
  <what-built>
    AuthModal now composes the `src/ui/` master components (Modal/Header/Input/
    Button) instead of inline markup. Because every class was extracted verbatim,
    the render must be pixel-identical to the before-screenshots, and the auth flow
    must behave exactly as before. This checkpoint is the pixel-parity + no-regression
    gate (the cardinal constraint of the phase).
  </what-built>
  <read_first>
    - .planning/phases/01-audit-design-system/audit/screenshots/before/ (login.png, signup.png, error.png — the oracle from plan 01-01)
    - .planning/phases/01-audit-design-system/01-RESEARCH.md Pitfall 5 (what silently drifts)
  </read_first>
  <how-to-verify>
    1. Run `npm run build && npm run preview` (or `npm run dev`).
    2. Capture AFTER screenshots of the same three states — login, signup, error —
       into `audit/screenshots/after/` and compare against `before/`:
       padding, the 12px `py-3` input height, placeholder color (`text-white/20`),
       focus ring (`ring-sky-500`), the 16px input font (no iOS focus-zoom), the
       32px modal radius, and the sky-500 primary button glow must all match.
    3. Exercise the auth flow: valid login succeeds; signup shows the extra fields
       and completes; invalid credentials show the red error banner
       (`mapAuthError`). No crash, no changed behavior.
  </how-to-verify>
  <acceptance_criteria>
    - After screenshots match before for login, signup, and error states (no drift).
    - Login + signup + error paths behave exactly as before (auth unregressed).
  </acceptance_criteria>
  <resume-signal>Type "identical + unregressed" if parity and auth both hold, or describe any drift/regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase auth | Existing auth surface — MUST remain byte-identical (no new validation, no changed calls) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / DoS (lockout) | AuthModal handleSubmit / supabase.auth.* | mitigate | Keep `handleSubmit`, `mapAuthError`, all `supabase.auth.*`/`profiles` calls and `required`/`minLength={6}` byte-identical; verify login+signup+error manually before merge (Task 2). |
| T-01-06-T | Tampering | Pixel drift during extraction | mitigate | Before/after screenshot comparison as the acceptance oracle (not code inspection); any "cleaned up" class string is a defect (Pitfall 5). |
| T-01-06-V | Input Validation | HTML validation weakened | mitigate | `required` + `minLength={6}` preserved verbatim; new validation is Phase 4 (V5), not here. |
</threat_model>

<verification>
- `npm run lint && npm run build` green.
- AuthModal imports/composes `src/ui/{Modal,Header,Input,Button}`.
- Auth logic byte-identical (grep + diff).
- Human-verify: after == before screenshots (login/signup/error) AND auth flow unregressed.
</verification>

<success_criteria>
D-09 satisfied: AuthModal runs on the master components with strictly identical
appearance and byte-identical auth behavior — the proof that the design system
(DS-01 + DS-02) is ready for phase-by-phase adoption in Phases 2-4.
</success_criteria>

<output>
Create `.planning/phases/01-audit-design-system/01-06-SUMMARY.md` when done.
</output>
