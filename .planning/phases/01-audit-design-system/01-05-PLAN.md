---
phase: 01-audit-design-system
plan: 05
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/ui/Card.tsx
  - src/ui/Header.tsx
autonomous: true
requirements: [DS-02]
must_haves:
  truths:
    - "src/ui/Card exposes glass and light card variants, values extracted verbatim from existing surfaces"
    - "src/ui/Header exposes the stacked title+subtitle shape and the row-with-close-button shape"
    - "Text color is a prop/variant (white on glass, slate-800 on light) — not hard-coded to one surface"
    - "Both components consume tokens / existing values only; no invented design values"
  artifacts:
    - path: "src/ui/Card.tsx"
      provides: "Master surface: glass + light (resting/interactive) card variants"
      exports: ["default"]
    - path: "src/ui/Header.tsx"
      provides: "Master heading: title(+subtitle) + optional close-button slot"
      exports: ["default"]
  key_links:
    - from: "src/ui/Header.tsx"
      to: "AuthModal title/subtitle"
      via: "text-2xl font-bold heading role (24px/700) extracted verbatim"
      pattern: "text-2xl font-bold"
    - from: "src/ui/Card.tsx"
      to: "existing glass + light card surfaces"
      via: "rounded-2xl / rounded-[32px] variants, shadow-sm resting"
      pattern: "rounded-2xl|rounded-\\[32px\\]"
---

<objective>
Build the two role-match master components in `src/ui/` (D-04): Card and Header.
Each exposes the distinct rendered shapes already present in the app as VARIANTS,
with class values extracted VERBATIM from the PATTERNS analog table (D-05/D-06).
Header is consumed by the plan 01-06 AuthModal migration (stacked title+subtitle).

Purpose: Complete the DS-02 master component set (Button/Card/Input/Modal/Header).
Output: `src/ui/Card.tsx`, `src/ui/Header.tsx`.

CONSTRAINT: Do NOT unify distinct rendered surfaces (glass `rounded-[32px]` vs
light `rounded-2xl`) — expose them as variants. Text color varies by surface →
expose as prop, never hard-code one. Lucide + Tailwind only + relative imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-audit-design-system/01-PATTERNS.md
@.planning/phases/01-audit-design-system/01-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Contracts to define (D-05 conventions). Verbatim class donors are in PATTERNS.md §Card / §Header. -->
Card:   interface CardProps   { variant?: 'glass'|'light'; interactive?: boolean; className?: string; children: React.ReactNode }
Header: interface HeaderProps { title: string; subtitle?: string; onClose?: () => void;
                                surface?: 'glass'|'light'  /* controls text color: white vs slate-800 */ }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: src/ui/Card.tsx — glass + light card variants (role-match)</name>
  <files>src/ui/Card.tsx</files>
  <read_first>
    - .planning/phases/01-audit-design-system/01-PATTERNS.md §`src/ui/Card.tsx` (glass = NearbySpotsList:18; light resting = Profile:265; interactive = AdminDashboard:101 — verbatim class strings)
    - .planning/phases/01-audit-design-system/01-UI-SPEC.md §Radius & Elevation (rounded-2xl card default, shadow-sm resting)
  </read_first>
  <action>
    Create `src/ui/Card.tsx` (`export default function Card({ variant='light',
    interactive=false, className, children }: CardProps)`). Two variants, values
    VERBATIM from PATTERNS:
      - glass:  `bg-white/95 backdrop-blur-md rounded-[32px] p-6 shadow-xl border border-white/20`
      - light (resting): `bg-white p-4 rounded-2xl border border-slate-100 shadow-sm`
    When `interactive` on the light variant, append the hover-elevation classes
    verbatim from AdminDashboard:101: `cursor-pointer hover:border-sky-300
    hover:shadow-md transition-all`. Merge an optional `className` after the variant
    classes. Do NOT unify the glass `rounded-[32px]` and light `rounded-2xl` radii.
  </action>
  <verify>
    <automated>npm run build && grep -q "rounded-2xl" src/ui/Card.tsx && grep -q "bg-white/95 backdrop-blur-md" src/ui/Card.tsx && grep -q "shadow-sm" src/ui/Card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Card exposes `glass` and `light` (resting + interactive) variants with verbatim classes.
    - Distinct radii preserved (`rounded-[32px]` glass, `rounded-2xl` light).
    - Build clean.
  </acceptance_criteria>
  <done>Card master reproduces both rendered card surfaces as variants.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: src/ui/Header.tsx — title+subtitle / row-with-close (role-match)</name>
  <files>src/ui/Header.tsx</files>
  <read_first>
    - src/components/AuthModal.tsx L98-103 (stacked title+subtitle) — the shape the 01-06 migration consumes
    - .planning/phases/01-audit-design-system/01-PATTERNS.md §`src/ui/Header.tsx` (stacked = AuthModal:98-103; row-with-close = FiltersModal:38-43 — verbatim)
    - .planning/phases/01-audit-design-system/01-UI-SPEC.md §Typography (heading 24px/700) + §Visual Hierarchy (heading→inputs→CTA order)
  </read_first>
  <action>
    Create `src/ui/Header.tsx` (`export default function Header({ title, subtitle,
    onClose, surface='glass' }: HeaderProps)`). Heading role VERBATIM: `text-2xl
    font-bold` (24px/700). Text color by `surface`: `text-white` (glass) vs
    `text-slate-800` (light) — expose as the `surface` prop, do NOT hard-code one.
    If `onClose` provided, render the row shape from FiltersModal:38-43:
    `<div className="flex justify-between items-center mb-6">` + heading +
    `<button onClick={onClose} className="p-2 bg-slate-100 rounded-full
    hover:bg-slate-200"><X size={20} className="text-slate-600" /></button>`.
    Otherwise render the stacked shape from AuthModal:98-103: `<h2 className="text-2xl
    font-bold text-white mb-2">{title}</h2>` + optional `<p className="text-white/50
    text-sm mb-8">{subtitle}</p>` (adjust the two color classes by `surface`). Keep
    spacing classes (`mb-2`, `mb-8`, `mb-6`) verbatim.
  </action>
  <verify>
    <automated>npm run build && grep -q "text-2xl font-bold" src/ui/Header.tsx && grep -q "text-white/50" src/ui/Header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Header renders stacked title+subtitle (default) and row-with-close (when `onClose`).
    - Heading is `text-2xl font-bold`; text color driven by `surface` prop.
    - Verbatim spacing/subtitle classes preserved.
    - Build clean.
  </acceptance_criteria>
  <done>Header master reproduces both rendered heading shapes; ready for the AuthModal proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational components; no data/auth/network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-T | Tampering | Verbatim class extraction | mitigate | Class strings copied byte-for-byte from PATTERNS analogs; grep gates assert key strings; parity proven downstream (plan 01-06) against before-screenshots. |
</threat_model>

<verification>
- `npm run build` clean for Card + Header.
- Grep gates confirm verbatim key class strings.
- No invented design values; distinct surfaces preserved as variants.
</verification>

<success_criteria>
The remaining two DS-02 master components (Card, Header) exist in `src/ui/` as
verbatim-extracted variants, completing the Button/Card/Input/Modal/Header set.
</success_criteria>

<output>
Create `.planning/phases/01-audit-design-system/01-05-SUMMARY.md` when done.
</output>
