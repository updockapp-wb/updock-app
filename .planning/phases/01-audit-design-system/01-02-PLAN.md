---
phase: 01-audit-design-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - package.json
  - package-lock.json
  - .planning/phases/01-audit-design-system/audit/build-size.txt
  - .planning/phases/01-audit-design-system/audit/stats.html
  - .planning/phases/01-audit-design-system/audit/knip.md
  - .planning/phases/01-audit-design-system/audit/depcheck.json
  - .planning/phases/01-audit-design-system/audit/lighthouse.json
  - .planning/phases/01-audit-design-system/01-AUDIT.md
autonomous: false
requirements: [DS-03]
user_setup: []
must_haves:
  truths:
    - "A firm bundle baseline (raw + gzip per chunk) is captured verbatim from vite build"
    - "Bundle composition is captured as a visualizer treemap"
    - "Dependency inventory (unused files/exports/deps + version mismatch) is captured"
    - "A directional perf baseline (Lighthouse mobile or documented fallback) is captured"
    - "An audit document exists describing structure, dependencies, debt, UI incoherences, with a directional PERF-03 target"
  artifacts:
    - path: ".planning/phases/01-audit-design-system/audit/build-size.txt"
      provides: "Firm bundle baseline (vite build compressed-size table)"
    - path: ".planning/phases/01-audit-design-system/audit/stats.html"
      provides: "rollup-plugin-visualizer treemap (gzip+brotli)"
    - path: ".planning/phases/01-audit-design-system/audit/knip.md"
      provides: "Unused files/exports/deps inventory"
    - path: ".planning/phases/01-audit-design-system/01-AUDIT.md"
      provides: "DS-03 deliverable — current-state audit + numeric baseline + directional target"
      contains: "PERF-03"
  key_links:
    - from: "audit/build-size.txt"
      to: "01-AUDIT.md"
      via: "gzip total JS frozen as firm baseline; -15% directional target set against it"
      pattern: "gzip"
    - from: "src/components/Map.tsx"
      to: "01-AUDIT.md UI-incoherence inventory"
      via: "hard-coded marker hex inventory (inventory only — migration is Phase 2)"
      pattern: "#"
---

<objective>
Produce the DS-03 architecture audit: an exhaustive, numeric photograph of the
current app (bundle size + composition, dependency inventory, directional perf,
UI incoherences) with a FIRM bundle baseline and a DIRECTIONAL PERF-03 target
(D-07, D-08). Runs on pristine source in Wave 1, before any token change, so the
baseline reflects the existing app.

Purpose: PERF-03 (Phase 5) is unmeasurable without a frozen baseline; downstream
UI migrations need the incoherence inventory.
Output: `audit/*` raw artifacts + `01-AUDIT.md` deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-design-system/01-RESEARCH.md
@.planning/phases/01-audit-design-system/01-VALIDATION.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
@vite.config.ts
@package.json

<interfaces>
<!-- Exact audit commands — from RESEARCH §Standard Stack + §Code Examples. Use verbatim. -->
Firm bundle baseline (no install):
  npm run build | tee .planning/phases/01-audit-design-system/audit/build-size.txt

Visualizer wired into vite.config.ts (RESEARCH §Code Examples):
  import { visualizer } from 'rollup-plugin-visualizer'
  visualizer({ filename: '.planning/phases/01-audit-design-system/audit/stats.html', template: 'treemap', gzipSize: true, brotliSize: true }) as PluginOption

Dependency inventory:
  npx knip --reporter markdown > .planning/phases/01-audit-design-system/audit/knip.md
  npx depcheck --json > .planning/phases/01-audit-design-system/audit/depcheck.json
  npm outdated || true
  npm ls @capacitor/cli @capacitor/core   # verify/deny CONCERNS.md mismatch (RESEARCH: likely already resolved, cli 8.2.0)

Perf (only if Chrome present per audit/PREFLIGHT.md; else fallback):
  npx lighthouse http://localhost:4173 --form-factor=mobile --output=json --output=html --output-path=.planning/phases/01-audit-design-system/audit/lighthouse
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Audit-tool legitimacy gate (blocking human-verify)</name>
  <action>Human performs the legitimacy glance for each [ASSUMED] audit tool (npm view <pkg> scripts.postinstall + source-repo match) and approves before any install. See <how-to-verify>. Not auto-approvable.</action>
  <what-built>
    Pre-install legitimacy gate. RESEARCH tagged every audit tool `[ASSUMED]`
    (slopcheck could not run in the research environment). Per the package
    legitimacy gate, each dev tool must be human-verified before install. This
    checkpoint is NOT auto-approvable.
  </what-built>
  <read_first>
    - .planning/phases/01-audit-design-system/01-RESEARCH.md §Package Legitimacy Audit (table of [ASSUMED] packages + verified versions)
  </read_first>
  <how-to-verify>
    For each package — `rollup-plugin-visualizer@7.0.1`, `knip@6.29.0`,
    `depcheck@1.4.7`, and `lighthouse@13.4.1` (run via npx) — do a quick
    legitimacy glance:
    1. `npm view <pkg> scripts.postinstall` — confirm there is NO suspicious
       postinstall script.
    2. Confirm the source repo on the registry page matches RESEARCH:
       npmjs.com/package/rollup-plugin-visualizer (github.com/btd/…),
       /knip (webpro-nl/knip), /depcheck (depcheck/depcheck),
       /lighthouse (GoogleChrome/lighthouse).
    3. Approve only the packages that check out.
  </how-to-verify>
  <acceptance_criteria>
    - Each of the four tools has been glanced for postinstall + source repo.
    - No `[SLOP]` package is approved.
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow the dev-dependency installs, or name any package to exclude.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install audit tools + wire visualizer + capture bundle/dependency baseline</name>
  <files>vite.config.ts, package.json, package-lock.json, .planning/phases/01-audit-design-system/audit/build-size.txt, .planning/phases/01-audit-design-system/audit/stats.html, .planning/phases/01-audit-design-system/audit/knip.md, .planning/phases/01-audit-design-system/audit/depcheck.json</files>
  <read_first>
    - .planning/phases/01-audit-design-system/01-RESEARCH.md §Code Examples (exact commands) + §Standard Stack (verified versions) + Pitfall 4 (enable gzipSize+brotliSize)
    - vite.config.ts (current: plugins:[react()] only — add visualizer)
    - .planning/codebase/CONCERNS.md (Capacitor version-mismatch claim to re-verify)
  </read_first>
  <action>
    After the legitimacy gate: `npm install -D rollup-plugin-visualizer knip depcheck`
    (mkdir -p the audit dir first — idempotent with plan 01-01). Wire the
    `visualizer(...)` plugin into `vite.config.ts` exactly as in the `<interfaces>`
    block (filename → audit/stats.html, template treemap, `gzipSize: true`,
    `brotliSize: true` per Pitfall 4). Capture the FIRM bundle baseline:
    `npm run build | tee audit/build-size.txt` (this also emits stats.html).
    Capture dependency inventory: `npx knip --reporter markdown > audit/knip.md`;
    `npx depcheck --json > audit/depcheck.json`; run `npm outdated || true` and
    `npm ls @capacitor/cli @capacitor/core` and record their output for the audit
    doc (re-verify — do not assume the CONCERNS.md mismatch persists; RESEARCH
    indicates cli 8.2.0 already matches core 8.x). Do NOT migrate anything — this
    is measurement only.
  </action>
  <verify>
    <automated>npm run lint && test -s .planning/phases/01-audit-design-system/audit/build-size.txt && test -f .planning/phases/01-audit-design-system/audit/stats.html && test -s .planning/phases/01-audit-design-system/audit/knip.md && test -f .planning/phases/01-audit-design-system/audit/depcheck.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; `build-size.txt` contains a per-chunk raw+gzip table.
    - `stats.html` exists (treemap with gzip/brotli).
    - `knip.md` + `depcheck.json` exist and are non-trivial.
    - Capacitor cli/core versions recorded (mismatch confirmed or denied).
    - ESLint clean.
  </acceptance_criteria>
  <done>Firm bundle baseline + composition + dependency inventory captured as artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Capture directional perf + author 01-AUDIT.md (DS-03 deliverable)</name>
  <files>.planning/phases/01-audit-design-system/audit/lighthouse.json, .planning/phases/01-audit-design-system/01-AUDIT.md</files>
  <read_first>
    - .planning/phases/01-audit-design-system/audit/PREFLIGHT.md (Chrome decision from plan 01-01)
    - .planning/phases/01-audit-design-system/01-RESEARCH.md Pitfalls 2/3 (Lighthouse against preview not dev; treat as directional proxy) + §Open Questions Q1 (freeze gzip total JS as headline)
    - src/components/Map.tsx (hard-coded marker/cluster hexes — inventory only, D-03; migration is Phase 2)
  </read_first>
  <action>
    Perf slice: if `audit/PREFLIGHT.md` says Chrome is present, run
    `npm run build && npm run preview` then Lighthouse mobile against
    http://localhost:4173 (the `<interfaces>` command) → `audit/lighthouse.*`. If
    Chrome is ABSENT (current env: none on PATH), record the documented fallback in
    the audit doc — Vite compressed-size report (already captured) + React Profiler
    notes on Map/nav screens — and mark Lighthouse manual/deferred (D-08 perf is
    directional, so this does not block). Then author `01-AUDIT.md` covering, from
    the captured artifacts: (a) structure summary (cite STRUCTURE.md/STACK.md),
    (b) dependency inventory + Capacitor version verdict, (c) UI-incoherence
    inventory — grep-count hard-coded `Map.tsx` marker hexes, note duplicated modal
    patterns and the dead `@theme` Deep Ocean palette / `.glass` class (0 consumers,
    RESEARCH Runtime State Inventory), (d) FIRM baseline = gzip total JS from
    build-size.txt, (e) DIRECTIONAL PERF-03 target (e.g. -15% gzip JS, refined in
    Phase 5). Map.tsx colours are INVENTORIED only — do not migrate.
  </action>
  <verify>
    <automated>test -s .planning/phases/01-audit-design-system/01-AUDIT.md && grep -q "PERF-03" .planning/phases/01-audit-design-system/01-AUDIT.md && grep -qiE "gzip|baseline" .planning/phases/01-audit-design-system/01-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `01-AUDIT.md` describes structure, dependencies, debt, and UI incoherences.
    - A firm numeric bundle baseline (gzip total JS) is stated verbatim from build-size.txt.
    - A directional PERF-03 target is stated (explicitly flagged directional, refined Phase 5).
    - Map.tsx hard-coded hexes are inventoried (count), NOT migrated.
    - Perf captured via Lighthouse OR the documented fallback per PREFLIGHT.md.
  </acceptance_criteria>
  <done>DS-03 audit document complete; firm baseline frozen; directional target set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | Installing `[ASSUMED]` dev tools; untrusted package code runs at install/build time |
| build output → committed artifacts | Generated audit files committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm dev-tool installs (visualizer/knip/depcheck/lighthouse) | mitigate | Blocking human-verify legitimacy gate (Task 1): `npm view <pkg> scripts.postinstall` glance + source-repo match before install; `[SLOP]` forbidden; not auto-approvable. |
| T-01-02-I | Information Disclosure | lighthouse.json / stats.html may embed local paths | accept | Dev-only artifacts under `.planning/`; no secrets; low value. |
| T-01-02-D | Denial of Service | audit tools are dev-time, one-shot | accept | No runtime/production surface added; visualizer is build-time only, not shipped. |
</threat_model>

<verification>
- `npm run build` succeeds with visualizer wired; `build-size.txt` + `stats.html` present.
- `knip.md` + `depcheck.json` present; Capacitor versions recorded.
- `01-AUDIT.md` present with firm baseline + directional PERF-03 target + Map.tsx hex inventory.
- Perf captured (Lighthouse) or fallback documented.
- `npm run lint` clean.
</verification>

<success_criteria>
DS-03 satisfied: an audit document describes the current state with a firm numeric
bundle baseline and a directional PERF-03 target, backed by committed raw artifacts.
</success_criteria>

<output>
Create `.planning/phases/01-audit-design-system/01-02-SUMMARY.md` when done.
</output>
