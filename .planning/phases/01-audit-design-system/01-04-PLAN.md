---
phase: 01-audit-design-system
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - src/ui/Modal.tsx
  - src/ui/Input.tsx
  - src/ui/Button.tsx
autonomous: true
requirements: [DS-02]
must_haves:
  truths:
    - "src/ui/Modal, Input, Button exist as reusable master components extracted verbatim from AuthModal (D-04, D-05)"
    - "Modal codifies the AnimatePresence shell + glass surface + backdrop + isOpen/onClose contract"
    - "Modal's 32px glass shell consumes the rounded-4xl token (not the literal rounded-[32px])"
    - "Input supports optional icon slot (pl-12 with icon / pl-4 without), keeping the 16px iOS rule"
    - "Button exposes the full variant set (primary/secondary/ghost/danger × sm/md/lg × loading/disabled), each style derived from existing values (D-06)"
    - "Icon-only Button variants require an aria-label"
  artifacts:
    - path: "src/ui/Modal.tsx"
      provides: "Master overlay: AnimatePresence + glass surface + backdrop + close button"
      exports: ["default"]
    - path: "src/ui/Input.tsx"
      provides: "Master form control: label + optional icon slot + input + focus ring"
      exports: ["default"]
    - path: "src/ui/Button.tsx"
      provides: "Master control: variant/size/state matrix + a11y aria-label contract"
      exports: ["default"]
  key_links:
    - from: "src/ui/Modal.tsx"
      to: "framer-motion AnimatePresence"
      via: "verbatim scale 0.95->1 open/close from AuthModal"
      pattern: "AnimatePresence"
    - from: "src/ui/Modal.tsx glass shell"
      to: "src/index.css --radius-4xl token"
      via: "rounded-4xl utility class (renders 32px)"
      pattern: "rounded-4xl"
    - from: "src/ui/Input.tsx"
      to: "index.css input{font-size:16px}"
      via: "no inline font-size override (iOS anti-zoom)"
      pattern: "focus:ring-sky-500"
---

<objective>
Build the three AuthModal-donor master components in the new `src/ui/` folder
(D-04): Modal, Input, Button. Each is extracted VERBATIM from `AuthModal.tsx`
(the master donor) and the variant-source table in PATTERNS — no visual invention.
Variants widen the API (D-06); they never widen the appearance (D-05). These are
the proof-critical trio that plan 01-06 re-authors AuthModal onto.

Purpose: DS-02 master components that consume tokens / render existing patterns
pixel-identically.
Output: `src/ui/Modal.tsx`, `src/ui/Input.tsx`, `src/ui/Button.tsx`.

CONSTRAINT: Do NOT normalise, simplify, or "clean up" any extracted class string
(Pitfall 5). Copy class strings exactly, including the 12px `py-3` exception. The
one exception is the 32px glass shell radius: consume it via the `rounded-4xl`
token (plan 01-03, renders an identical 32px) instead of the literal `rounded-[32px]`
— this is token wiring per DS-01/UI-SPEC line 51, not a visual change. Lucide icons
+ Tailwind only + relative imports (CONVENTIONS).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-audit-design-system/01-PATTERNS.md
@.planning/phases/01-audit-design-system/01-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@src/components/AuthModal.tsx

<interfaces>
<!-- Component contracts to define (D-05 conventions: PascalCase, export default, interface [Name]Props, isOpen/onClose, handle*). -->
Modal:  interface ModalProps { isOpen: boolean; onClose: () => void; children: React.ReactNode }
Input:  interface InputProps  { label: string; value: string; onChange: React.ChangeEventHandler<HTMLInputElement>;
                                icon?: LucideIcon; type?: string; placeholder?: string; required?: boolean;
                                minLength?: number }
Button: interface ButtonProps { variant?: 'primary'|'secondary'|'ghost'|'danger'; size?: 'sm'|'md'|'lg';
                                loading?: boolean; disabled?: boolean; iconOnly?: boolean; 'aria-label'?: string;
                                type?: 'button'|'submit'; onClick?: ...; children?: React.ReactNode }
<!-- Verbatim class donors are in PATTERNS.md §Modal / §Input / §Button (with AuthModal line numbers). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: src/ui/Modal.tsx — AnimatePresence glass overlay (exact analog)</name>
  <files>src/ui/Modal.tsx</files>
  <read_first>
    - src/components/AuthModal.tsx L80-96, L218-220 (backdrop + motion shell + close button — the exact donor)
    - .planning/phases/01-audit-design-system/01-PATTERNS.md §`src/ui/Modal.tsx` (verbatim class strings) + §Shared Patterns (glass + Framer Motion)
    - .planning/phases/01-audit-design-system/01-UI-SPEC.md line 51 (rounded-[32px] reconciles into the radius token → rounded-4xl)
    - .planning/codebase/CONVENTIONS.md (Modal/Drawer pattern, isOpen/onClose)
  </read_first>
  <action>
    Create `src/ui/Modal.tsx` as `export default function Modal({ isOpen, onClose,
    children }: ModalProps)`. Extract VERBATIM from AuthModal L81-96: the
    `<AnimatePresence>{isOpen && (...)}` wrapper; the backdrop
    `fixed inset-0 z-[5000] flex items-center justify-center p-4 bg-black/60
    backdrop-blur-md`; the `motion.div` with `initial/animate/exit` scale 0.95->1
    and surface `bg-white/10 backdrop-blur-xl border border-white/20 w-full max-w-sm
    rounded-4xl p-8 shadow-2xl relative overflow-hidden`; the top-right close
    button `absolute top-4 right-4 p-2 bg-white/5 hover:bg-white/10 rounded-full
    transition-colors` with `<X size={20} className="text-white/70" />`. Render
    `{children}` after the close button. The outer glass shell MUST use the
    `rounded-4xl` token (plan 01-03 `--radius-4xl: 2rem` → identical 32px) in place
    of the literal `rounded-[32px]` — token consumption per UI-SPEC line 51 / DS-01,
    NOT a visual change. Keep `z-[5000]` verbatim.
  </action>
  <verify>
    <automated>npm run build && grep -q "AnimatePresence" src/ui/Modal.tsx && grep -q "bg-black/60 backdrop-blur-md" src/ui/Modal.tsx && grep -q "z-\[5000\]" src/ui/Modal.tsx && grep -q "rounded-4xl" src/ui/Modal.tsx && ! grep -q "rounded-\[32px\]" src/ui/Modal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/Modal.tsx` exports default `Modal({isOpen,onClose,children})`.
    - Backdrop, glass surface, scale animation, and close button classes are byte-identical to AuthModal, except the outer radius consumes the `rounded-4xl` token (32px) rather than the literal `rounded-[32px]`.
    - No `rounded-[32px]` literal remains in the file.
    - `tsc -b` + build clean.
  </acceptance_criteria>
  <done>Modal master renders the AuthModal shell identically (radius via rounded-4xl token) with consumer-supplied children.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: src/ui/Input.tsx — label + optional icon slot + input (exact analog)</name>
  <files>src/ui/Input.tsx</files>
  <read_first>
    - src/components/AuthModal.tsx L159-172 (email field: label + icon + input — canonical) and L127-133 (icon-less last-name variant → pl-4)
    - .planning/phases/01-audit-design-system/01-PATTERNS.md §`src/ui/Input.tsx` (verbatim classes + variant notes: pl-12 vs pl-4, py-3 exception, do-not-override 16px)
    - .planning/phases/01-audit-design-system/01-UI-SPEC.md §Spacing (12px exception) + §Typography (label 12px bold uppercase)
  </read_first>
  <action>
    Create `src/ui/Input.tsx` (`export default function Input(props: InputProps)`).
    Extract VERBATIM from AuthModal L159-172: wrapper `space-y-2`; label
    `text-xs font-bold text-white/70 uppercase tracking-wider ml-1`; a `relative`
    div; when `icon` present render `<Icon className="absolute left-4 top-1/2
    -translate-y-1/2 text-white/50" size={18} />` and use input padding `pl-12`,
    else omit the icon and use `pl-4`. Input classes verbatim:
    `w-full bg-black/20 border border-white/10 rounded-xl py-3 pl-12 pr-4 text-white
    placeholder:text-white/20 focus:outline-none focus:ring-2 focus:ring-sky-500
    transition-all` (swap `pl-12`↔`pl-4` per icon presence). Forward `type`, `value`,
    `onChange`, `placeholder`, `required`, `minLength`. Do NOT set any inline
    font-size (global `input{font-size:16px}` must win — Pitfall 5). Keep `py-3`
    (12px) verbatim — do NOT change to `py-4`.
  </action>
  <verify>
    <automated>npm run build && grep -q "focus:ring-2 focus:ring-sky-500" src/ui/Input.tsx && grep -q "placeholder:text-white/20" src/ui/Input.tsx && grep -q "py-3" src/ui/Input.tsx && ! grep -q "font-size" src/ui/Input.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/Input.tsx` renders label + optional icon slot + input with verbatim classes.
    - `pl-12` when icon present, `pl-4` when absent; `py-3` kept; no inline font-size.
    - Focus ring `focus:ring-2 focus:ring-sky-500` and `placeholder:text-white/20` present.
    - Build clean.
  </acceptance_criteria>
  <done>Input master reproduces the AuthModal field pattern with an optional icon slot.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: src/ui/Button.tsx — variant/size/state matrix + aria-label contract</name>
  <files>src/ui/Button.tsx</files>
  <read_first>
    - src/components/AuthModal.tsx L197-204 (primary submit + loading — the visual anchor)
    - .planning/phases/01-audit-design-system/01-PATTERNS.md §`src/ui/Button.tsx` (the full variant-source table with verbatim class signatures per variant + size rungs + loading/disabled)
    - .planning/phases/01-audit-design-system/01-UI-SPEC.md §Visual Hierarchy & Accessibility (primary = sole anchor; icon-only requires aria-label)
  </read_first>
  <action>
    Create `src/ui/Button.tsx` (`export default function Button(props: ButtonProps)`).
    Build a variant→class map using the VERBATIM signatures from the PATTERNS
    variant-source table (do NOT invent styles):
      - primary  (AuthModal:200): `bg-sky-500 hover:bg-sky-400 text-white font-bold rounded-xl shadow-lg shadow-sky-500/20 transition-all active:scale-[0.98]`
      - secondary(Profile:431):   `bg-slate-200 hover:bg-slate-300 text-slate-600 font-bold rounded-2xl transition-all`
      - ghost    (AuthModal:93):  `bg-white/5 hover:bg-white/10 rounded-full transition-colors`
      - danger   (AdminDashboard:439): `bg-rose-500 hover:bg-rose-600 text-white rounded-xl font-bold transition-colors`
    Size rung (vertical padding, from existing usage): sm=`py-2`, md=`py-3`, lg=`py-4`.
    Loading: prefix `<Loader2 size={18} className="animate-spin" />` inside a
    `flex items-center justify-center gap-2` button (AuthModal:200-202). Disabled:
    `disabled={loading||disabled}` + `disabled:opacity-50 disabled:pointer-events-none`.
    Default variant `primary`, default size `lg` (matches AuthModal submit `py-4`,
    the sole visual anchor — do NOT flatten it). Accessibility contract (UI-SPEC):
    if `iconOnly` (rounded-full, no visible text), REQUIRE an `aria-label` — throw or
    console.error in dev and forward the label to the button; never render icon-only
    without one.
  </action>
  <verify>
    <automated>npm run build && grep -q "bg-sky-500 hover:bg-sky-400" src/ui/Button.tsx && grep -q "aria-label" src/ui/Button.tsx && grep -q "animate-spin" src/ui/Button.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Button exposes variant primary/secondary/ghost/danger, size sm/md/lg, loading + disabled.
    - Each variant class string matches the PATTERNS verbatim source (primary keeps sky-500 anchor).
    - Icon-only requires `aria-label` (enforced, not just typed).
    - Build clean.
  </acceptance_criteria>
  <done>Button master covers the full anticipated variant set, every style derived from existing values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational components; no data/auth/network surface (Architectural Responsibility Map: Browser/Client + Styling) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-T | Tampering | Verbatim class extraction | mitigate | Class strings copied byte-for-byte from AuthModal/PATTERNS (radius via rounded-4xl token, identical 32px); grep gates assert key strings; pixel-identity proven downstream in plan 01-06 against before-screenshots. |
| T-01-04-I | Information Disclosure | Missing aria-label on icon-only buttons | mitigate | Button enforces `aria-label` for icon-only variants (a11y contract, UI-SPEC). |
</threat_model>

<verification>
- `npm run build` (tsc -b + vite) clean for all three components.
- Grep gates confirm verbatim key class strings + rounded-4xl token wiring + aria-label enforcement.
- Components consume existing values / tokens only — no new hex/design values invented.
</verification>

<success_criteria>
Three of the five DS-02 master components (Modal, Input, Button) exist in `src/ui/`,
extracted verbatim from the app (Modal's 32px shell wired to the rounded-4xl token),
with the full variant set and the a11y contract.
</success_criteria>

<output>
Create `.planning/phases/01-audit-design-system/01-04-SUMMARY.md` when done.
</output>
</content>
