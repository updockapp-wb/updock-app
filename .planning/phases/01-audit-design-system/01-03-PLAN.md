---
phase: 01-audit-design-system
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - src/index.css
autonomous: false
requirements: [DS-01]
must_haves:
  truths:
    - "src/index.css is the single token source; dead Deep Ocean palette and dead .glass class are pruned"
    - "The @theme/:root color conflict is resolved by canonicalizing on the rendered light values"
    - "--font-sans is the actually-rendered system stack (not DM Sans / Inter)"
    - "A named radius token reconciles the off-scale rounded-[32px]; Tailwind default spacing/radius scales are untouched"
    - "The build still succeeds and every screen renders pixel-identically"
  artifacts:
    - path: "src/index.css"
      provides: "Canonicalized single token source (colors, font, radius, glass) per DS-01"
      contains: "@theme"
  key_links:
    - from: "src/index.css @theme"
      to: "Tailwind-generated utilities"
      via: "CSS-first token source, no indirection layer (D-01)"
      pattern: "--color-|--radius-|--font-"
    - from: "src/index.css input rule"
      to: "iOS focus-zoom prevention"
      via: "global input{font-size:16px} kept verbatim"
      pattern: "font-size: 16px"
---

<objective>
Canonicalize `src/index.css` into the single token source (DS-01, D-01/D-02/D-03):
prune the dead "Deep Ocean" `@theme` palette (0 utility consumers) and the dead
`.glass` class, resolve the `@theme` vs `:root` color double-declaration by keeping
the actually-rendered LIGHT values, canonicalize `--font-sans` to the rendered
system stack (DM Sans / Inter are declared-but-unloaded), add ONE named radius
token to reconcile the off-scale `rounded-[32px]`, and keep the Tailwind v4 default
spacing/radius scales + the iOS 16px input rule + glass vars untouched.

Purpose: A single, honest token source with zero dead/contradictory values — the
foundation DS-02 components render against.
Output: cleaned `src/index.css`; build green; zero visual change.

CONSTRAINT: This is a SUBTRACTION + thin semantic layer, NOT a rewrite. Never
override Tailwind's default `--spacing` or default radius vars (changes every
screen). Only ADD tokens. Every retained value must equal what already renders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-audit-design-system/01-PATTERNS.md
@.planning/phases/01-audit-design-system/01-UI-SPEC.md
@.planning/phases/01-audit-design-system/01-RESEARCH.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prune dead tokens + canonicalize colors, font, and radius token</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (current @theme L3-20 dead Deep Ocean palette; :root L22-53 rendered light values; input rule L55-58; dead .glass L87-94)
    - .planning/phases/01-audit-design-system/01-PATTERNS.md §`src/index.css` (exact PRUNE/CANONICALIZE map with the verbatim rendered values)
    - .planning/phases/01-audit-design-system/01-UI-SPEC.md §Color + §Radius & Elevation + §Spacing (rendered token values, 32px reconciliation, 16px input rule)
    - .planning/phases/01-audit-design-system/01-RESEARCH.md Pitfalls 1 & 6 + §Runtime State Inventory (0-consumer proof) + §Architectural Responsibility Map
  </read_first>
  <action>
    Edit `src/index.css`:
    1. DELETE the dead `@theme` "Deep Ocean" palette vars (`--color-background`
       #0f172a, `--color-surface`, `--color-surface-transparent`, `--color-primary`
       #38bdf8, `--color-secondary` #2dd4bf, `--color-accent` #f472b6) — grep-verified
       0 utility consumers (per D-02, RESEARCH Runtime State Inventory / Pitfall 1).
    2. CANONICALIZE `--font-sans` to the actually-rendered system stack
       (`system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica,
       Arial, sans-serif`) — NOT `'Inter'`/`'DM Sans'` (A1, Pitfall 6; user-confirmed
       system stack). Update the `:root font-family` to match the same rendered stack.
    3. In `@theme`, define the canonical SEMANTIC color tokens equal to the rendered
       light values from PATTERNS: `--color-primary: #0ea5e9` (Sky 500),
       `--color-secondary: #0f172a`, destructive/accent `#f43f5e` (Rose 500), text
       `#1e293b` / muted `#64748b`, bg `#f8fafc`. Remove the now-redundant `:root`
       color re-declarations that duplicated these (resolve the double-declaration —
       Pitfall 1). Keep `--glass-bg`/`--glass-border` verbatim.
    4. ADD exactly ONE off-scale radius token to reconcile `rounded-[32px]` (6×):
       `--radius-4xl: 2rem;` (per RESEARCH A4 — renders 32px). Do NOT add spacing or
       8/12/16/24 radius tokens — Tailwind v4 defaults already render them.
    5. DELETE the dead `.glass` class (L87-94, 0 consumers).
    6. KEEP verbatim: the global `input, textarea, select { font-size: 16px; }` rule
       (iOS anti-zoom) and the html/body/#root base styles.
    Do NOT touch any `src/ui/` or component file — this plan owns only index.css.
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -q -- "--radius-4xl" src/index.css && grep -q "font-size: 16px" src/index.css && ! grep -q "#38bdf8" src/index.css && ! grep -Eq "^\.glass" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc -b + vite build) succeeds after the prune.
    - Dead Deep Ocean vars (`#38bdf8`, `--color-surface`, etc.) and `.glass` removed.
    - `--font-sans` = system stack; no `'Inter'`/`'DM Sans'` remain as a load target.
    - Canonical `--color-primary: #0ea5e9` present; no conflicting `:root` twin.
    - `--radius-4xl: 2rem` added; NO custom `--spacing` or default-radius override added.
    - `input{font-size:16px}` rule intact.
  </acceptance_criteria>
  <done>index.css is the single, dead-code-free token source; build green; values equal the rendered app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify token prune is pixel-identical vs before-screenshots</name>
  <action>Human builds+previews and spot-checks screens against audit/screenshots/before/ to confirm the token prune produced no visible change. See <how-to-verify>.</action>
  <what-built>
    The token file was canonicalized (dead palette + `.glass` pruned, font set to
    the rendered system stack, semantic colors + one radius token added). Because
    the prune targets 0-consumer dead code and keeps rendered values, no screen
    should change — this checkpoint confirms that against the app.
  </what-built>
  <read_first>
    - .planning/phases/01-audit-design-system/audit/screenshots/before/ (the parity oracle from plan 01-01)
  </read_first>
  <how-to-verify>
    1. Run `npm run build && npm run preview`.
    2. Spot-check a few screens (auth modal, a card list, a form) against the
       `before/` screenshots — background, primary sky accent, text colors, and the
       system font must look identical.
    3. Confirm no font change (San Francisco / system sans on iOS, not a webfont).
  </how-to-verify>
  <acceptance_criteria>
    - No visible change in color, background, or typeface versus the before-state.
  </acceptance_criteria>
  <resume-signal>Type "identical" if screens are unchanged, or describe any drift.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Token/CSS edit only; no data, auth, or network surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-T | Tampering | src/index.css canonicalization | mitigate | Only 0-consumer dead code pruned (grep-verified); retained values equal rendered values; human-verify against before-screenshots catches any drift. |
| T-01-03-E | Elevation of Privilege | Accidental override of Tailwind default scale | mitigate | Explicitly forbid overriding `--spacing`/default radius vars; verify only `--radius-4xl` added. |
</threat_model>

<verification>
- `npm run lint && npm run build` green.
- Dead Deep Ocean palette + `.glass` removed; `--radius-4xl` added; system font set.
- Human-verify: screens pixel-identical to before-screenshots.
</verification>

<success_criteria>
DS-01 satisfied: `src/index.css` is the single token source (colors, typography,
spacing via defaults, radii, shadows/glass) with no dead or contradictory values,
and the app renders unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-audit-design-system/01-03-SUMMARY.md` when done.
</output>
