---
phase: 05-recette-globale-nettoyage-final
plan: 05
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - .planning/phases/05-recette-globale-nettoyage-final/05-QA-CHECKLIST.md
autonomous: false
requirements: [QA-01]

must_haves:
  truths:
    - "A merged global recette checklist exists covering carte, fiche spot, favoris, avis, session, ajout/edition spot, profil, auth — full regression merge of Phases 2/3/4 plus previously uncovered flows (D-10)"
    - "The 2 known out-of-scope bugs are listed as explicit exclusions, separate from newly found regressions (D-12)"
    - "npm run lint is green and npm run build succeeds as the automated pre-recette gate"
    - "npx cap sync has run so the native iOS + Android shells carry the new hashed lazy chunks"
    - "The manual recette passes 100% on real iOS and Android devices with zero functional regression (D-11)"
    - "The recette runs as a single final pass after CODE-01/02 and PERF-03 are all complete, not an intermediate pass after the bundle work alone (D-13)"
  artifacts:
    - path: ".planning/phases/05-recette-globale-nettoyage-final/05-QA-CHECKLIST.md"
      provides: "the merged global regression checklist + known exclusions"
      contains: "PASS/FAIL"
  key_links:
    - from: "05-QA-CHECKLIST.md"
      to: "Phases 2/3/4 QA checklists"
      via: "merged coverage of all critical flows"
      pattern: "carte|spot|favori|session|auth"
---

<objective>
Close the milestone with the global non-regression recette (QA-01/D-10/D-11/D-13). Build the merged checklist (Phases 2/3/4 flows + avis/session/auth + the 2 D-12 known exclusions), run the automated gate (lint + build + cap sync), then execute a single final manual pass on real iOS and Android devices.

Purpose: This is the closing gate of the entire v2.0 refactor. The cleanup (05-01/02/03), state homogenization, and code-splitting (05-04) touch transverse files with cross-platform risk (dynamic import in the Capacitor WebView — Pitfall 1), so the recette runs on BOTH platforms in one final pass after all other work is done (D-13).
Output: the 05-QA-CHECKLIST.md artifact and a 100%-PASS device recette confirming zero regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recette-globale-nettoyage-final/05-RESEARCH.md
@.planning/phases/04-formulaires-interactions/04-QA-CHECKLIST.md
@.planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md
@.planning/todos/push-notif-no-popup-iphone.md
@.planning/todos/country-list-incomplete-other-emoji.md

<interfaces>
Flow surface to cover (from src/components + App.tsx tabs): Map, SpotDetail, Profile, AuthModal, AddSpotForm, ReviewForm/ReviewList (avis), SessionForm/SessionList/SessionCard (session), AdminDashboard, FiltersModal, SearchModal; App.tsx tabs map/favorites/list/profile.

D-12 known exclusions (list as KNOWN, not regressions):
- Push notifications session not received on iPhone (.planning/todos/push-notif-no-popup-iphone.md) - out of milestone.
- Incomplete country list in CommunityStatsScreen (.planning/todos/country-list-incomplete-other-emoji.md) - backlog data bug.

Special attention items this phase introduces (verify on-device, both platforms):
- Lazy chunk-load resilience: cold-launch the app, open the map tab, open admin (if admin) and premium flows — no blank screen / no "Failed to fetch dynamically imported module" (Pitfall 1 / Error Boundary).
- set-state-in-effect fixes: verify logout resets profile/token/notifications state correctly (auth flow).
- deleteSpot now confirms via app Modal (not native confirm) — verify the delete confirmation still gates the destructive action.
- AuthModal signup message now a Toast (not native alert).

cap sync note: run `npm run build && npx cap sync` before device testing so native public/ carries the new hashed chunks. (Env caveat from STATE.md: cap sync may require Node >=22; current env v20 — if cap sync is blocked, surface it in the checkpoint for the user to run.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the merged global recette checklist (D-10/D-12)</name>
  <files>.planning/phases/05-recette-globale-nettoyage-final/05-QA-CHECKLIST.md (new)</files>
  <read_first>
    - .planning/phases/04-formulaires-interactions/04-QA-CHECKLIST.md (structure + formulaires/favoris scenarios)
    - .planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md (fiche spot + profil scenarios)
    - .planning/todos/push-notif-no-popup-iphone.md and .planning/todos/country-list-incomplete-other-emoji.md (D-12 exclusions)
    - .planning/phases/05-recette-globale-nettoyage-final/05-VALIDATION.md (Wave 0 requirement + manual-only verifications)
  </read_first>
  <action>
    Create 05-QA-CHECKLIST.md as the full global regression recette. Include a scenario table with a PASS/FAIL column covering EVERY critical flow: carte (markers, filters, search, offline preview), fiche spot (view, favori, edition inline), favoris (add/remove from list + detail, offline revert), avis (ReviewForm add + ReviewList display), session (SessionForm create, SessionList/SessionCard display + join), ajout/edition spot (AddSpotForm validation + no-photo confirm, admin edit), profil (view, signOut, premium modal), auth (login, signup, logout reset). Add a dedicated "Phase-5-specific verifications" section for: lazy chunk-load resilience on cold launch (both platforms), logout state reset (set-state-in-effect fixes), deleteSpot app-Modal confirmation, AuthModal Toast. Add a clearly separated "Known exclusions (NOT regressions)" section listing the 2 D-12 bugs. Mark the recette as requiring both iOS and Android real devices (D-11), single final pass (D-13). Reuse the wording/scenarios from the Phase 3/4 checklists where they overlap; do not invent new UX requirements.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-recette-globale-nettoyage-final/05-QA-CHECKLIST.md; grep -ci "exclusion" .planning/phases/05-recette-globale-nettoyage-final/05-QA-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - 05-QA-CHECKLIST.md exists with a PASS/FAIL scenario table covering all 8 flow groups (carte, fiche spot, favoris, avis, session, ajout/edition spot, profil, auth)
    - It contains a "Phase-5-specific verifications" section (lazy chunk-load, logout reset, deleteSpot Modal confirm, AuthModal Toast)
    - It contains a separate "Known exclusions" section naming both D-12 bugs
    - It states iOS + Android real-device requirement and single-final-pass (D-13)
  </acceptance_criteria>
  <done>The merged global recette checklist artifact exists and is ready to drive the device pass.</done>
</task>

<task type="auto">
  <name>Task 2: Automated pre-recette gate (lint + build + cap sync)</name>
  <files>(no source changes; runs gates and cap sync)</files>
  <read_first>
    - .planning/STATE.md (Deferred Items: cap sync may need Node >=22)
    - .planning/phases/05-recette-globale-nettoyage-final/05-VALIDATION.md (Sampling Rate: phase gate = lint green + build green)
  </read_first>
  <action>
    Run `npm run lint` (must be 0 problems — the D-01 green gate), then `npm run build` (must succeed), then `npx cap sync` so the native iOS and Android shells pick up the new hashed lazy chunks before device testing (Pitfall 1: stale chunks blank the screen). If `npx cap sync` fails due to the Node >=22 requirement (current env v20, per STATE.md Deferred Items), do not fake it — record the exact error and surface it in Task 3's checkpoint so the user runs cap sync in a Node 22 environment before testing. Capture the lint/build/cap sync results for the SUMMARY.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -2; npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - npm run lint exits 0 with no problems
    - npm run build completes with no errors
    - npx cap sync either completes for both platforms, or its blocking error (e.g. Node version) is captured verbatim for the checkpoint
  </acceptance_criteria>
  <done>Lint green, build green, native shells synced (or the sync blocker surfaced for the user).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual global recette on iOS + Android (D-10/D-11/D-13)</name>
  <what-built>
    Full v2.0 refactor is code-complete: 7 contexts split + lint green (05-01), dead code/deps cleaned (05-02), error handling harmonized + cacheSpotImages parallelized (05-03), Map/AdminDashboard/PremiumModal lazy-loaded behind Suspense + Error Boundary with eager gzip JS under the -15% target (05-04). The merged recette checklist (05-QA-CHECKLIST.md) and the automated gate (lint + build + cap sync) are done.
  </what-built>
  <how-to-verify>
    1. Ensure the native shells are synced: if Task 2 reported a cap sync blocker (Node >=22), run `npm run build && npx cap sync` in a Node 22 environment first.
    2. On a real iOS device AND a real Android device, cold-launch the app and execute every scenario in 05-QA-CHECKLIST.md, marking PASS/FAIL.
    3. Pay special attention to the Phase-5-specific verifications: (a) map area shows the skeleton then loads with no blank screen or "Failed to fetch dynamically imported module"; (b) opening admin (if admin) and premium flows loads without error; (c) logout correctly resets profile/notifications state; (d) deleting a spot shows the app Modal confirm (not a native dialog) and still requires confirmation; (e) the signup message appears as a Toast, not a native alert.
    4. Log the 2 D-12 known bugs as KNOWN exclusions (not regressions) if encountered.
    5. Any newly found regression -> stop and request gap closure (`/gsd:plan-phase 05 --gaps`), do not arbitrate.
  </how-to-verify>
  <resume-signal>Type "approved" if the recette passes 100% on both platforms (known D-12 exclusions aside), or describe each failing scenario.</resume-signal>
  <action>Execute every scenario in 05-QA-CHECKLIST.md on a real iOS device AND a real Android device, marking PASS/FAIL per the how-to-verify steps above. Log the 2 D-12 bugs as known exclusions, not regressions. On any newly found regression, stop and request gap closure (/gsd:plan-phase 05 --gaps).</action>
  <verify>
    <human-check>All 05-QA-CHECKLIST.md scenarios marked PASS on both iOS and Android (D-12 exclusions aside), and the Phase-5-specific verifications (lazy chunk-load, logout reset, deleteSpot Modal confirm, AuthModal Toast) confirmed on-device.</human-check>
  </verify>
  <done>Manual global recette 100% PASS on real iOS + Android; zero functional regression; D-12 bugs logged as known exclusions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device WebView -> app | Final human validation that no refactor regressed input validation, access control, or error handling across platforms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | Phase-4 form validation must not regress | mitigate | Recette re-verifies AddSpotForm / edition validation (ROBUST-01) on-device; any regression triggers gap closure. |
| T-05-10 | Elevation | admin gating must survive lazy mounting | mitigate | Recette confirms non-admins cannot open AdminDashboard and the role check still gates the mount; server RLS remains the real boundary. |
| T-05-11 | DoS | lazy chunk-load failure on real devices | mitigate | On-device cold-launch of map + admin + premium flows on both platforms confirms the Error Boundary recovers (no blank screen). |
</threat_model>

<verification>
- 05-QA-CHECKLIST.md exists and is complete
- npm run lint green, npm run build green, cap sync done (or blocker surfaced)
- Manual recette 100% PASS on iOS + Android; D-12 bugs logged as known exclusions
</verification>

<success_criteria>
The merged checklist passes 100% on real iOS and Android devices with zero functional regression across the entire milestone, and the 2 known out-of-scope bugs are logged as exclusions. QA-01 is satisfied and the v2.0 refactor closes.
</success_criteria>

<output>
Create `.planning/phases/05-recette-globale-nettoyage-final/05-05-SUMMARY.md` when done
</output>
