---
phase: 05-recette-globale-nettoyage-final
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-03]
files_modified:
  - src/App.tsx
  - src/components/Map.tsx
  - src/components/Profile.tsx
  - src/components/ErrorBoundary.tsx
  - src/main.tsx
  - vite.config.ts
autonomous: true
requirements: [PERF-03]

must_haves:
  truths:
    - "Map is loaded via React.lazy behind a Suspense fallback, not a static import — code-splitting is the primary bundle strategy (D-06)"
    - "The map area shows a skeleton/spinner while the Mapbox chunk loads, with no eager preload at login (D-07)"
    - "AdminDashboard is only mounted (and its chunk fetched) when isAdminOpen is true — code-splitting extended beyond Mapbox to non-critical screens (D-08)"
    - "PremiumModal is loaded via React.lazy at its Profile.tsx call site (D-08)"
    - "Lazy screens are wrapped in an Error Boundary so a failed dynamic import does not blank the subtree"
    - "The eager/initial gzip JS (loaded before the map renders) is reduced vs the 504.17 kB Phase-1 baseline and beats the -15% target"
    - "The bundle measurement is written to a Phase-5 audit folder without overwriting the Phase-1 baseline artifacts, reproducing the exact Phase-1 methodology (D-09)"
  artifacts:
    - path: "src/components/ErrorBoundary.tsx"
      provides: "class Error Boundary with reload affordance for lazy chunk-load failures"
      contains: "componentDidCatch"
    - path: ".planning/phases/05-recette-globale-nettoyage-final/audit/build-size.txt"
      provides: "Phase-5 per-chunk gzip table"
    - path: ".planning/phases/05-recette-globale-nettoyage-final/audit/stats.html"
      provides: "Phase-5 rollup-plugin-visualizer treemap"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/Map.tsx"
      via: "React.lazy dynamic import"
      pattern: "lazy\\(\\(\\) => import\\(.*Map"
    - from: "src/App.tsx"
      to: "src/components/AdminDashboard.tsx"
      via: "gated lazy mount"
      pattern: "isAdminOpen &&"
---

<objective>
Reduce the bundle (PERF-03/D-06/D-07/D-08) by converting the three large conditionally-needed surfaces to React.lazy, guarding them with an Error Boundary (Pitfall 1: a rejected dynamic import in the Capacitor WebView blanks the screen), and measuring the result into a Phase-5 audit folder. The operative PERF-03 metric is eager/initial gzip JS (JS parsed before the map renders), resolved by the user; the summed-total gzip JS is reported alongside for continuity but is NOT the pass/fail gate.

Purpose: Mapbox GL is 54.5% of gzip JS and currently eager. Lazy-loading it (plus AdminDashboard and PremiumModal) moves ~274 kB off the initial parse path, driving eager gzip JS from ~504 kB to an expected ~230 kB — comfortably under the -15% target (~428.5 kB).
Output: lazy Map/AdminDashboard/PremiumModal behind Suspense + Error Boundary, a Phase-5 audit folder with build-size.txt + stats.html, and a two-number report (eager/initial = PASS/FAIL vs baseline; summed-total for continuity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recette-globale-nettoyage-final/05-PATTERNS.md
@.planning/phases/05-recette-globale-nettoyage-final/05-RESEARCH.md
@.planning/phases/01-audit-design-system/01-AUDIT.md

<interfaces>
Verified integration points (2026-07-31):
- App.tsx:8 `import Map from './components/Map'` (default export MapComponent at Map.tsx:107). Render site App.tsx ~134-142: {activeTab === 'map' && (<motion.div key="map">...<Map onSpotClick selectedSpot isAddingSpotMode onSetAddingSpotMode/>...)}.
- App.tsx:27 `import AdminDashboard from './components/AdminDashboard'` (default export). Currently mounted UNCONDITIONALLY at ~259-263 with isOpen={isAdminOpen}; state isAdminOpen at App.tsx:42.
- Profile.tsx:10 `import PremiumModal from './PremiumModal'` (default export). Rendered Profile.tsx:441 <PremiumModal isOpen={isPremiumOpen} onClose=.../>; state isPremiumOpen at Profile.tsx:29.
- main.tsx:4 `import 'mapbox-gl/dist/mapbox-gl.css'` (eager CSS side-effect; optional to move into Map.tsx).
- vite.config.ts:19 visualizer filename hardcoded to '.planning/phases/01-audit-design-system/audit/stats.html' (must NOT be clobbered). ANALYZE env already gates the visualizer.
- MapSkeleton fallback: reuse the auth-loading spinner classes from App.tsx:98 "w-8 h-8 border-4 border-sky-500 border-t-transparent rounded-full animate-spin", confined to the map area so nav stays interactive (D-07).

Baseline (01-AUDIT.md §2, frozen): summed-total gzip JS = 504.17 kB (mapbox-gl-*.js 274.66 kB / 54.5%, index-*.js 214.11 kB / 42.5%, web-*.js + CSS 13.60 kB). Target -15% = ~428.5 kB. Measurement commands: `npm run build` (per-chunk raw|gzip table) and `ANALYZE=1 npm run build` (stats.html treemap).

No React.lazy/Suspense/Error Boundary exists anywhere in the repo yet (all NEW patterns; source = RESEARCH.md Patterns 1-2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Error Boundary + lazy-load Map behind Suspense (D-06/D-07)</name>
  <files>src/components/ErrorBoundary.tsx (new), src/App.tsx, src/components/Map.tsx, src/main.tsx</files>
  <read_first>
    - src/App.tsx (imports, Map render site ~134-142, spinner classes at :98)
    - src/components/Map.tsx (default export signature; the 4 props passed)
    - src/main.tsx (mapbox CSS import at :4)
    - .planning/phases/05-recette-globale-nettoyage-final/05-PATTERNS.md (App.tsx React.lazy section, MapSkeleton)
    - .planning/phases/05-recette-globale-nettoyage-final/05-RESEARCH.md (Pattern 1, Pitfall 1)
  </read_first>
  <action>
    (1) Create src/components/ErrorBoundary.tsx: a class component with static getDerivedStateFromError + componentDidCatch (console.error the error). Props: { children, fallback } where fallback is a ReactNode or a render function receiving a retry callback. Default fallback for the map = a full-area div showing a short translated message and a "reload" button that resets the boundary state (setState hasError:false) to retry the lazy import. This addresses Pitfall 1 (a rejected dynamic import in the Capacitor WebView blanks the subtree).
    (2) In App.tsx: replace `import Map from './components/Map'` with `const Map = lazy(() => import('./components/Map'))` (add lazy, Suspense to the react import). Wrap the existing <Map .../> render inside <ErrorBoundary><Suspense fallback={<MapSkeleton/>}>...</Suspense></ErrorBoundary>, keeping the surrounding motion.div and all 4 props (onSpotClick, selectedSpot, isAddingSpotMode, onSetAddingSpotMode) unchanged. Define MapSkeleton inline (or a small local component) reusing the App.tsx:98 spinner classes, confined to the map area.
    (3) Optionally move `import 'mapbox-gl/dist/mapbox-gl.css'` from main.tsx:4 into Map.tsx so the CSS defers with the lazy chunk (does not affect the JS metric; keep if it risks a visual flash).
    Do not change Map.tsx's props or behavior — only its import boundary.
  </action>
  <verify>
    <automated>grep -c "lazy(() => import('./components/Map'" src/App.tsx; test -f src/components/ErrorBoundary.tsx; npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ErrorBoundary.tsx exists with getDerivedStateFromError and componentDidCatch
    - App.tsx imports Map via lazy(() => import('./components/Map')) and no longer statically imports it
    - The Map render is wrapped in both ErrorBoundary and Suspense with a map-area skeleton fallback
    - npm run build produces a separate mapbox chunk and completes with no errors
  </acceptance_criteria>
  <done>Map is lazy behind Suspense + Error Boundary with a skeleton; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Gated lazy-mount AdminDashboard and PremiumModal (D-08)</name>
  <files>src/App.tsx, src/components/Profile.tsx</files>
  <read_first>
    - src/App.tsx (AdminDashboard import :27, mount ~259-263, isAdminOpen :42)
    - src/components/Profile.tsx (PremiumModal import :10, render :441, isPremiumOpen :29)
    - src/components/AdminDashboard.tsx (top-level effects/subscriptions — confirm nothing runs at mount that gated mounting would lose, per Q3)
    - .planning/phases/05-recette-globale-nettoyage-final/05-RESEARCH.md (Pattern 2, Q3)
  </read_first>
  <action>
    (1) App.tsx: replace `import AdminDashboard from './components/AdminDashboard'` with `const AdminDashboard = lazy(() => import('./components/AdminDashboard'))`. Change the unconditional mount to a gated lazy mount: {isAdminOpen && (<Suspense fallback={null}><AdminDashboard isOpen onClose={() => setIsAdminOpen(false)} onSpotSelect={handleSpotSelect} /></Suspense>)} — this avoids fetching the 490-line chunk for non-admins entirely. Before switching to gated mount, confirm AdminDashboard has no mount-time side-effect (subscription/fetch) that must run before it is opened; if it does, keep it mounted and wrap in Suspense instead (fallback={null}). The admin role check that gates opening must still fire before the mount (access control unchanged — server RLS remains the real boundary).
    (2) Profile.tsx: replace `import PremiumModal from './PremiumModal'` with `const PremiumModal = lazy(() => import('./PremiumModal'))`. Gate the render: {isPremiumOpen && (<Suspense fallback={null}><PremiumModal isOpen onClose={() => setIsPremiumOpen(false)} /></Suspense>)}. Add lazy/Suspense to Profile.tsx's react import.
    fallback = null for both (user-tap triggers, not first paint).
  </action>
  <verify>
    <automated>grep -c "lazy(() => import('./components/AdminDashboard'" src/App.tsx; grep -c "lazy(() => import('./PremiumModal'" src/components/Profile.tsx; npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - AdminDashboard and PremiumModal are both imported via lazy(() => import(...)) and no longer statically imported
    - AdminDashboard renders only under {isAdminOpen && ...} (gated) or, if a mount-time side-effect was found, is documented as kept-mounted with a reason
    - PremiumModal renders only under {isPremiumOpen && ...}
    - npm run build emits separate chunks for AdminDashboard and PremiumModal and completes with no errors
  </acceptance_criteria>
  <done>AdminDashboard and PremiumModal lazy-loaded (admin gated so non-admins never fetch it); build green.</done>
</task>

<task type="auto">
  <name>Task 3: Measure bundle into a Phase-5 audit folder and report eager vs total gzip JS (D-09)</name>
  <files>vite.config.ts, .planning/phases/05-recette-globale-nettoyage-final/audit/build-size.txt (new), .planning/phases/05-recette-globale-nettoyage-final/audit/stats.html (new)</files>
  <read_first>
    - vite.config.ts (visualizer filename at :19 hardcoded to the Phase-1 path)
    - .planning/phases/01-audit-design-system/01-AUDIT.md (§2 baseline definition + composition)
    - audit/build-size.txt (Phase-1 methodology to reproduce)
  </read_first>
  <action>
    (1) Parametrize the visualizer output so it does NOT clobber the Phase-1 baseline: change vite.config.ts:19 filename to '.planning/phases/05-recette-globale-nettoyage-final/audit/stats.html' (or make it env-driven, e.g. process.env.ANALYZE_OUT || the Phase-5 path). Create the .planning/phases/05-recette-globale-nettoyage-final/audit/ folder.
    (2) Run `npm run build` and capture the per-chunk raw|gzip table into .planning/phases/05-recette-globale-nettoyage-final/audit/build-size.txt. Run `ANALYZE=1 npm run build` to write stats.html into the Phase-5 audit folder.
    (3) Compute and report TWO numbers in the SUMMARY: (a) eager/initial gzip JS = sum of the .js chunks loaded before the map renders (exclude the now-lazy mapbox, AdminDashboard, PremiumModal chunks) — this is the PASS/FAIL metric vs the -15% target (~428.5 kB from the 504.17 kB baseline); (b) summed-total gzip JS = sum of ALL .js chunk gzip sizes, for continuity with the Phase-1 baseline (NOT the gate). State clearly whether eager/initial gzip JS is under the target (PASS).
    Do not overwrite audit/build-size.txt or audit/stats.html at the Phase-1 path.
  </action>
  <verify>
    <automated>ANALYZE=1 npm run build 2>&1 | tail -5; test -f .planning/phases/05-recette-globale-nettoyage-final/audit/stats.html && test -f .planning/phases/05-recette-globale-nettoyage-final/audit/build-size.txt</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/05-recette-globale-nettoyage-final/audit/build-size.txt and stats.html both exist
    - The Phase-1 artifacts audit/build-size.txt and audit/stats.html are byte-unchanged (not clobbered)
    - The SUMMARY reports both eager/initial gzip JS (with PASS/FAIL vs ~428.5 kB) and summed-total gzip JS
    - eager/initial gzip JS is below the -15% target (expected ~230 kB); if not, the SUMMARY flags it explicitly
  </acceptance_criteria>
  <done>Phase-5 audit artifacts produced without clobbering the baseline; eager/initial gzip JS measured and shown to pass the -15% target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin -> admin bundle | Lazy-loading admin UI could expose the admin chunk to non-admins if the mount is not gated. |
| Capacitor WebView -> local chunk FS | Dynamic import() fetches chunks from the app filesystem; a failed import can blank the screen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Info disclosure | AdminDashboard lazy chunk | mitigate | Gate the mount ({isAdminOpen && ...}) so non-admins never fetch the chunk; the admin role check still fires before opening. Chunk contents are client code (not secret); server RLS remains the real access control. |
| T-05-07 | DoS | lazy chunk-load failure in Capacitor WebView | mitigate | Wrap lazy screens in an Error Boundary with a reload affordance (Pitfall 1); on-device chunk-load resilience verified in the 05-05 recette (D-11). |
| T-05-08 | Tampering | overwriting the frozen Phase-1 baseline artifact | mitigate | Redirect the visualizer output to a Phase-5 audit folder; verify Phase-1 artifacts are byte-unchanged. |
</threat_model>

<verification>
- Map, AdminDashboard, PremiumModal all lazy; AdminDashboard gated
- ErrorBoundary wraps the map lazy screen
- Phase-5 audit folder has build-size.txt + stats.html; Phase-1 artifacts untouched
- eager/initial gzip JS under the -15% target; summed-total reported
- npm run build green
</verification>

<success_criteria>
The three large surfaces load lazily behind Suspense + Error Boundary, and eager/initial gzip JS is measured below the -15% target with the summed-total reported for continuity. PERF-03 is satisfied on the user-resolved operative metric.
</success_criteria>

<output>
Create `.planning/phases/05-recette-globale-nettoyage-final/05-04-SUMMARY.md` when done
</output>
