---
phase: 05-recette-globale-nettoyage-final
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - test-fcm.mjs
autonomous: true
requirements: [CODE-01]

must_haves:
  truths:
    - "test-fcm.mjs no longer exists in the repo root"
    - "geojson is listed in package.json devDependencies"
    - "npx knip reports no actionable unused dependency (every remaining flag has a documented keep-reason) — unused-only removal, no version bumps (D-03)"
    - "npm run build still succeeds after dependency changes"
    - "@capacitor/android, firebase, and the supabase edge functions are still present — Android build required for the recette (D-11)"
  artifacts:
    - path: "package.json"
      provides: "geojson devDependency added; no required package removed"
      contains: "geojson"
  key_links:
    - from: "src/components/Map.tsx"
      to: "geojson"
      via: "type-only import now declared in package.json"
      pattern: "geojson"
---

<objective>
Close the dead-code / unused-dependency half of CODE-01: delete the confirmed dead scratch file, resolve the `geojson` unlisted-dependency warning, and produce a documented disposition for every remaining knip flag (keep vs remove with reason). Net web-bundle impact is ~0 kB by design — the deliverable is a clean, justified dependency graph, not a size win.

Purpose: CODE-01 requires dead code and unused deps to be "identified THEN removed". Several knip flags are false positives (native platforms, dev tools, server-side edge functions) that MUST be kept; the value here is the audited decision, not blind deletion.
Output: `test-fcm.mjs` deleted, `geojson` added to devDependencies, and a disposition note for each knip flag captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recette-globale-nettoyage-final/05-RESEARCH.md

<interfaces>
<!-- knip inventory verified 2026-07-31 (RESEARCH.md Dead Code Inventory). -->

knip flags and their REQUIRED dispositions:
  Unused files (3):
    supabase/functions/notify-session-created/index.ts   → KEEP (deployed server-side, outside client graph)
    supabase/functions/send-session-reminders/index.ts   → KEEP (deployed server-side)
    test-fcm.mjs                                          → DELETE (root scratch FCM test script)
  Unused dependency (1):
    @capacitor/android   → KEEP (D-11 requires a real Android build/test)
  Unused devDependencies (3):
    @capacitor/assets    → verify (asset-generation CLI, run manually)
    autoprefixer         → verify (postcss.config.js uses only '@tailwindcss/postcss'; Tailwind v4 autoprefixes internally)
    depcheck             → KEEP (self-flags; invoked via npx)
  Unlisted dependency (1):
    geojson  → ADD to devDependencies (imported type-only by Map.tsx:12; already transitive via @types/mapbox-gl)

Do NOT remove `firebase` (^12.11.0) — knip did NOT flag it; it is a required peer of @capacitor-firebase/messaging (native push). depcheck's extra flags (@capacitor/ios, tailwindcss, postcss, @tailwindcss/postcss, knip) are all false positives — ignore depcheck for removal decisions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead scratch file and add the geojson devDependency</name>
  <files>test-fcm.mjs, package.json</files>
  <read_first>
    - package.json (current dependency/devDependency lists)
    - src/components/Map.tsx (line 12 — the geojson import that triggers the unlisted-dependency warning)
    - .planning/phases/05-recette-globale-nettoyage-final/05-RESEARCH.md (Package Legitimacy Audit — geojson approved as explicit devDependency)
  </read_first>
  <action>
    Delete the root-level `test-fcm.mjs` (dead manual FCM test script, outside the app graph — safe per knip). Then add `geojson` to `devDependencies` in package.json, pinning the version already resolved transitively (run `npm ls geojson` to read the resolved version and pin that exact major, e.g. `"geojson": "^0.5.0"` — use the version npm reports). This resolves knip's "unlisted dependency" flag for the type-only import in Map.tsx:12. Do not add it to runtime `dependencies` (it is type-only). Run `npm install` to sync the lockfile.
  </action>
  <verify>
    <automated>test ! -f test-fcm.mjs && grep -q '"geojson"' package.json && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test-fcm.mjs` is false (file removed)
    - `package.json` contains a `geojson` entry under `devDependencies`
    - `npm ls geojson` resolves without an "extraneous" or "missing" error
    - `npm run build` completes with no errors
  </acceptance_criteria>
  <done>test-fcm.mjs deleted, geojson declared in devDependencies, lockfile synced, build green.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and document disposition of every remaining knip flag</name>
  <files>package.json (only if a devDependency is verified genuinely unused), SUMMARY (disposition table)</files>
  <read_first>
    - package.json (devDependencies)
    - postcss.config.js (confirms only '@tailwindcss/postcss' is wired — autoprefixer not referenced there)
    - .planning/phases/05-recette-globale-nettoyage-final/05-RESEARCH.md (Dead Code Inventory + Alternatives Considered — knip authoritative, depcheck noisy)
  </read_first>
  <action>
    Re-run `npx knip` for a fresh inventory. For EACH remaining flag, record a disposition (KEEP + reason, or REMOVE + evidence) in the plan SUMMARY:
    - Edge functions (`notify-session-created`, `send-session-reminders`): KEEP — deployed server-side, outside the Vite client graph; deleting them removes live functions.
    - `@capacitor/android`: KEEP — D-11 mandates a real Android build/test.
    - `firebase`: KEEP — required peer of @capacitor-firebase/messaging (native push); verify via `npm ls firebase` / the plugin's peerDependencies before concluding.
    - `depcheck`: KEEP — invoked via npx (self-flag).
    - `@capacitor/assets`: verify whether any npm script or asset-generation step references it; KEEP unless zero references found.
    - `autoprefixer`: grep the repo for any `autoprefixer` reference (postcss.config.js uses only '@tailwindcss/postcss'). ONLY if there is zero reference anywhere (config, scripts, imports) AND `npm run build` stays green after removal, remove it from devDependencies; otherwise KEEP and document why.
    Removal is conservative: remove a package only with positive evidence it is unreferenced and a green build afterward. Never remove @capacitor/android, firebase, depcheck, or any edge function.
  </action>
  <verify>
    <automated>npx knip 2>&1 | tail -15 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a disposition line (KEEP+reason / REMOVE+evidence) for every knip flag
    - `@capacitor/android`, `firebase`, `depcheck`, and both supabase edge-function files still exist / are still listed
    - If any devDependency was removed, `npm run build` is still green and the removal has a documented zero-reference grep as evidence
    - `npx knip` shows no remaining unlisted-dependency flag for `geojson`
  </acceptance_criteria>
  <done>Every knip flag has a documented disposition; only verified-unused packages (if any) removed; required packages preserved; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/dependency graph | Removing a package that is a native/peer dependency would break a platform build at runtime, not compile time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | dependency removal (native/peer deps) | mitigate | Conservative removal policy: knip authoritative, depcheck ignored for removal; @capacitor/android + firebase + edge functions explicitly protected; every removal requires zero-reference evidence + green build. |
| T-05-03 | DoS | deleting a live server-side edge function | mitigate | Edge functions are flagged only because they are outside the client graph; explicitly KEEP — never delete supabase/functions/*. |
</threat_model>

<verification>
- `test-fcm.mjs` gone; `geojson` in devDependencies; lockfile synced
- `npx knip` reviewed, every flag dispositioned
- `npm run build` green
</verification>

<success_criteria>
The dependency graph is clean and every knip flag has a justified decision. CODE-01's "identified then removed" is satisfied: genuine dead code removed, false positives documented as intentional keeps.
</success_criteria>

<output>
Create `.planning/phases/05-recette-globale-nettoyage-final/05-02-SUMMARY.md` when done
</output>
