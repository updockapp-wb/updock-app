---
phase: 05-recette-globale-nettoyage-final
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/context/SpotsContext.tsx
  - src/context/AuthContext.tsx
  - src/components/AuthModal.tsx
  - src/components/Profile.tsx
  - src/components/AdminDashboard.tsx
  - src/components/SpotDetail.tsx
  - src/components/AddSpotForm.tsx
  - src/utils/offline.ts
  - src/context/FavoritesContext.tsx
autonomous: true
requirements: [CODE-02, CODE-01]

must_haves:
  truths:
    - "SpotsContext addSpot/approveSpot/deleteSpot rethrow to the caller instead of calling Toast.show with hardcoded French"
    - "SpotsContext.deleteSpot no longer calls native confirm(); confirmation happens via the app Modal at the call site"
    - "AuthContext.signOut has error handling and rethrows instead of swallowing errors"
    - "AuthModal no longer calls native alert(); it shows a Capacitor Toast"
    - "cacheSpotImages caches images in parallel (Promise.all) instead of a serial for-await loop"
    - "npm run lint stays green and npm run build succeeds"
  artifacts:
    - path: "src/context/SpotsContext.tsx"
      provides: "harmonized rethrow error pattern for all mutations"
      contains: "throw"
    - path: "src/utils/offline.ts"
      provides: "parallelized cacheSpotImages"
      contains: "Promise.all"
  key_links:
    - from: "src/context/SpotsContext.tsx mutations"
      to: "calling components"
      via: "rethrow then caller shows translated Toast"
      pattern: "throw"
    - from: "src/components/AuthModal.tsx"
      to: "@capacitor/toast"
      via: "Toast.show replaces alert()"
      pattern: "Toast"
---

<objective>
Finish state homogenization (CODE-02/D-05) by aligning the 3 verified error-handling divergences to the canonical rethrow-to-caller pattern, and fix the cacheSpotImages serialization bug (CODE-01/D-02). ProfileContext and SessionsContext are already aligned — no work there.

Purpose: D-05's real scope is exactly 3 divergences plus the D-02 perf fix. Harmonizing routes every context's error feedback through the same rethrow then component-Toast boundary (Pitfall 4: contexts have no i18n access).
Output: SpotsContext mutations rethrow plus app-Modal confirm, AuthContext.signOut rethrows, AuthModal uses Toast, cacheSpotImages runs in parallel batches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recette-globale-nettoyage-final/05-PATTERNS.md
@.planning/phases/05-recette-globale-nettoyage-final/05-RESEARCH.md

<interfaces>
Canonical rethrow pattern reference: FavoritesContext.tsx:95-106 (revert optimistic update, console.error, then throw err; caller shows translated Toast). SpotsContext.updateSpot:266-287 is a second in-file aligned example.

Divergences to fix (verified 2026-07-31):
- SpotsContext.addSpot: hardcoded French Toast.show at :128, :133-136, :205-208, :212-215 -> rethrow
- SpotsContext.approveSpot: hardcoded Toast at :230 -> rethrow
- SpotsContext.deleteSpot: native confirm('Delete this spot?') at :235 + hardcoded Toast at :262 -> app Modal confirm at call site + rethrow
- AuthContext.signOut (:37-39): await supabase.auth.signOut() with no try/catch -> try/catch + console.error + throw
- AuthModal.tsx:73: alert(t('auth.alert_signup')) -> Toast.show({ text: t('auth.alert_signup'), duration: 'short' })

Caller pattern to mirror (App.tsx:183): toggleFavorite(spot.id).catch(() => Toast.show({ text: t('...'), duration: 'short' })). Call sites: AddSpotForm (addSpot), AdminDashboard + SpotDetail (approveSpot/deleteSpot), Profile.tsx (signOut).

App Modal shell for delete confirm: src/ui/Modal.tsx default export Modal({ isOpen, onClose, children, surface, layout }); reuse the light+center confirm style from AddSpotForm's Phase-4 no-photo confirmation.
Toast import: import { Toast } from '@capacitor/toast' (already in SpotsContext.tsx:5).
cacheSpotImages (src/utils/offline.ts:7-31): serial for-of awaiting cache.match+fetch+cache.put per URL. Caller FavoritesContext.tsx:47 favSpots.forEach(s => cacheSpotImages(s.image_urls)). CACHE_NAME already defined.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harmonize SpotsContext + AuthContext + AuthModal to the rethrow/Toast/Modal canon</name>
  <files>src/context/SpotsContext.tsx, src/context/AuthContext.tsx, src/components/AuthModal.tsx, src/components/Profile.tsx, src/components/AdminDashboard.tsx, src/components/SpotDetail.tsx, src/components/AddSpotForm.tsx</files>
  <read_first>
    - src/context/FavoritesContext.tsx (lines 95-106 canonical rethrow reference)
    - src/context/SpotsContext.tsx (updateSpot:266-287 aligned example; addSpot/approveSpot/deleteSpot divergences)
    - src/ui/Modal.tsx (master modal shell for the delete confirm)
    - src/components/AuthModal.tsx (line 73 alert; already has t via useLanguage)
    - src/App.tsx (line 183 caller Toast pattern)
    - src/components/AdminDashboard.tsx and src/components/SpotDetail.tsx (approveSpot/deleteSpot call sites)
  </read_first>
  <action>
    (1) SpotsContext.addSpot / approveSpot: remove the direct hardcoded-French Toast.show calls (:128, :133-136, :205-208, :212-215, :230). On error paths follow the canon: console.error, revert optimistic state, throw err. Drop hardcoded French success Toasts; if success feedback is wanted the calling component shows it via t(). Update call sites (AddSpotForm for addSpot, AdminDashboard for approveSpot) to attach .catch(() => Toast.show({ text: t('<existing i18n key>'), duration: 'short' })), reusing existing translation keys (no new UX copy).
    (2) SpotsContext.deleteSpot: remove native confirm('Delete this spot?') at :235 and the hardcoded Toast at :262. Move confirmation to the call site (AdminDashboard/SpotDetail) using src/ui/Modal.tsx with translated copy via t() and an isOpen boolean; the context method receives an already-confirmed call, performs the delete, and throws err on failure so the caller shows a translated Toast.
    (3) AuthContext.signOut (:37-39): wrap in try/catch, capture the Supabase error, console.error('Error signing out:', err), throw err. Update Profile.tsx signOut call site to attach .catch(() => Toast.show({ text: t('<existing signout/generic error key>'), duration: 'short' })).
    (4) AuthModal.tsx:73: replace alert(t('auth.alert_signup')) with Toast.show({ text: t('auth.alert_signup'), duration: 'short' }); import Toast from '@capacitor/toast'.
    Keep all existing i18n keys; change only the delivery mechanism, not copy semantics.
  </action>
  <verify>
    <automated>grep -c "confirm(" src/context/SpotsContext.tsx; grep -vE '^\s*//' src/components/AuthModal.tsx | grep -c "alert("; npm run lint 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - grep "confirm(" src/context/SpotsContext.tsx returns 0 matches
    - grep "alert(" src/components/AuthModal.tsx (excluding comments) returns 0 matches
    - SpotsContext addSpot/approveSpot/deleteSpot each contain a throw on their error path and no hardcoded French Toast string
    - AuthContext.signOut contains a try/catch with console.error and throw
    - npm run lint stays at 0 problems and npm run build completes with no errors
  </acceptance_criteria>
  <done>All 3 divergences aligned; lint + build green.</done>
</task>

<task type="auto">
  <name>Task 2: Parallelize cacheSpotImages (D-02)</name>
  <files>src/utils/offline.ts, src/context/FavoritesContext.tsx</files>
  <read_first>
    - src/utils/offline.ts (lines 7-31 serial loop; CACHE_NAME + console.error style)
    - src/context/FavoritesContext.tsx (line 47 caller)
    - .planning/phases/05-recette-globale-nettoyage-final/05-RESEARCH.md (Code Examples: cacheSpotImages async/batched fix)
  </read_first>
  <action>
    Replace the serial for-of loop in cacheSpotImages with Promise.all over imageUrls.map(async url => ...): keep the per-URL try/catch (one failed URL must not abort the batch), the cache.match short-circuit (return if already cached), fetch(url, { mode: 'cors' }), and cache.put(url, res) only when res.ok. Preserve the outer try/catch around caches.open(CACHE_NAME), the early return for null/empty imageUrls, and the existing console.error message style. Do NOT change cache key names or storage shapes (no runtime-state migration). Optionally bound concurrency at the FavoritesContext:47 caller so a large favorites list does not saturate the network on login (Claude's discretion, keep file style).
  </action>
  <verify>
    <automated>grep -c "Promise.all" src/utils/offline.ts; npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/offline.ts contains Promise.all and no longer contains a for-of loop awaiting cache.put per URL
    - Per-URL try/catch is preserved (one failure does not abort the batch)
    - CACHE_NAME and the cache key/storage shape are unchanged
    - npm run build completes with no errors and npm run lint stays green
  </acceptance_criteria>
  <done>cacheSpotImages parallelized with preserved resilience; build + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Supabase | Existing mutations (add/approve/delete spot, signOut) already cross here; this plan changes error delivery only, not the calls or their authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | deleteSpot confirmation moved from native confirm() to app Modal | mitigate | Confirmation must still gate the destructive delete — the context performs the delete only after the Modal-confirmed call; server RLS remains the real authorization. Validated by the delete-spot flow in the 05-05 recette. |
| T-05-05 | Repudiation | signOut error now surfaced instead of swallowed | accept | Surfacing the error improves observability; no security regression. Existing form validation (Phase 4) untouched. |
</threat_model>

<verification>
- grep confirms no native confirm() in SpotsContext and no alert() in AuthModal
- AuthContext.signOut rethrows; SpotsContext mutations rethrow
- cacheSpotImages uses Promise.all
- npm run lint green, npm run build green
</verification>

<success_criteria>
The 3 error-handling divergences are aligned to the rethrow/Toast/Modal canon and cacheSpotImages is parallelized. CODE-02 error-pattern homogenization and the D-02 perf fix (CODE-01) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-recette-globale-nettoyage-final/05-03-SUMMARY.md` when done
</output>
