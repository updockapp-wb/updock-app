---
phase: 05-anonymous-access
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/Profile.tsx
autonomous: true
requirements:
  - ANON-05

must_haves:
  truths:
    - "Anonymous user sees a dedicated login screen on the Profile tab"
    - "Profile login screen has a title, subtitle, Sign in button, and Create account button"
    - "Sign in button opens AuthModal in login mode"
    - "Create account button opens AuthModal in signup mode"
    - "Language toggle remains accessible on the anonymous Profile screen"
    - "After login from Profile tab, user stays on Profile tab (sees real profile)"
  artifacts:
    - path: "src/components/Profile.tsx"
      provides: "Anonymous profile screen with login/signup CTAs + language toggle"
      contains: "anon_profile.title"
  key_links:
    - from: "src/components/Profile.tsx"
      to: "AuthModal"
      via: "onOpenAuth callback prop"
      pattern: "onOpenAuth"
    - from: "src/components/Profile.tsx"
      to: "src/translations/fr.json"
      via: "t('anon_profile.title')"
      pattern: "anon_profile"
---

<objective>
Create the anonymous Profile screen: a centered layout with illustration, community title/subtitle, login/signup buttons, and the language toggle. Authenticated users continue to see their full profile as before.

Purpose: The Profile tab becomes a conversion funnel for anonymous users while keeping settings accessible.
Output: Profile.tsx renders an anonymous screen when !user, with auth CTAs and language toggle.
</objective>

<execution_context>
@C:/Users/wandr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wandr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-anonymous-access/05-CONTEXT.md
@.planning/phases/05-anonymous-access/05-RESEARCH.md
@.planning/phases/05-anonymous-access/05-UI-SPEC.md
@.planning/phases/05-anonymous-access/05-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/Profile.tsx (current interface):
```typescript
interface ProfileProps {
    onOpenAuth?: () => void;
    onAdminClick?: () => void;
    onSpotSelect?: (spotId: string) => void;
}
```

From src/context/AuthContext.tsx:
```typescript
export function useAuth(): { user: User | null; loading: boolean; signOut: () => void; session: Session | null; }
```

Translation keys available (from Plan 01):
```
"anon_profile.title": "Rejoins la communaute Updock" / "Join the Updock community"
"anon_profile.subtitle": "Decouvre les spots, partage tes sessions..." / "Discover spots, share your sessions..."
"anon_profile.btn_login": "Se connecter" / "Sign in"
"anon_profile.btn_signup": "Creer un compte" / "Create an account"
```

UI-SPEC anonymous profile screen:
- Container: flex column, items-center, justify-center, full height, px-6, bg-slate-50
- Illustration: 120px circle, bg-sky-50, MapPin icon (lucide, size 48, Sky 500)
- Title: text-xl (20px), font-bold, text-slate-800, text-center, mt-6
- Subtitle: text-base (16px), font-normal, text-slate-500, text-center, mt-2, max-w-[280px]
- Primary button: full width max-w-[320px], bg-sky-500, text-white, font-bold, py-4, rounded-xl, mt-8
- Secondary button: full width max-w-[320px], bg-white, border border-slate-200, text-slate-700, font-bold, py-4, rounded-xl, mt-3
- Language toggle: same as current settings row, separated by mt-12
- Subtle fade-in: framer-motion initial={{ opacity: 0 }} animate={{ opacity: 1 }}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement anonymous Profile screen</name>
  <files>src/components/Profile.tsx</files>
  <read_first>
    - src/components/Profile.tsx
    - src/translations/fr.json
    - src/translations/en.json
    - .planning/phases/05-anonymous-access/05-UI-SPEC.md
  </read_first>
  <action>
**1. Add imports to Profile.tsx:**

Add `MapPin` to the lucide-react import (it's not currently imported -- check first, add if missing):
```typescript
import { User, CreditCard, ChevronRight, Globe, LogOut, LogIn, Shield, Edit2, X, Camera, Calendar, Users, Bell, MapPin } from 'lucide-react';
```

Add `motion` from framer-motion:
```typescript
import { motion } from 'framer-motion';
```

**2. Add early return for anonymous users AFTER all hook calls:**

React hooks must be called in the same order regardless of user state. So the early return must come AFTER all existing `useLanguage()`, `useFavorites()`, `useAuth()`, `useProfile()`, `useState()`, `useSessions()`, `useNotifications()`, and `useEffect()` calls.

After the last `useEffect` (the `checkPermission()` one at line 50-52), add:

```typescript
// Anonymous Profile Screen
if (!user) {
  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      className="w-full h-full flex flex-col items-center justify-center px-6 bg-slate-50"
    >
      {/* Illustration */}
      <div className="w-[120px] h-[120px] rounded-full bg-sky-50 flex items-center justify-center">
        <MapPin size={48} className="text-sky-500" />
      </div>

      {/* Title */}
      <h2 className="text-xl font-bold text-slate-800 text-center mt-6">
        {t('anon_profile.title')}
      </h2>

      {/* Subtitle */}
      <p className="text-base font-normal text-slate-500 text-center mt-2 max-w-[280px]">
        {t('anon_profile.subtitle')}
      </p>

      {/* Sign in button (primary) */}
      <button
        onClick={() => onOpenAuth?.()}
        className="w-full max-w-[320px] bg-sky-500 hover:bg-sky-400 text-white font-bold py-4 rounded-xl mt-8 transition-colors"
      >
        {t('anon_profile.btn_login')}
      </button>

      {/* Create account button (secondary) */}
      <button
        onClick={() => onOpenAuth?.()}
        className="w-full max-w-[320px] bg-white border border-slate-200 text-slate-700 font-bold py-4 rounded-xl mt-3 hover:bg-slate-50 transition-colors"
      >
        {t('anon_profile.btn_signup')}
      </button>

      {/* Language toggle (settings accessible for anonymous) */}
      <div className="mt-12 w-full max-w-[320px]">
        <div className="bg-white rounded-2xl border border-slate-100 overflow-hidden">
          <div className="flex items-center justify-between p-4 hover:bg-slate-50 transition-colors">
            <div className="flex items-center gap-3 text-slate-700">
              <Globe size={20} className="text-sky-500" />
              <span className="font-medium">{t('profile.language')}</span>
            </div>
            <div className="flex bg-slate-100 rounded-lg p-1">
              <button
                onClick={() => setLanguage('fr')}
                className={`px-3 py-1 rounded-md text-sm font-bold transition-all ${language === 'fr' ? 'bg-white shadow text-sky-600' : 'text-slate-400'}`}
              >
                FR
              </button>
              <button
                onClick={() => setLanguage('en')}
                className={`px-3 py-1 rounded-md text-sm font-bold transition-all ${language === 'en' ? 'bg-white shadow text-sky-600' : 'text-slate-400'}`}
              >
                EN
              </button>
            </div>
          </div>
        </div>
      </div>

      {/* Version */}
      <p className="text-xs text-slate-300 mt-8">Updock v1.1.0 (Beta)</p>
    </motion.div>
  );
}
```

**3. No changes to the authenticated profile section** -- the existing profile code after this early return stays exactly as-is.

**4. Post-login behavior** -- After the user logs in from the Profile tab, `useAuth()` will return a non-null `user`, so the early return is skipped and the real profile renders automatically. The user stays on the Profile tab because `activeTab` in App.tsx is not changed by auth state changes. This satisfies the CONTEXT.md locked decision: "After login from Profile tab, user stays on Profile tab."

**5. Note on AuthModal mode** -- Both buttons call `onOpenAuth?.()` which opens AuthModal. AuthModal defaults to `isLogin = true` (sign-in mode). The user can toggle to sign-up inside AuthModal using the existing "Don't have an account? Sign Up" link. This is acceptable per Claude's discretion -- the important thing is both buttons trigger the auth flow. A future enhancement could pass an `initialMode` prop to AuthModal, but that is out of scope for this plan.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Profile.tsx` contains `if (!user)` early return block
    - Profile.tsx renders `{t('anon_profile.title')}` in the anonymous screen
    - Profile.tsx renders `{t('anon_profile.subtitle')}` in the anonymous screen
    - Profile.tsx renders `{t('anon_profile.btn_login')}` button with `onClick={() => onOpenAuth?.()}`
    - Profile.tsx renders `{t('anon_profile.btn_signup')}` button with `onClick={() => onOpenAuth?.()}`
    - Profile.tsx anonymous screen contains a `<Globe` icon (language toggle present)
    - Profile.tsx anonymous screen contains `setLanguage('fr')` and `setLanguage('en')` calls
    - Profile.tsx imports `MapPin` from lucide-react
    - Profile.tsx imports `motion` from framer-motion
    - The anonymous screen renders a 120px circle with `MapPin` icon (`w-[120px] h-[120px] rounded-full bg-sky-50`)
    - All React hooks are called BEFORE the `if (!user)` early return (no conditional hook calls)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Anonymous Profile screen renders with illustration, community messaging, login/signup CTAs, and language toggle. Authenticated profile unchanged. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Profile.tsx has an early return for `!user` that shows the anonymous screen
- Anonymous profile screen has: MapPin illustration, title, subtitle, 2 buttons, language toggle
- Authenticated profile renders exactly as before (no regression)
- All hooks are called unconditionally (no React rules violations)
</verification>

<success_criteria>
- Anonymous users see a centered, branded login screen on the Profile tab
- Both sign-in and create-account buttons open AuthModal
- Language toggle is accessible for anonymous users
- After login, the profile tab transitions to the real profile without redirect
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-anonymous-access/05-03-SUMMARY.md`
</output>
