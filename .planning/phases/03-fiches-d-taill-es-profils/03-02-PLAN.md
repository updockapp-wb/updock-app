---
phase: 03-fiches-d-taill-es-profils
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/ui/Modal.tsx
  - src/components/PremiumModal.tsx
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "Le master src/ui/Modal expose une 3e forme supportée : surface=\"light\" + layout=\"center\""
    - "Les 2 formes existantes de Modal (glass+center, light+sheet) rendent exactement les mêmes classes qu'avant — AuthModal et FiltersModal ne changent pas d'un caractère"
    - "PremiumModal ne contient plus sa propre coque de dialogue (AnimatePresence + backdrop + panneau) : elle vient du master, conformément à la migration de PremiumModal vers src/ui/Modal actée pour cette phase (D-05)"
    - "Le titre de PremiumModal est rendu par src/ui/Header surface=\"light\""
    - "Les couleurs de PremiumModal ayant une correspondance 1:1 avec un token sont câblées (text-muted, bg-secondary)"
  artifacts:
    - path: "src/ui/Modal.tsx"
      provides: "Master modal à 3 formes, dont light+center extraite verbatim de PremiumModal"
      contains: "layout === 'center'"
    - path: "src/components/PremiumModal.tsx"
      provides: "Dialogue premium consommant Modal + Header du design system"
      contains: "surface=\"light\" layout=\"center\""
  key_links:
    - from: "src/components/PremiumModal.tsx"
      to: "src/ui/Modal.tsx"
      via: "import + props surface/layout"
      pattern: "surface=\"light\" layout=\"center\""
    - from: "src/components/PremiumModal.tsx"
      to: "src/ui/Header.tsx"
      via: "import + prop surface"
      pattern: "Header surface=\"light\""
---

<objective>
Ajouter la 3e forme `surface="light"` + `layout="center"` au master `src/ui/Modal`, puis
migrer `PremiumModal.tsx` vers `Modal` + `Header` avec câblage des tokens — sans le moindre
changement de rendu observable.

Purpose : D-05 verrouille la migration de `PremiumModal` vers `src/ui/Modal` dans cette
phase (UI-02). Mais la prémisse littérale de D-05 est factuellement fausse et l'a été
corrigée par `03-UI-SPEC.md` (approuvé) : `PremiumModal` n'est pas un dialogue « glass
centré », c'est un dialogue **blanc opaque centré** (`bg-white rounded-3xl p-8`) avec un
backdrop animé en fondu. Le dispatch actuel du master (`Modal.tsx:30`,
`surface === 'light' || layout === 'sheet'`) route `surface="light"` seul vers le
**bottom-sheet**. Migrer sans ajouter la 3e forme produirait une carte translucide à texte
slate sur fond clair (illisible) ou un panneau qui slide depuis le bas : régression majeure
garantie (`03-RESEARCH.md` Pitfall 1). L'extension d'abord, la migration ensuite — miroir
exact du travail réalisé en Phase 2 sur `FiltersModal`.

Output : `src/ui/Modal.tsx` à 3 formes + `src/components/PremiumModal.tsx` migré.

**Contrainte cardinale : harmonisation, pas rebranding. Chaque classe est extraite verbatim
de l'existant ; aucune valeur de design n'est inventée. Toute divergence de rendu est une
régression, pas un arbitrage.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md
@.planning/phases/03-fiches-d-taill-es-profils/03-BASELINE.md

<interfaces>
<!-- Contrats existants dont dépend ce plan. Extraits du code : aucune exploration requise. -->

src/ui/Modal.tsx — API actuelle (à ÉTENDRE, pas à réécrire) :
    interface ModalProps {
        isOpen: boolean;
        onClose: () => void;
        children: React.ReactNode;
        surface?: 'glass' | 'light';
        layout?: 'center' | 'sheet';
    }
    Défauts : surface='glass', layout='center'.
    Dispatch actuel (ligne 30) : `const isLightSheet = surface === 'light' || layout === 'sheet';`
    -> branche bottom-sheet claire (avec dev-warning si la paire est mixte), sinon branche
    glass centrée par défaut.

src/ui/Header.tsx — API (consommée telle quelle, NE PAS modifier) :
    interface HeaderProps { title: string; subtitle?: string; onClose?: () => void; surface?: 'glass' | 'light'; }
    Forme empilée (sans onClose), surface='light' -> `<h2 className="text-2xl font-bold text-text mb-2">{title}</h2>`
    ATTENTION : passer `onClose` basculerait sur la forme rangée-avec-close (`flex justify-between
    items-center mb-6` + bouton close intégré). NE PAS passer onClose ici.
    La prop `subtitle` rend `<p className="text-muted text-sm mb-8">` : incompatible avec la
    description de PremiumModal (`text-slate-500 mb-8 leading-relaxed`), donc non utilisée.

Consommateurs actuels de Modal (re-confirmés en plan 03-01) :
  - src/components/AuthModal.tsx  -> défauts implicites (glass + center)
  - src/components/FiltersModal.tsx -> surface="light" layout="sheet" explicites
Aucun ne passe light+center : la nouvelle branche leur est inatteignable.

Tokens disponibles (src/index.css @theme, aliasés à la palette Tailwind v4) :
  --color-primary: var(--color-sky-500)
  --color-secondary: var(--color-slate-900)
  --color-accent: var(--color-rose-500)
  --color-text: var(--color-slate-800)
  --color-muted: var(--color-slate-500)
  --color-background: var(--color-slate-50)
  --radius-4xl: 2rem
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Ajouter la 3e forme light+center au master src/ui/Modal</name>
  <files>src/ui/Modal.tsx</files>
  <read_first>
    - src/ui/Modal.tsx (état actuel complet : dispatch binaire ligne 30, forme light+sheet lignes 39-58, forme glass+center lignes 60-82)
    - src/components/PremiumModal.tsx (lignes 14-30 : source verbatim des classes de la nouvelle forme)
    - .planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md (§ « Contrat du nouveau composant — Modal surface="light" layout="center" » : spécification exacte + tableau des différences load-bearing)
  </read_first>
  <action>
    Remplacer le dispatch binaire de `Modal.tsx` par un dispatch à 3 voies, dans cet ordre :

    1. Nouvelle branche EN PREMIER : condition `surface === 'light' && layout === 'center'`.
       Elle rend, à l'intérieur de `<AnimatePresence>{isOpen && (...)}</AnimatePresence>` :
       - wrapper `<div>` de classe `fixed inset-0 z-[5000] flex items-center justify-center p-6`
       - backdrop `<motion.div>` avec `initial={{ opacity: 0 }}`, `animate={{ opacity: 1 }}`,
         `exit={{ opacity: 0 }}`, `onClick={onClose}` et la classe
         `absolute inset-0 bg-slate-900/40 backdrop-blur-sm`
       - panneau `<motion.div>` avec `initial={{ scale: 0.95, opacity: 0, y: 20 }}`,
         `animate={{ scale: 1, opacity: 1, y: 0 }}`, `exit={{ scale: 0.95, opacity: 0, y: 20 }}`
         et la classe `relative w-full max-w-sm bg-white rounded-3xl p-8 shadow-2xl overflow-hidden`,
         contenant `{children}`
       - AUCUN bouton close intégré (celui de `PremiumModal` est un bouton flottant absolu dans
         les children, qui s'appuie sur le `relative` du panneau). AUCUNE prop `transition`
         explicite : conserver le ressort par défaut de framer-motion, exactement comme aujourd'hui.

    2. Deuxième branche : la forme light+sheet actuelle, condition resserrée à
       `surface === 'light' || layout === 'sheet'` mais évaluée APRÈS la branche light+center
       (donc atteinte par light+sheet, glass+sheet et light seul). Son markup et ses classes
       restent **strictement inchangés** : `fixed inset-0 z-[3000] flex items-end sm:items-center
       justify-center pointer-events-none`, backdrop enfant statique `absolute inset-0 bg-black/20
       backdrop-blur-sm pointer-events-auto`, panneau `relative z-10 bg-white w-full max-w-sm
       sm:rounded-3xl rounded-t-3xl p-6 shadow-2xl pointer-events-auto`, animation `y: "100%"`.
       Conserver le `console.warn` de dev pour les paires mixtes restantes (`glass`+`sheet`),
       en reformulant son message pour qu'il ne mente plus : les paires supportées sont
       désormais glass+center, light+sheet et light+center ; glass+sheet reste non supportée.

    3. Troisième branche (fallback) : la forme glass+center actuelle, markup et classes
       **strictement inchangés** (`fixed inset-0 z-[5000] flex items-center justify-center p-4
       bg-black/60 backdrop-blur-md`, panneau `bg-white/10 backdrop-blur-xl border
       border-white/20 w-full max-w-sm rounded-4xl p-8 shadow-2xl relative overflow-hidden`,
       bouton close `<Button variant="ghost" iconOnly aria-label="Close">`).

    Mettre à jour le commentaire de tête du fichier pour documenter les 3 formes et leur origine
    verbatim (light+center vient de `PremiumModal.tsx:16-30`), en conservant le style des
    commentaires existants.

    **Câblage de token du backdrop — procédure déterministe obligatoire** (`03-UI-SPEC.md`
    § Contraintes d'implémentation) : écrire d'abord la nouvelle branche avec le littéral
    `bg-slate-900/40`. Puis lancer `npm run build` et comparer les valeurs résolues :
    `grep -oE "\.(bg-slate-900\\\\/40|bg-secondary\\\\/40)\{[^}]*\}" dist/assets/*.css`.
    - Si les deux règles produisent une valeur `color-mix(...)`/`oklch(...)` **identique**,
      remplacer par `bg-secondary/40` et consigner la preuve (les deux chaînes CSS) dans le SUMMARY.
    - Si la comparaison n'est pas concluante (une des deux classes absente du CSS compilé, ou
      valeurs différentes), **garder le littéral `bg-slate-900/40`** et consigner le résultat
      négatif dans le SUMMARY. Ne pas deviner.

    Ne modifier NI `src/ui/Header.tsx`, NI `src/ui/Button.tsx`, NI `AuthModal.tsx`,
    NI `FiltersModal.tsx`. `PremiumModal.tsx` est traité en Task 2.
  </action>
  <verify>
    <automated>npm run build && grep -q "layout === 'center'" src/ui/Modal.tsx && grep -q "relative w-full max-w-sm bg-white rounded-3xl p-8 shadow-2xl overflow-hidden" src/ui/Modal.tsx && grep -q "relative z-10 bg-white w-full max-w-sm sm:rounded-3xl rounded-t-3xl p-6 shadow-2xl pointer-events-auto" src/ui/Modal.tsx && grep -q "bg-white/10 backdrop-blur-xl border border-white/20 w-full max-w-sm rounded-4xl p-8 shadow-2xl relative overflow-hidden" src/ui/Modal.tsx && grep -q "z-\[3000\]" src/ui/Modal.tsx && test -z "$(git status --short src/components/AuthModal.tsx src/components/FiltersModal.tsx src/ui/Header.tsx src/ui/Button.tsx)"</automated>
  </verify>
  <done>Modal.tsx expose 3 formes ; les chaînes de classes des formes light+sheet et glass+center sont inchangées au caractère près ; le build passe ; AuthModal/FiltersModal/Header/Button non touchés.</done>
  <acceptance_criteria>
    - `npm run build` termine sans erreur (le typecheck `tsc -b` inclus).
    - `src/ui/Modal.tsx` contient une condition testant `surface === 'light' && layout === 'center'` évaluée AVANT la condition `surface === 'light' || layout === 'sheet'` (assertion source : le numéro de ligne de la première est inférieur à celui de la seconde).
    - La chaîne exacte `relative w-full max-w-sm bg-white rounded-3xl p-8 shadow-2xl overflow-hidden` est présente (panneau light+center).
    - La chaîne exacte `fixed inset-0 z-[5000] flex items-center justify-center p-6` est présente (wrapper light+center, `p-6` et non `p-4`).
    - La branche light+center contient `initial={{ scale: 0.95, opacity: 0, y: 20 }}` (animation scale + y, distincte du `scale` seul de glass+center et du `y: "100%"` de light+sheet).
    - La branche light+center ne contient aucun `<Button` ni `<X` (pas de close intégré) : assertion source sur le corps de la branche.
    - Les chaînes exactes `relative z-10 bg-white w-full max-w-sm sm:rounded-3xl rounded-t-3xl p-6 shadow-2xl pointer-events-auto` et `bg-white/10 backdrop-blur-xl border border-white/20 w-full max-w-sm rounded-4xl p-8 shadow-2xl relative overflow-hidden` sont toujours présentes à l'identique (formes existantes préservées).
    - `grep -c "z-\[3000\]" src/ui/Modal.tsx` retourne 1 et `grep -c "z-\[5000\]" src/ui/Modal.tsx` retourne 2 (light+sheet inchangé ; glass+center et light+center à 5000).
    - Un `console.warn` de dev subsiste pour la paire non supportée glass+sheet, et son message ne prétend plus que light+sheet est la seule paire light valide.
    - `git status --short src/components/AuthModal.tsx src/components/FiltersModal.tsx src/ui/Header.tsx src/ui/Button.tsx` est vide.
    - Le SUMMARY consigne le résultat de la comparaison CSS `bg-slate-900/40` vs `bg-secondary/40` (câblé avec preuve, ou littéral conservé avec le résultat négatif).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 : Migrer PremiumModal vers Modal + Header et câbler ses tokens</name>
  <files>src/components/PremiumModal.tsx</files>
  <read_first>
    - src/components/PremiumModal.tsx (état actuel complet, 61 lignes)
    - src/ui/Modal.tsx (après Task 1 : la 3e forme light+center est la coque cible)
    - src/ui/Header.tsx (forme empilée surface="light" — rend `text-2xl font-bold text-text mb-2`)
    - .planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md (§ Inventaire des migrations structurelles, sites 6 et 7 ; § Non-migrations ; § Écarts assumés)
  </read_first>
  <action>
    Réécrire `PremiumModal.tsx` en conservant EXACTEMENT sa signature
    (`interface PremiumModalProps { isOpen: boolean; onClose: () => void; }`, export default) et
    son contenu visible, mais en déléguant la coque au master :

    - Imports : ajouter `Modal` depuis `'../ui/Modal'` et `Header` depuis `'../ui/Header'`.
      Retirer `motion` et `AnimatePresence` de l'import `framer-motion` — ils deviennent inutilisés
      et `tsc -b` échouerait sur un import mort. Conserver `X` et `Sparkles` depuis `lucide-react`
      et `useLanguage` depuis `'../context/LanguageContext'`.
    - Remplacer tout le bloc `<AnimatePresence>{isOpen && (<div className="fixed inset-0 z-[5000] …">
      <motion.div backdrop/><motion.div panneau>…</motion.div></div>)}</AnimatePresence>` par
      `<Modal isOpen={isOpen} onClose={onClose} surface="light" layout="center">` … `</Modal>`.
      Le wrapper, le backdrop animé et le panneau viennent désormais du master : ne PAS les
      redéclarer dans le composant.
    - Le bouton close reste un `<button>` natif (aucun variant `Button` ne correspond :
      `ghost` rend `bg-white/5 hover:bg-white/10`, surface glass). Conserver
      `onClick={onClose}` et la classe `absolute top-4 right-4 p-2 bg-slate-100 rounded-full
      text-muted hover:bg-slate-200 transition-colors` (seul changement : `text-slate-500`
      -> `text-muted`). Ajouter `aria-label="Close"` : écart a11y explicitement autorisé par
      `03-UI-SPEC.md` § Écarts assumés, zéro effet visuel.
    - Conserver à l'identique le conteneur `<div className="flex flex-col items-center text-center mt-2">`
      et le badge `<div className="w-16 h-16 rounded-2xl bg-gradient-to-tr from-sky-400 to-blue-500
      flex items-center justify-center shadow-lg shadow-blue-500/30 mb-6">` avec
      `<Sparkles size={32} className="text-white" />` (dégradé hors token : rester littéral).
    - Remplacer `<h2 className="text-2xl font-bold text-slate-800 mb-2">{t('premium.title')}</h2>`
      par `<Header surface="light" title={t('premium.title')} />`. NE PAS passer `onClose` à
      `Header` (basculerait sur la forme rangée-avec-close et injecterait un second bouton close).
      NE PAS passer `subtitle`.
    - La description reste un `<p>` custom : `<p className="text-muted mb-8 leading-relaxed">{t('premium.desc')}</p>`
      (seul changement : `text-slate-500` -> `text-muted`). Ne pas la passer en `subtitle` de
      `Header` : cela ajouterait `text-sm` et retirerait `leading-relaxed`.
    - Le CTA reste un `<button>` natif (`py-3.5` est hors de l'échelle `Button` sm/md/lg =
      py-2/py-3/py-4, et `bg-slate-900` n'a aucun variant correspondant). Conserver
      `onClick={onClose}` et la classe `w-full py-3.5 bg-secondary text-white font-bold rounded-xl
      active:scale-95 transition-all` (seul changement : `bg-slate-900` -> `bg-secondary`).
    - Conserver les 3 clés i18n inchangées : `premium.title`, `premium.desc`, `premium.btn`.

    Ne toucher à aucun autre fichier. `Profile.tsx` consomme `<PremiumModal isOpen onClose />` :
    la signature ne change pas, donc aucune modification côté appelant (plan 03-03 travaille
    ce fichier en parallèle — ne PAS l'éditer ici).
  </action>
  <verify>
    <automated>npm run build && grep -q 'surface="light" layout="center"' src/components/PremiumModal.tsx && grep -q '<Header surface="light" title={t(.premium.title.)} />' src/components/PremiumModal.tsx && test "$(grep -c 'AnimatePresence' src/components/PremiumModal.tsx)" = "0" && test "$(grep -c 'bg-slate-900' src/components/PremiumModal.tsx)" = "0" && test "$(grep -c 'text-slate-500' src/components/PremiumModal.tsx)" = "0" && test "$(grep -c 'text-slate-800' src/components/PremiumModal.tsx)" = "0" && test "$(grep -c 'text-muted' src/components/PremiumModal.tsx)" = "2" && test "$(grep -c 'bg-secondary' src/components/PremiumModal.tsx)" = "1" && grep -q 'aria-label="Close"' src/components/PremiumModal.tsx</automated>
  </verify>
  <done>PremiumModal consomme Modal (light+center) et Header (light), n'a plus de coque locale, et ses 4 câblages de token (2 text-muted, 1 bg-secondary, titre absorbé par Header) sont en place ; le build passe.</done>
  <acceptance_criteria>
    - `npm run build` termine sans erreur.
    - `grep -c 'AnimatePresence' src/components/PremiumModal.tsx` retourne 0 (coque locale supprimée) et `grep -c 'motion' src/components/PremiumModal.tsx` retourne 0.
    - `grep -c 'text-slate-500' src/components/PremiumModal.tsx` retourne 0 ; `grep -c 'text-muted' src/components/PremiumModal.tsx` retourne 2 (bouton close + description).
    - `grep -c 'text-slate-800' src/components/PremiumModal.tsx` retourne 0 (titre absorbé par `Header`).
    - `grep -c 'bg-slate-900' src/components/PremiumModal.tsx` retourne 0 ; `grep -c 'bg-secondary' src/components/PremiumModal.tsx` retourne 1 (CTA).
    - Le fichier contient `<Header surface="light" title={t('premium.title')} />` et ne contient AUCUN `<Header` avec une prop `onClose` ou `subtitle`.
    - La chaîne `py-3.5` est toujours présente (CTA non migré vers `Button`) et la chaîne `w-16 h-16 rounded-2xl bg-gradient-to-tr from-sky-400 to-blue-500 flex items-center justify-center shadow-lg shadow-blue-500/30 mb-6` est intacte (badge Sparkles).
    - Le bouton close porte `aria-label="Close"` et conserve `p-2 bg-slate-100 rounded-full` + `hover:bg-slate-200`.
    - Les 3 clés i18n `premium.title`, `premium.desc`, `premium.btn` sont toujours référencées (assertion : `grep -c "premium\." src/components/PremiumModal.tsx` >= 3) et aucun texte user-facing n'est codé en dur.
    - `git status --short src/` ne liste que `src/ui/Modal.tsx` et `src/components/PremiumModal.tsx` (aucun autre fichier touché par ce plan).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Refactor de présentation pur : aucun nouveau flux d'authentification, aucune nouvelle entrée serveur, aucune donnée utilisateur traitée. `PremiumModal` n'affiche que du texte i18n statique et n'appelle aucune API. |
| Consommateurs existants -> master Modal étendu | Frontière de compatibilité : une régression du dispatch casserait `AuthModal` (surface d'authentification) ou `FiltersModal`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | `src/ui/Modal.tsx` (dispatch à 3 voies) | mitigate | Ordre des branches imposé (light+center d'abord) et chaînes de classes des 2 formes existantes asserties au caractère près dans `<acceptance_criteria>` via `grep -q` sur les chaînes complètes. Consommateurs re-confirmés en plan 03-01 : aucun ne passe light+center, la nouvelle branche leur est inatteignable. |
| T-03-02-02 | Denial of Service (UX) | `AuthModal` via `src/ui/Modal` | mitigate | `AuthModal` est la seule voie de connexion : un modal cassé bloquerait l'authentification. La forme glass+center reste la branche fallback par défaut, `git status` sur `AuthModal.tsx` doit rester vide, et la recette du plan 03-06 rouvre explicitement `AuthModal` et `FiltersModal`. |
| T-03-02-03 | Elevation of Privilege | Rangée « Devenir Premium » (`Profile.tsx:404`) | accept | `PremiumModal` n'accorde aucun droit : son CTA appelle `onClose`, aucun paiement ni changement de rôle. Aucune frontière d'autorisation traversée. |
| T-03-02-04 | Information Disclosure | Backdrop / z-index du dialogue | accept | `z-[5000]` et le backdrop opaque sont conservés à l'identique : pas de fuite visuelle de contenu sous-jacent introduite ni supprimée. |
| T-03-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | Aucun `npm install` : `03-RESEARCH.md` § Package Legitimacy Audit ne relève aucune cible. Toutes les dépendances utilisées (`framer-motion`, `lucide-react`) sont déjà présentes et vérifiées dans `package.json`. |
</threat_model>

<verification>
- `npm run build` passe (typecheck `tsc -b` inclus) après chaque tâche.
- `src/ui/Modal.tsx` : 3 branches, light+center évaluée en premier, chaînes des 2 formes existantes identiques au caractère près.
- `src/components/PremiumModal.tsx` : 0 `AnimatePresence`, 0 `motion`, 0 `text-slate-500`, 0 `text-slate-800`, 0 `bg-slate-900` ; 2 `text-muted`, 1 `bg-secondary`, 1 `<Header surface="light">`.
- `git status --short` limité aux 2 fichiers du plan.
- Vérification visuelle (déléguée au plan 03-06) : `PremiumModal` pixel-identique à la capture AVANT, `AuthModal` et `FiltersModal` non régressés.
</verification>

<success_criteria>
- Le master `Modal` couvre les 3 formes de dialogue réellement présentes dans l'app, sans avoir inventé aucune apparence (DS-02 : « variants widen the API, never the appearance »).
- `PremiumModal` ne duplique plus de coque de dialogue et consomme 2 composants maîtres — contribution directe et vérifiable à UI-02.
- Aucun changement de rendu observable, hormis l'ajout d'`aria-label="Close"` (écart a11y autorisé).
</success_criteria>

<output>
Créer `.planning/phases/03-fiches-d-taill-es-profils/03-02-SUMMARY.md` une fois terminé, en y
consignant : le résultat de la comparaison CSS `bg-slate-900/40` vs `bg-secondary/40` (câblé ou
littéral, avec les chaînes CSS relevées), et la liste exacte des câblages de token appliqués à
`PremiumModal.tsx`.
</output>
