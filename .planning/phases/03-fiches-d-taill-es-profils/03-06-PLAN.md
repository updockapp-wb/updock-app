---
phase: 03-fiches-d-taill-es-profils
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05"]
files_modified:
  - .planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md
autonomous: false
requirements: [UI-01, UI-02, PERF-02]

must_haves:
  truths:
    - "Chaque wiring de token du périmètre est prouvé byte-identique en CSS compilé (var() différente, valeur oklch()/color-mix() identique)"
    - "Les 5 surfaces (fiche snap 0.35/0.95 sur 3 onglets, profil anonyme, profil authentifié, PremiumModal, CommunityStats) sont capturées APRÈS et comparées pixel-à-pixel aux captures AVANT de 03-BASELINE.md — aucune divergence hormis les écarts assumés"
    - "AuthModal et FiltersModal ne sont pas régressés par l'extension du master Modal"
    - "La checklist de recette manuelle mobile (carte, fiche spot, favoris, avis, session, profil, auth, premium, stats communauté) passe à 100% sur device réel, y compris une passe explicitement non authentifiée et la vérification de la safe-area"
    - "La gestuelle native de SpotDetail (drag-to-dismiss, snap points, layoutId liste<->fiche, lightbox next/prev) est intacte sur device"
  artifacts:
    - path: ".planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md"
      provides: "Résultat de la recette QA-01 (100%) + preuve byte-identité + verdict UI-01/UI-02/PERF-02 de la phase"
      contains: "100%"
  key_links:
    - from: "03-QA-CHECKLIST.md"
      to: "03-BASELINE.md (captures AVANT + métriques)"
      via: "comparaison APRÈS/AVANT des 5 surfaces et des métriques A/B/C"
      pattern: "AVANT"
---

<objective>
Valider la phase 3 : prouver la **byte-identité** de tous les wirings de token (CSS compilé
+ captures visuelles APRÈS vs AVANT des 5 surfaces), confirmer la **non-régression** de
`AuthModal`/`FiltersModal`, et passer la **checklist de recette manuelle mobile à 100%** sur
device réel — success criterion 4 du ROADMAP Phase 3.

Purpose : c'est le phase gate. Les plans 03-02 à 03-05 ont délégué ici toute vérification
visuelle, gestuelle et device (elles ne sont pas scriptables en CLI). Ce plan est
l'ancrage de comparaison contre `03-BASELINE.md` (produit par 03-01) : il transforme
« le build passe » en « zéro régression prouvée ». Sans lui, UI-01/UI-02 restent des
assertions de code non confrontées au rendu réel, et QA-01 (le filet de sécurité permanent
du milestone) n'est pas honoré pour cette phase.

Output : `03-QA-CHECKLIST.md` (recette 100% + preuves byte-identité + verdict de phase),
et la mise à jour des cases de vérification visuelle/device.

**Contrainte cardinale : toute divergence de rendu détectée en recette est une régression à
corriger (retour en plan de gap closure), pas un arbitrage. Les seuls écarts observables
autorisés sont l'`aria-label="Close"` de PremiumModal et l'éventuelle correction du
commentaire `src/index.css:41` (`03-UI-SPEC.md` § Écarts assumés).**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md
@.planning/phases/03-fiches-d-taill-es-profils/03-BASELINE.md
@.planning/phases/03-fiches-d-taill-es-profils/03-VALIDATION.md

<interfaces>
<!-- Ce que ce plan valide, tel que produit par les plans amont. -->

Périmètre migré (à confronter au rendu réel) :
  - 03-02 : src/ui/Modal (3e forme light+center) + PremiumModal (Modal + Header + tokens)
  - 03-03 : Profile (Card ×2 + Button secondary + tokens) + CommunityStatsScreen (Card ×2 + tokens)
  - 03-04 : SpotDetail (tokens de couleur + rayons canoniques), coque vaul/layoutId gelés
  - 03-05 : loading="lazy" ×3 + prefetch lightbox ; métriques A/B/C APRÈS dans 03-BASELINE.md

Protocole de preuve byte-identité (méthodo 01-VERIFICATION.md) :
  npm run build
  grep -oE "\.(text-slate-800|text-text)\{[^}]*\}" dist/assets/*.css
  # Attendu : chaîne var() différente, valeur oklch()/color-mix() IDENTIQUE.
  # Puis getComputedStyle sur l'élément avant/après (chrome-devtools-mcp).

5 surfaces à comparer (captures AVANT dans 03-BASELINE.md § 7, préfixe `03-before-`) :
  fiche spot snap 0.35 ; fiche spot snap 0.95 (onglets Info/Avis/Sessions) ; profil anonyme ;
  profil authentifié ; PremiumModal ; CommunityStatsScreen.

Chemin critique gestuel (recette device) :
  Map -> clic marker -> SpotDetail au snap 0.35 -> drag vers 0.95 -> swipe-to-dismiss ->
  onglet Avis -> ouverture lightbox -> next/prev -> fermeture.

Gardes d'accès à re-tester NON authentifié :
  cœur favori -> onOpenAuth (SpotDetail:288) ; ouverture lightbox -> onOpenAuth (:397) ;
  vignette blur-sm + cadenas (:412-421) ; rangée Admin masquée si non-isAdmin (Profile:154,414).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Prouver la byte-identité en CSS compilé des wirings de token du périmètre</name>
  <files>.planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md (§ Contrat de wiring de tokens — liste des paires littéral/token par fichier ; § Protocole de preuve obligatoire)
    - .planning/phases/03-fiches-d-taill-es-profils/03-RESEARCH.md (§ Protocole de vérification byte-identique — commandes exactes)
  </read_first>
  <action>
    Lancer `npm run build`, puis pour CHAQUE paire de tokens câblée dans la phase, extraire du
    CSS compilé les deux règles (le littéral d'origine et le token) et prouver que leur valeur
    résolue est IDENTIQUE (la chaîne `var()` diffère, la valeur `oklch()` / `color-mix()` est la
    même). Paires à prouver (au minimum une occurrence de chaque) :
    - `text-slate-800` vs `text-text`
    - `text-slate-500` vs `text-muted`
    - `text-sky-500` vs `text-primary`
    - `bg-slate-50` vs `bg-background`
    - `text-rose-500` vs `text-accent` (et `fill-rose-500` vs `fill-accent`)
    - `border-sky-500` / `focus:border-sky-500` vs `border-primary` / `focus:border-primary`
    - `bg-slate-900` vs `bg-secondary` et `bg-slate-900/40` vs `bg-secondary/40` (backdrop Modal —
      reprendre/confirmer le résultat consigné par le SUMMARY de 03-02)
    - Rayons : `rounded-[24px]` vs `rounded-3xl` (=1.5rem) et `rounded-t-[32px]` vs `rounded-t-4xl`
      (=2rem) — prouver par la valeur `border-radius` compilée.

    Commande type (adapter le sélecteur) :
    `grep -oE "\.(text-slate-800|text-text)\{[^}]*\}" dist/assets/*.css`.

    Créer `.planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md` avec une section
    `## 1. Preuve byte-identité (CSS compilé)` contenant un tableau : paire | valeur littéral |
    valeur token | verdict (IDENTIQUE / DIVERGENT). Tout DIVERGENT est une régression à corriger
    avant de poursuivre (retour au plan responsable). Consigner explicitement le cas
    `text-slate-900` -> `text-secondary` comme **NON câblé par choix conservateur** (donc pas de
    preuve requise, littéral conservé).
  </action>
  <verify>
    <automated>npm run build && test -f .planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md && grep -q "Preuve byte-identité" .planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md && grep -q "IDENTIQUE" .planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md && ls dist/assets/*.css >/dev/null 2>&1</automated>
  </verify>
  <done>03-QA-CHECKLIST.md existe avec la section 1 ; chaque paire de tokens câblée est prouvée IDENTIQUE en CSS compilé ; text-slate-900 documenté comme non câblé ; aucun DIVERGENT non résolu.</done>
  <acceptance_criteria>
    - `npm run build` produit un `dist/assets/*.css` (assertion : `ls dist/assets/*.css` réussit).
    - `03-QA-CHECKLIST.md` contient une section « Preuve byte-identité (CSS compilé) » avec une ligne par paire listée dans l'action.
    - Chaque paire câblée porte le verdict `IDENTIQUE` (valeur `oklch()`/`color-mix()`/`border-radius` égale entre littéral et token).
    - Aucune ligne ne porte `DIVERGENT` (sinon la régression est corrigée par le plan responsable avant de continuer).
    - La paire `text-slate-900` -> `text-secondary` est explicitement notée « NON câblé (choix conservateur) », sans exiger de preuve.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 : Comparaison visuelle APRÈS/AVANT des 5 surfaces + non-régression AuthModal/FiltersModal</name>
  <files>.planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/03-fiches-d-taill-es-profils/03-BASELINE.md (§ 7 captures AVANT, préfixe `03-before-`)
    - .planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md (§ Hiérarchie visuelle — points focaux à ne pas déplacer ; § Écarts assumés ; § Zones gelées)
  </read_first>
  <action>
    Étape visuelle nécessitant un rendu réel (chrome-devtools-mcp sur `npm run dev` + éventuel
    device) : produire les captures APRÈS des 5 surfaces et les comparer aux captures AVANT.
    Aucune commande CLI ne juge la byte-identité visuelle. Renseigner une section
    `## 2. Comparaison visuelle APRÈS/AVANT` dans 03-QA-CHECKLIST.md.
  </action>
  <what-built>
    Les migrations DS + wirings (03-02..03-04) et le lazy loading (03-05) sont implémentés, le
    build passe, et la byte-identité CSS est prouvée (Task 1). Reste le jugement visuel humain
    sur le rendu réel : non scriptable.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, ouvrir Chrome via chrome-devtools-mcp, se connecter au compte de test.
    2. Capturer les 5 surfaces APRÈS avec le préfixe `03-after-` sous `audit/screenshots/` :
       fiche spot au snap 0.35 ; fiche au snap 0.95 sur les 3 onglets (Info/Avis/Sessions) ;
       profil anonyme (déconnecté) ; profil authentifié ; PremiumModal (ouvert depuis Profil) ;
       CommunityStatsScreen.
    3. Comparer chaque APRÈS à son AVANT (03-BASELINE.md § 7). Vérifier que les points focaux
       (`03-UI-SPEC.md` § Hiérarchie visuelle) n'ont pas bougé : titre+badge de la fiche, bloc
       identité du profil, 2 KPI de CommunityStats, badge Sparkles de PremiumModal.
    4. Rouvrir `AuthModal` (flux de connexion) et `FiltersModal` (depuis la carte) et vérifier
       qu'ils sont pixel-identiques aux captures Phase 1/2 : l'extension du master Modal ne doit
       PAS les avoir régressés.
    5. Consigner en section 2 : par surface, verdict IDENTIQUE / ÉCART. Les seuls écarts
       tolérés : `aria-label="Close"` de PremiumModal (invisible) et l'éventuel commentaire
       `src/index.css:41`. Tout autre écart = régression à corriger.
  </how-to-verify>
  <verify>
    <human-check>03-QA-CHECKLIST.md § 2 liste les 5 surfaces + AuthModal + FiltersModal avec un verdict IDENTIQUE (ou ÉCART documenté et résolu) ; les captures `03-after-*` existent et sont référencées ; aucun point focal n'a bougé.</human-check>
  </verify>
  <done>Les 5 surfaces + AuthModal + FiltersModal sont comparées APRÈS/AVANT ; toutes IDENTIQUES hormis les écarts assumés ; captures 03-after-* archivées et référencées.</done>
  <acceptance_criteria>
    - La section 2 contient une ligne par surface (les 5 + AuthModal + FiltersModal) avec verdict.
    - Chaque capture `03-after-*` référencée existe réellement sur le disque et a un pendant `03-before-*` dans 03-BASELINE.md § 7.
    - Aucun verdict d'ÉCART non assumé ne subsiste (les seuls écarts tolérés sont `aria-label="Close"` et le commentaire `src/index.css:41`).
    - Les points focaux des 4 surfaces migrées sont confirmés inchangés (titre+badge fiche, identité profil, KPI CommunityStats, badge Sparkles PremiumModal).
  </acceptance_criteria>
  <resume-signal>Taper « visuel validé » une fois la section 2 remplie et les écarts résolus, ou décrire toute régression visuelle constatée.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 : Recette manuelle mobile QA-01 à 100% sur device (fonctionnel + gestuel + non authentifié + safe-area)</name>
  <files>.planning/phases/03-fiches-d-taill-es-profils/03-QA-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/03-fiches-d-taill-es-profils/03-VALIDATION.md (§ Manual-Only Verifications)
    - .planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md (§ Zones gelées — gardes d'accès, safe-area, layoutId, coque vaul)
    - .planning/phases/03-fiches-d-taill-es-profils/03-RESEARCH.md (Pitfall 5 support iOS lazy ; § Security Domain V4 gardes)
  </read_first>
  <action>
    Étape device nécessitant un iPhone réel (WebView Capacitor) : la gestuelle tactile, le
    comportement `loading="lazy"` en WebView iOS et la safe-area ne sont pas reproductibles en
    desktop. Dérouler la checklist QA-01 pour le périmètre de la phase et renseigner une section
    `## 3. Recette QA-01 (device)` dans 03-QA-CHECKLIST.md avec, pour chaque item, PASS/FAIL.
    La phase n'est validée que si TOUS les items sont PASS (100%, zéro régression).
  </action>
  <what-built>
    Toutes les migrations DS, wirings, lazy loading et preuves byte-identité (Tasks 1-2) sont
    en place. Reste la recette fonctionnelle et gestuelle bout-en-bout sur device : non
    scriptable, non reproductible en desktop.
  </what-built>
  <how-to-verify>
    Dérouler sur device iOS réel et cocher chaque item (PASS/FAIL) :
    1. **Fiche spot — gestuelle (chemin critique)** : Map -> clic marker -> SpotDetail s'ouvre au
       snap 0.35 -> drag vers 0.95 -> swipe-to-dismiss ferme la fiche. Le morphing du nom du spot
       (layoutId liste<->fiche) est visible. Aucun accroc de scroll interne.
    2. **Lightbox** : ouvrir la lightbox depuis la vignette -> next/prev fluides (pas de délai
       perceptible au clic, grâce au prefetch) -> swipe dans la lightbox NE déclenche PAS le
       drag-to-dismiss de la coque (portail isolé) -> fermeture OK.
    3. **Onglets** Info/Avis/Sessions : bascule OK, avatars d'avis/sessions s'affichent (lazy)
       sans image manquante (contrôle Pitfall 5 sur device).
    4. **Passe NON authentifiée** : déconnecté, la vignette est floutée + cadenas ; le cœur favori
       ouvre l'auth ; l'ouverture lightbox ouvre l'auth ; la rangée « Tableau de Bord Admin » est
       masquée pour un compte non-admin.
    5. **Profil** : authentifié -> avatar, 2 stats (Card), rangées de réglages, Log Out ; anonyme
       -> branche « Sign In / Join ». PremiumModal : ouverture depuis « Devenir Premium »,
       fermeture par le bouton close ET par clic sur le backdrop.
    6. **CommunityStatsScreen** : ouverture depuis Profil ; le titre n'est PAS sous la status bar
       (safe-area préservée) ; les 2 KPI (Card) et la liste de pays s'affichent.
    7. **Auth & Filtres** : le flux de connexion (`AuthModal`) et les filtres carte
       (`FiltersModal`) fonctionnent comme avant (non-régression du master Modal étendu).
    8. Écrire le **verdict de phase** en fin de 03-QA-CHECKLIST.md : recette à 100% (ou liste des
       FAIL -> gap closure), rappel des verdicts UI-01/UI-02 (byte-identité) et PERF-02
       (métriques A/B/C de 03-BASELINE.md), et note sur la dégradation gracieuse iOS observée.
  </how-to-verify>
  <verify>
    <human-check>03-QA-CHECKLIST.md § 3 liste tous les items de recette (gestuelle, lightbox, onglets, passe non authentifiée, profil, premium, community stats, auth/filtres) marqués PASS à 100% sur device réel, avec un verdict de phase couvrant UI-01/UI-02/PERF-02 et QA-01.</human-check>
  </verify>
  <done>La recette QA-01 du périmètre passe à 100% sur device ; la gestuelle native, les gardes non authentifiées et la safe-area sont confirmées intactes ; le verdict de phase est écrit.</done>
  <acceptance_criteria>
    - La section 3 contient tous les items listés dans `<how-to-verify>` marqués PASS (aucun FAIL non résolu ; tout FAIL déclenche un plan de gap closure).
    - Le drag-to-dismiss, les snap points, le morphing `layoutId` et la lightbox next/prev sont confirmés fonctionnels sur device.
    - La passe non authentifiée confirme : vignette floutée + cadenas, cœur favori -> auth, lightbox -> auth, rangée Admin masquée (non-régression des gardes V4).
    - La safe-area de CommunityStatsScreen est confirmée (titre non masqué par la status bar).
    - `AuthModal` et `FiltersModal` sont confirmés non régressés.
    - Un verdict de phase final couvre UI-01, UI-02 (byte-identité + visuel) et PERF-02 (métriques A/B/C), et référence `03-BASELINE.md`.
    - `03-QA-CHECKLIST.md` contient la chaîne « 100% » dans le verdict de recette.
  </acceptance_criteria>
  <resume-signal>Taper « recette 100% » une fois tous les items PASS, ou lister les FAIL constatés (déclenche un plan de gap closure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Plan de vérification/documentation : aucun code applicatif écrit (seul `03-QA-CHECKLIST.md` est produit). |
| Session de recette -> backend Supabase de production | La recette utilise un compte de test réel ; interactions en lecture + toggles réversibles (favori, join/leave session). Ne pas créer de données parasites persistantes. |
| Gardes d'accès côté client | La recette non authentifiée VÉRIFIE la non-régression des gardes V4 (favori/lightbox/blur/admin) — c'est le contrôle, pas une nouvelle surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Elevation of Privilege | Gardes `!user` / `isAdmin` (SpotDetail, Profile) | mitigate | La passe non authentifiée (Task 3) est un contrôle explicite : vignette floutée + cadenas, cœur favori et lightbox déclenchant l'auth, rangée Admin masquée pour non-admin. Autorité réelle = RLS Supabase ; le masquage client est cosmétique et ne doit pas avoir régressé. |
| T-03-06-02 | Information Disclosure | Captures `03-after-*` + 03-QA-CHECKLIST.md (versionnés) | mitigate | Masquer l'email du compte de test sur les captures du profil authentifié (`Profile` L205). Interdit : URL signée Supabase Storage, JWT, token dans une capture. Cohérent avec T-03-01-A du plan 03-01. |
| T-03-06-03 | Tampering | Master `Modal` étendu -> AuthModal (voie de connexion) | mitigate | Task 3 rouvre explicitement `AuthModal` et `FiltersModal` : un dispatch cassé bloquerait l'authentification. Vérification device + comparaison aux captures Phase 1/2. |
| T-03-06-04 | Repudiation | Verdict de phase (UI-01/UI-02/PERF-02/QA-01) | mitigate | Verdict tracé dans `03-QA-CHECKLIST.md` avec preuves (CSS compilé, captures avant/après, métriques 03-BASELINE.md) — auditable, pas une simple assertion. |
| T-03-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | Aucun `npm install`. Note d'hygiène héritée : `.mcp.json` lance `npx -y chrome-devtools-mcp@latest` non épinglé (config Phase 2, non introduite ici). |
</threat_model>

<verification>
- `npm run build` produit un CSS compilé exploitable pour la preuve byte-identité (Task 1).
- Chaque paire de tokens câblée est prouvée IDENTIQUE ; `text-slate-900` documenté non câblé.
- Les 5 surfaces + AuthModal + FiltersModal sont comparées APRÈS/AVANT : IDENTIQUES hormis écarts assumés.
- La recette QA-01 du périmètre passe à 100% sur device (gestuelle, non authentifié, safe-area, lazy iOS).
- Verdict de phase écrit couvrant UI-01, UI-02, PERF-02 et référençant 03-BASELINE.md.
</verification>

<success_criteria>
- La byte-identité de tous les wirings est prouvée (CSS + visuel) : UI-01/UI-02 satisfaits selon le critère d'override documenté, sans régression.
- La non-régression fonctionnelle et gestuelle est validée à 100% en recette manuelle mobile (success criterion 4 du ROADMAP Phase 3 ; contribution QA-01).
- Le delta PERF-02 (métriques A/B/C) est confirmé et documenté contre `03-BASELINE.md`.
</success_criteria>

<output>
Créer `.planning/phases/03-fiches-d-taill-es-profils/03-06-SUMMARY.md` une fois terminé, en y
consignant : le verdict byte-identité (paires prouvées IDENTIQUES), le résultat de la comparaison
visuelle des 5 surfaces + AuthModal/FiltersModal, le score de recette QA-01 (100% attendu) et le
verdict de phase UI-01/UI-02/PERF-02 avec renvoi à 03-QA-CHECKLIST.md et 03-BASELINE.md.
</output>
