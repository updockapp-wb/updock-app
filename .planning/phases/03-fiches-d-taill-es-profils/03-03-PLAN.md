---
phase: 03-fiches-d-taill-es-profils
plan: 03
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/components/Profile.tsx
  - src/components/CommunityStatsScreen.tsx
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "Les 2 cartes stats de Profile (Spots Added, Favorites) sont rendues par src/ui/Card"
    - "Le bouton Log Out de Profile est rendu par src/ui/Button variant=\"secondary\" size=\"lg\""
    - "Les 2 cartes KPI de CommunityStatsScreen (spots publiés, riders inscrits) sont rendues par src/ui/Card"
    - "Toute couleur/rayon de Profile et CommunityStatsScreen ayant une correspondance 1:1 avec un token est câblée (text-primary/text-text/text-muted/bg-background/text-accent/border-primary)"
    - "Le padding safe-area de CommunityStatsScreen (pt-[calc(1rem+env(safe-area-inset-top))]) est intact ; l'app bar h1/flèche-à-gauche reste custom ; le code mort de Profile n'est pas migré"
    - "Le périmètre UI-02 couvre Profile.tsx + CommunityStatsScreen.tsx, y compris le bouton d'entrée « Tableau de Bord Admin » dans Profile.tsx (l'écran AdminDashboard lui-même reste hors scope) (D-04)"
  artifacts:
    - path: "src/components/Profile.tsx"
      provides: "Écran Profil consommant Card + Button du design system, avec tokens câblés"
      contains: "from '../ui/Card'"
    - path: "src/components/CommunityStatsScreen.tsx"
      provides: "Écran stats communauté consommant Card + tokens, coque et safe-area intactes"
      contains: "from '../ui/Card'"
  key_links:
    - from: "src/components/Profile.tsx"
      to: "src/ui/Card.tsx"
      via: "import + <Card> autour des 2 KPI"
      pattern: "<Card>"
    - from: "src/components/Profile.tsx"
      to: "src/ui/Button.tsx"
      via: "import + variant=\"secondary\" size=\"lg\" sur Log Out"
      pattern: "variant=\"secondary\" size=\"lg\""
    - from: "src/components/CommunityStatsScreen.tsx"
      to: "src/ui/Card.tsx"
      via: "import + <Card> autour des 2 KPI"
      pattern: "<Card>"
---

<objective>
Migrer l'écran Profil (`Profile.tsx`) et son écran satellite `CommunityStatsScreen.tsx`
vers les composants maîtres du design system (`Card`, `Button`) là où le rendu est
**byte-identique**, et câbler tous les tokens ayant une correspondance 1:1 — sans le
moindre changement de rendu observable.

Purpose : D-04 verrouille `Profile.tsx` + `CommunityStatsScreen.tsx` dans le périmètre de
UI-02. Ce sont les seuls fichiers de la phase où des composants maîtres s'appliquent
littéralement (4 `Card` + 1 `Button secondary`, prouvés byte-identiques par l'inventaire
de `03-RESEARCH.md`). Le reste (rangées de réglages, champ nom, app bar, switch notif) n'a
aucun équivalent DS et reste custom, avec ses tokens câblés — conformément au critère
d'override documenté de UI-01/UI-02 (`03-UI-SPEC.md` § Contrat de périmètre).

Output : `Profile.tsx` et `CommunityStatsScreen.tsx` migrés (Card + Button + tokens),
`PremiumModal` (traité en plan 03-02, parallèle) et `AdminDashboard` (hors scope) non touchés.

**Contrainte cardinale : harmonisation, pas rebranding. Chaque migration est prouvée
byte-identique ; chaque wiring de token est une correspondance 1:1 aliasée à la palette
Tailwind v4 (`src/index.css` `@theme`). Toute divergence de rendu est une régression.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md
@.planning/phases/03-fiches-d-taill-es-profils/03-RESEARCH.md

<interfaces>
<!-- Contrats des masters consommés. Extraits du code : aucune exploration requise. -->

src/ui/Card.tsx — API (consommée telle quelle, NE PAS modifier) :
    interface CardProps { variant?: 'light' | 'glass'; interactive?: boolean; className?: string; children: React.ReactNode; }
    Défaut variant='light' -> rend EXACTEMENT `bg-white p-4 rounded-2xl border border-slate-100 shadow-sm`.
    PAS de prop `onClick` : les rangées cliquables ne peuvent donc PAS devenir des Card (et
    leurs classes ne matchent pas de toute façon).

src/ui/Button.tsx — API (consommée telle quelle, NE PAS modifier) :
    variant='secondary' size='lg' -> `bg-slate-200 hover:bg-slate-300 text-slate-600 font-bold
    rounded-2xl transition-all` + `py-4` (lg) + base `flex items-center justify-center gap-2
    disabled:opacity-50 disabled:pointer-events-none`. La variante secondary a été extraite de
    Profile:431 en Phase 1. Sans `disabled`/`loading`, les classes disabled:* ne produisent
    aucun style -> rendu identique.

Tokens (src/index.css @theme, aliasés à la palette Tailwind v4 -> byte-identiques par valeur) :
  --color-primary=sky-500, --color-secondary=slate-900, --color-accent=rose-500,
  --color-text=slate-800, --color-muted=slate-500, --color-background=slate-50.

Table de wiring exacte (source de vérité) : `03-UI-SPEC.md` § Contrat de wiring de tokens
et `03-RESEARCH.md` § Surface de wiring de tokens. Les numéros de ligne ci-dessous sont des
HINTS — appliquer par correspondance de chaîne de classe, car les migrations Card/Button
décalent les numéros de ligne.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Migrer Profile.tsx — Card ×2, Button secondary, wiring des tokens</name>
  <files>src/components/Profile.tsx</files>
  <read_first>
    - src/components/Profile.tsx (état complet — 450 lignes ; repérer le retour anticipé `if (!user)` L71 : tout ce qui suit est la branche authentifiée)
    - src/ui/Card.tsx (rend `bg-white p-4 rounded-2xl border border-slate-100 shadow-sm`)
    - src/ui/Button.tsx (variant secondary size lg — origine Profile:431)
    - .planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md (§ Inventaire des migrations structurelles sites 1/2/5 ; § Contrat de wiring de tokens ; § Non-migrations ; § Zones gelées ; § Hors périmètre)
    - .planning/phases/03-fiches-d-taill-es-profils/03-RESEARCH.md (Pitfall 6 : code mort à NE PAS migrer)
  </read_first>
  <action>
    Migrer UNIQUEMENT dans la branche authentifiée (après le retour anticipé `if (!user)` L71).

    A) Migrations structurelles byte-identiques (les 3 seules autorisées dans ce fichier) :
    1. Ajouter `import Card from '../ui/Card';` et `import Button from '../ui/Button';`.
    2. Grille de stats (≈L264-273) : remplacer les deux `<div className="bg-white p-4 rounded-2xl
       border border-slate-100 shadow-sm">…</div>` (Spots Added, Favorites) par `<Card>…</Card>`.
       Conserver le contenu interne inchangé : `<p className="text-slate-400 text-xs font-bold
       uppercase mb-1">…</p>` (text-slate-400 = HORS token, rester littéral) puis
       `<p className="text-2xl font-black text-text">…</p>` (font-black = exception legacy préservée).
    3. Log Out (≈L429-435) : remplacer le `<button className="w-full bg-slate-200 hover:bg-slate-300
       text-slate-600 font-bold py-4 rounded-2xl transition-all mb-8 flex items-center justify-center
       gap-2" onClick={() => signOut()}>` par `<Button variant="secondary" size="lg" onClick={() =>
       signOut()} className="w-full mb-8">`, en conservant les enfants `<LogOut size={18} />` et le
       libellé « Log Out » codé en dur (NE PAS franciser — § Copywriting).

    B) Wiring des tokens (correspondances 1:1 — appliquer par chaîne, hints de ligne) :
       - `text-sky-500` -> `text-primary` (80, 127, 178, 292, 315, 329, 357, 383)
       - `bg-sky-500` -> `bg-primary` (96, 244, 391) — NE PAS toucher L212 (code mort après le
         retour anticipé L71, Pitfall 6 : le wiring y serait sans effet observable, laisser littéral)
       - `focus:border-sky-500` -> `focus:border-primary` (233)
       - `border-sky-500` -> `border-primary` (374, spinner)
       - `text-slate-800` -> `text-text` (84, 199, 267, 271, 278, 294)
       - `text-slate-500` -> `text-muted` (89, 225, 366)
       - `bg-slate-50` -> `bg-background` (76, 157, 233, 255)
       - `hover:bg-slate-50` -> `hover:bg-background` (104, 125, 311, 327, 354, 405, 417)
       - `text-rose-500` -> `text-accent` (408)

    C) NE PAS faire :
       - NE PAS câbler `text-slate-900` -> `text-secondary` (L255) : choix conservateur verrouillé
         par `03-UI-SPEC.md` (token nommé pour un fond de CTA, pas pour du texte). Rester littéral.
       - NE PAS migrer le nom utilisateur (L199) vers `<Header>` (ajouterait `mb-2`, décalerait
         l'email de 8px) — seulement wirer sa couleur.
       - NE PAS migrer les rangées de réglages ni le champ nom (`Input` glass-only = Phase 4).
       - NE PAS migrer, ni « nettoyer », ni supprimer le code mort (branche `!user` déjà retournée,
         ternaire L200-218, gardes `{user && …}`) — c'est CODE-01 / Phase 5.
       - NE PAS toucher au bouton d'entrée « Tableau de Bord Admin » autrement que par wiring de
         token (l'écran AdminDashboard reste hors scope).
       - NE PAS wirer les littéraux hors token : `slate-100/200/300/400/600/700`, `sky-50/100/400/
         600/700`, `from-sky-400 to-blue-600`, `hover:bg-sky-400`, dégradés, ombres.

    Lancer `npm run build` (typecheck `tsc -b` inclus) et corriger toute erreur de props avant de terminer.
  </action>
  <verify>
    <automated>npm run build && grep -q "from '../ui/Card'" src/components/Profile.tsx && grep -q "from '../ui/Button'" src/components/Profile.tsx && test "$(grep -c '<Card>' src/components/Profile.tsx)" = "2" && grep -q 'variant="secondary" size="lg"' src/components/Profile.tsx && test "$(grep -c 'text-slate-800' src/components/Profile.tsx)" = "0" && test "$(grep -c 'text-slate-500' src/components/Profile.tsx)" = "0" && test "$(grep -c 'text-sky-500' src/components/Profile.tsx)" = "0" && test "$(grep -c 'border-sky-500' src/components/Profile.tsx)" = "0" && test "$(grep -c 'text-rose-500' src/components/Profile.tsx)" = "0" && test "$(grep -c 'bg-slate-50' src/components/Profile.tsx)" = "0" && grep -q 'text-primary' src/components/Profile.tsx && grep -q 'text-text' src/components/Profile.tsx && grep -q 'text-muted' src/components/Profile.tsx && grep -q 'bg-background' src/components/Profile.tsx && grep -q 'text-accent' src/components/Profile.tsx && grep -q 'focus:border-primary' src/components/Profile.tsx && test -z "$(git status --short src/components/CommunityStatsScreen.tsx src/ui/Card.tsx src/ui/Button.tsx)"</automated>
  </verify>
  <done>Profile.tsx consomme Card ×2 + Button secondary ; tous les littéraux à correspondance 1:1 sont câblés (sauf le code mort L212 et le choix conservateur text-slate-900) ; build vert ; aucun autre fichier touché.</done>
  <acceptance_criteria>
    - `npm run build` termine sans erreur.
    - `grep -c '<Card>' src/components/Profile.tsx` retourne 2 ; les deux Card entourent les KPI « Spots Added » et « Favorites ».
    - `grep -q 'variant="secondary" size="lg"'` réussit ; le bouton Log Out ne comporte plus la chaîne `bg-slate-200 hover:bg-slate-300 text-slate-600 font-bold py-4 rounded-2xl` en dur (assertion source : `grep -c 'bg-slate-200 hover:bg-slate-300' src/components/Profile.tsx` = 0).
    - `text-slate-800`, `text-slate-500`, `text-sky-500`, `border-sky-500` (couvre aussi `focus:border-sky-500`), `text-rose-500`, `bg-slate-50` (couvre aussi `hover:bg-slate-50`) : chacun retourne 0 en `grep -c`.
    - `text-primary`, `text-text`, `text-muted`, `bg-background`, `text-accent`, `focus:border-primary` : chacun présent (`grep -q`).
    - `grep -c 'bg-primary' src/components/Profile.tsx` >= 3 (les 3 sites live câblés) ; il reste au plus une occurrence `bg-sky-500` et elle vit dans le code mort (≈L212) — documenter ce résidu dans le SUMMARY.
    - `text-slate-900` est TOUJOURS présent (choix conservateur non câblé, ≈L255) : `grep -q 'text-slate-900' src/components/Profile.tsx` réussit.
    - Le libellé « Log Out » codé en dur est préservé (`grep -q 'Log Out' src/components/Profile.tsx`).
    - `git status --short` ne liste que `src/components/Profile.tsx` pour ce fichier ; `CommunityStatsScreen.tsx`, `Card.tsx`, `Button.tsx` sont vides de diff.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 : Migrer CommunityStatsScreen.tsx — Card ×2 + wiring, safe-area et app bar intactes</name>
  <files>src/components/CommunityStatsScreen.tsx</files>
  <read_first>
    - src/components/CommunityStatsScreen.tsx (état complet — repérer l'app bar L88-98 et la grille KPI L109-122)
    - src/ui/Card.tsx (chaîne rendue exacte)
    - .planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md (§ Inventaire sites 3/4 ; § Non-migrations app bar ; § Zones gelées safe-area ; § Contrat de wiring)
    - .planning/phases/03-fiches-d-taill-es-profils/03-RESEARCH.md (§ Code Examples — bloc CommunityStatsScreen.tsx montrant les cibles exactes)
  </read_first>
  <action>
    A) Migrations structurelles (les 2 seules autorisées) :
    1. Ajouter `import Card from '../ui/Card';`.
    2. Grille KPI (≈L109-122) : remplacer les deux `<div className="bg-white p-4 rounded-2xl border
       border-slate-100 shadow-sm">…</div>` (total spots, total users) par `<Card>…</Card>`.
       Conserver le contenu : `<p className="text-slate-400 text-xs font-bold uppercase mb-1">
       {t('community_stats.total_spots' / 'total_users')}</p>` (text-slate-400 hors token) puis
       `<p className="text-2xl font-black text-text">{fmt(...)}</p>`.

    B) Wiring des tokens (par chaîne, hints de ligne) :
       - `bg-slate-50` -> `bg-background` (88 — fond plein écran, usage sémantiquement parfait)
       - `text-slate-800` -> `text-text` (97, 114, 120, 127)
       - `hover:text-slate-800` -> `hover:text-text` (93)
       - `text-slate-500` -> `text-muted` (139)
       - `border-sky-500` -> `border-primary` (104, spinner)

    C) NE PAS toucher (§ Zones gelées / Non-migrations) :
       - NE PAS toucher au `pt-[calc(1rem+env(safe-area-inset-top))]` de l'app bar (≈L90) :
         fix safe-area antérieur, régression iOS garantie si modifié.
       - NE PAS migrer l'app bar (`<h1 className="text-lg font-bold …">` + `ArrowLeft` à GAUCHE +
         `border-b bg-white`) vers `src/ui/Header` : aucune des 2 formes de Header ne correspond
         (`h1`/`text-lg`/flèche à gauche). Seules ses couleurs sont wirées (97, 93).
       - NE PAS migrer les conteneurs de rangées (≈L130) vers Card (rayon ≠ rounded-2xl, pas de
         shadow-sm, pas de p-4).
       - NE PAS toucher à la liste de pays ni à `countryCodeToFlag()` (le bug de liste incomplète
         reste au backlog, hors périmètre DS).
       - NE PAS wirer `border-slate-100` -> `bg-background` (slate-100 ≠ slate-50).

    Lancer `npm run build` et corriger toute erreur avant de terminer.
  </action>
  <verify>
    <automated>npm run build && grep -q "from '../ui/Card'" src/components/CommunityStatsScreen.tsx && test "$(grep -c '<Card>' src/components/CommunityStatsScreen.tsx)" = "2" && grep -q 'pt-\[calc(1rem+env(safe-area-inset-top))\]' src/components/CommunityStatsScreen.tsx && test "$(grep -c 'bg-slate-50' src/components/CommunityStatsScreen.tsx)" = "0" && test "$(grep -c 'text-slate-800' src/components/CommunityStatsScreen.tsx)" = "0" && test "$(grep -c 'text-slate-500' src/components/CommunityStatsScreen.tsx)" = "0" && test "$(grep -c 'border-sky-500' src/components/CommunityStatsScreen.tsx)" = "0" && grep -q 'bg-background' src/components/CommunityStatsScreen.tsx && grep -q 'text-text' src/components/CommunityStatsScreen.tsx && grep -q 'text-muted' src/components/CommunityStatsScreen.tsx && grep -q 'border-primary' src/components/CommunityStatsScreen.tsx && grep -q 'text-lg font-bold' src/components/CommunityStatsScreen.tsx && test -z "$(git status --short src/components/Profile.tsx src/ui/Card.tsx)"</automated>
  </verify>
  <done>CommunityStatsScreen consomme Card ×2, tokens câblés, safe-area et app bar h1/flèche-à-gauche intactes ; build vert ; aucun autre fichier touché.</done>
  <acceptance_criteria>
    - `npm run build` termine sans erreur.
    - `grep -c '<Card>' src/components/CommunityStatsScreen.tsx` retourne 2 (les 2 KPI).
    - `grep -q 'pt-\[calc(1rem+env(safe-area-inset-top))\]'` réussit — le padding safe-area est intact au caractère près.
    - `bg-slate-50`, `text-slate-800` (couvre `hover:text-slate-800`), `text-slate-500`, `border-sky-500` : chacun retourne 0 en `grep -c`.
    - `bg-background`, `text-text`, `text-muted`, `border-primary` : chacun présent.
    - `grep -q 'text-lg font-bold'` réussit : le `<h1>` de l'app bar (`text-lg`) reste custom, non migré vers `Header`.
    - Aucun `import Header` n'est ajouté (`grep -c "from '../ui/Header'" src/components/CommunityStatsScreen.tsx` = 0).
    - `git status --short` ne liste que `src/components/CommunityStatsScreen.tsx` pour ce fichier.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Refactor de présentation pur : aucun nouveau flux d'auth, aucune nouvelle entrée serveur, aucune donnée traitée. Les données lues (`spots`, `favorites`, stats) restent inchangées. |
| Gardes d'accès côté client -> réorganisation JSX | Frontière de non-régression : `isAdmin` conditionne la rangée « Tableau de Bord Admin » (Profile L154, 414) ; le retour anticipé `!user` (L71) sépare la branche anonyme. Une réorganisation du JSX ne doit pas exposer une rangée réservée. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Elevation of Privilege | Rangée « Tableau de Bord Admin » (`Profile.tsx`, garde `isAdmin`) | mitigate | Ne modifier que les couleurs de la rangée par wiring ; ne PAS déplacer ni retirer la garde `isAdmin`. Recette non-authentifiée + non-admin en plan 03-06 vérifie que la rangée reste masquée. Autorité réelle = RLS Supabase (le masquage est cosmétique). |
| T-03-03-02 | Denial of Service (UX) | `Profile` via master `Card`/`Button` | mitigate | `Card` et `Button` sont consommés tels quels, sans modification des masters (`git status` sur `Card.tsx`/`Button.tsx` vide). Un bug de props serait attrapé par `tsc -b` (verify build). |
| T-03-03-03 | Information Disclosure | Code mort de `Profile` (branche `!user` post-retour) | accept | Non migré, non supprimé (CODE-01/Phase 5). Inatteignable à l'exécution : aucune surface d'exposition nouvelle. |
| T-03-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | Aucun `npm install` (`03-RESEARCH.md` § Package Legitimacy Audit : aucune cible). `Card`/`Button`/`lucide-react` déjà présents et vérifiés dans `package.json`. |
</threat_model>

<verification>
- `npm run build` passe après chaque tâche (typecheck `tsc -b` inclus).
- `Profile.tsx` : 2 `<Card>`, 1 `<Button variant="secondary" size="lg">`, littéraux 1:1 câblés (hors code mort et hors choix conservateur `text-slate-900`).
- `CommunityStatsScreen.tsx` : 2 `<Card>`, tokens câblés, `pt-[calc(...)]` safe-area intact, app bar `text-lg`/`<h1>` non migrée.
- Byte-identité CSS + visuelle (captures avant/après) : déléguée au plan 03-06 (phase gate).
- Recette non-authentifiée / non-admin (gardes préservées) : déléguée au plan 03-06.
</verification>

<success_criteria>
- Tout markup de `Profile`/`CommunityStatsScreen` ayant un équivalent byte-identique dans `src/ui/*` est migré (4 Card + 1 Button), et toute couleur/rayon 1:1 est câblée : contribution directe et vérifiable à UI-02 selon le critère d'override documenté.
- Aucun changement de rendu observable ; aucune migration non byte-identique forcée.
- Les zones gelées (safe-area, app bar, code mort, gardes d'accès) sont intactes.
</success_criteria>

<output>
Créer `.planning/phases/03-fiches-d-taill-es-profils/03-03-SUMMARY.md` une fois terminé, en y
consignant : la liste exacte des migrations Card/Button appliquées, la liste des wirings de token
par fichier, et le résidu assumé (`bg-sky-500` sur le code mort L212 de Profile, `text-slate-900`
non câblé par choix conservateur).
</output>
