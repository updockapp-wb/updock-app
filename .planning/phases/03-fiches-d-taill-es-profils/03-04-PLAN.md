---
phase: 03-fiches-d-taill-es-profils
plan: 04
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/components/SpotDetail.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Toute couleur de SpotDetail (hors zones gelées et hors overlay d'édition) ayant une correspondance 1:1 avec un token est câblée (text-primary/text-text/text-muted/bg-background/fill-accent/text-accent)"
    - "Les valeurs arbitraires de rayon rounded-[24px] (218, 741) et rounded-t-[32px] (763) sont remplacées par rounded-3xl / rounded-t-4xl (byte-identiques, sert DS-01)"
    - "Le motion.h2 + layoutId partagé (titre du spot) est intact — l'animation d'élément partagé liste<->fiche n'est pas cassée"
    - "La coque vaul (Drawer.Root/Portal/Content, snapPoints, handlers touch) reste vaul et ne migre pas vers src/ui/Modal (D-01) ; les icon-buttons du header (Partager, Favori, Fermer, Modifier) restent des <button> natifs et ne migrent pas vers src/ui/Button (D-02) ; les onglets et la lightbox sont intacts"
    - "L'overlay d'édition (L526-664) n'est pas touché (Phase 4 / UI-03) ; aucune migration Card/Header/Button/Input/Modal n'est faite (aucune n'est byte-identique dans ce fichier)"
  artifacts:
    - path: "src/components/SpotDetail.tsx"
      provides: "Fiche détail spot avec tokens câblés et rayons canoniques, coque vaul et gestuelle intactes"
      contains: "rounded-3xl"
  key_links:
    - from: "src/components/SpotDetail.tsx"
      to: "src/index.css @theme (tokens)"
      via: "classes utilitaires text-primary/text-text/text-muted/bg-background/fill-accent"
      pattern: "text-text"
    - from: "src/components/SpotDetail.tsx (titre)"
      to: "src/components/NearbySpotsList.tsx (carte)"
      via: "layoutId partagé spot-name-${id} — À PRÉSERVER"
      pattern: "layoutId=\\{`spot-name-"
---

<objective>
Harmoniser `SpotDetail.tsx` via **wiring de tokens** et **suppression des valeurs de rayon
arbitraires**, sans migrer aucun composant maître (aucun n'est byte-identique dans ce
fichier) et sans toucher aux zones gelées.

Purpose : UI-01 exige que la fiche détail spot « utilise exclusivement les composants du
design system ». L'inventaire exhaustif de `03-RESEARCH.md` prouve que **zéro** composant
maître s'applique byte-identique à `SpotDetail` (Header bloqué par `layoutId` + `text-slate-900` ;
Card bloqué par `bg-slate-50` + absence de `shadow-sm` ; Button bloqué par D-02 + gradient ;
Modal bloqué par D-01 `vaul`). UI-01 est donc honoré, selon le critère d'override documenté
(`03-UI-SPEC.md` § Contrat de périmètre), par le câblage des tokens 1:1 et le retrait des
rayons arbitraires — le reste n'a aucun équivalent DS et resterait une invention visuelle.

Output : `SpotDetail.tsx` avec tokens câblés + rayons canoniques ; coque `vaul`, gestuelle,
`layoutId`, onglets, lightbox et overlay d'édition strictement intacts.

**Contrainte cardinale : harmonisation, pas rebranding. Chaque wiring est une correspondance
1:1 aliasée à la palette Tailwind v4. Toute divergence de rendu ou de gestuelle est une
régression, pas un arbitrage.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md
@.planning/phases/03-fiches-d-taill-es-profils/03-RESEARCH.md

<interfaces>
<!-- Tokens et zones gelées. Source de vérité : 03-UI-SPEC.md. -->

Tokens (src/index.css @theme, aliasés Tailwind v4 -> byte-identiques par valeur) :
  --color-primary=sky-500, --color-text=slate-800, --color-muted=slate-500,
  --color-background=slate-50, --color-accent=rose-500.
Rayons canoniques byte-identiques (défauts Tailwind v4) :
  --radius-3xl=1.5rem (=24px) ; --radius-4xl=2rem (=32px, aussi redéclaré src/index.css:21).

Faits vérifiés (grep direct) :
  - layoutId `spot-name-${spot.id}` existe à SpotDetail.tsx:225 ET NearbySpotsList.tsx:63
    (2 nœuds appariés par framer-motion) — migrer le titre casse l'animation SILENCIEUSEMENT.
  - rounded-[24px] apparaît 3× dans SpotDetail : 2 dans le périmètre (218, 741) + 1 dans
    l'overlay d'édition HORS PÉRIMÈTRE (532). rounded-t-[32px] apparaît 1× (763, Drawer.Content).
  - Sites <img> : 246 (uploader), 409 (vignette), 610/622 (overlay édition, hors scope),
    721 (motion.img lightbox). AUCUN loading="lazy" dans ce plan (PERF-02 = plan 03-05).

Table de wiring exacte : `03-UI-SPEC.md` § Contrat de wiring de tokens (SpotDetail.tsx).
Numéros de ligne = HINTS ; appliquer par correspondance de chaîne.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Câbler les tokens de couleur de SpotDetail (hors zones gelées et hors overlay d'édition)</name>
  <files>src/components/SpotDetail.tsx</files>
  <read_first>
    - src/components/SpotDetail.tsx (état complet — 780 lignes ; repérer la coque `vaul` Drawer.Root/Portal/Content, le `motion.h2` layoutId L225, les icon-buttons header L275-311, les onglets, la lightbox L668-770, et l'overlay d'édition L526-664)
    - .planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md (§ Contrat de wiring de tokens SpotDetail ; § Zones gelées ; § Hors périmètre ; § Non-migrations)
    - .planning/phases/03-fiches-d-taill-es-profils/03-RESEARCH.md (Pitfall 2 layoutId ; Pitfall 8 coque vaul ; § Architectural Responsibility Map)
  </read_first>
  <action>
    Câbler UNIQUEMENT les correspondances 1:1, par chaîne de classe (hints de ligne), en
    ÉVITANT strictement l'overlay d'édition (L526-664) et les zones gelées :

    - `text-sky-500` -> `text-primary` (263, 378)
    - `text-slate-800` -> `text-text` (380, 385, 391, 440)
    - `text-slate-500` -> `text-muted` (238, 252, 486, 512)
    - `bg-slate-50` -> `bg-background` (377, 382, 441, 486, 512) — cartes stats + CTA verrouillés ;
      NE PAS toucher aux `bg-slate-50` de l'overlay d'édition (≈L545-584, Input, hors scope)
    - `fill-rose-500 text-rose-500` -> `fill-accent text-accent` (298, cœur favori actif)

    NE PAS câbler (choix conservateur verrouillé par `03-UI-SPEC.md`) :
    - `text-slate-900` -> `text-secondary` : laisser LITTÉRAL à L226 (titre, de toute façon dans
      le `motion.h2` gelé) et L453 (note moyenne). Sémantiquement bancal, non câblé.

    NE PAS toucher (zones gelées / hors périmètre) :
    - Le `motion.h2` avec `layoutId={`spot-name-${spot.id}`}` (L225) : NE PAS le migrer vers
      `src/ui/Header`, NE PAS changer son `layoutId`. Seule sa classe couleur pourrait bouger —
      or elle est `text-slate-900`, NON câblée (voir ci-dessus) : donc cette ligne ne change pas.
    - Les icon-buttons du header (Partager/Favori/Fermer/Modifier, L275-311) : restent des
      `<button>` natifs (D-02).
    - La coque `vaul` (`Drawer.Root`/`Portal`/`Content`, `snapPoints`, `modal={false}`, handlers
      `onPointerDown`/`onTouchStart`/`onTouchEnd`, `stopPropagation`) : structure et comportement
      gelés (D-01, Pitfall 8). Le SEUL changement admis dans cette zone est le rayon (Task 2).
    - Le portail de la lightbox (`Drawer.Portal` mobile / `createPortal(document.body)` desktop) et
      la lightbox elle-même (L668-770) : load-bearing, NE PAS déplacer.
    - Les onglets Info/Avis/Sessions (D-03) : pas de composant Tabs au DS.
    - L'overlay d'édition (L526-664) : formulaire d'édition de spot = Phase 4 / UI-03. NE PAS le
      toucher, NE PAS y wirer de token, NE PAS corriger le bug `URL.createObjectURL` (L622,
      Pitfall 7 — à consigner pour Phase 4, pas à corriger ici).
    - Les gardes d'accès `!user` (favori L288, lightbox L397, `blur-sm`+cadenas L412-421, droit
      d'édition L253) : NE PAS les régresser en réorganisant le JSX.
    - Les littéraux hors token : `slate-100/200/300/400/600/700`, `sky-50/100/400/600/700`,
      dégradés (`from-sky-500 to-blue-600`), `bg-black/60`, `bg-black/95`, `bg-white/70`, ombres.

    NE migrer AUCUN composant maître (Card/Header/Button/Modal/Input) : l'inventaire prouve qu'aucun
    n'est byte-identique ici. Aucun nouvel `import` depuis `../ui/`.

    Lancer `npm run build` et corriger toute erreur avant de terminer.
  </action>
  <verify>
    <automated>npm run build && grep -q 'text-primary' src/components/SpotDetail.tsx && grep -q 'text-text' src/components/SpotDetail.tsx && grep -q 'text-muted' src/components/SpotDetail.tsx && grep -q 'bg-background' src/components/SpotDetail.tsx && grep -q 'fill-accent text-accent' src/components/SpotDetail.tsx && test "$(grep -c 'text-sky-500' src/components/SpotDetail.tsx)" = "0" && test "$(grep -c 'layoutId' src/components/SpotDetail.tsx)" = "1" && grep -q 'spot-name-' src/components/SpotDetail.tsx && grep -q 'text-slate-900' src/components/SpotDetail.tsx && grep -q 'Drawer.Content' src/components/SpotDetail.tsx && grep -q 'snapPoints' src/components/SpotDetail.tsx && test "$(grep -c "from '../ui/" src/components/SpotDetail.tsx)" = "0" && grep -q 'URL.createObjectURL' src/components/SpotDetail.tsx</automated>
  </verify>
  <done>Tous les tokens de couleur 1:1 (hors zones gelées et overlay) sont câblés ; text-slate-900 reste littéral ; layoutId intact (count=1) ; coque vaul et overlay d'édition intacts ; aucun import ../ui/ ajouté ; build vert.</done>
  <acceptance_criteria>
    - `npm run build` termine sans erreur.
    - `text-primary`, `text-text`, `text-muted`, `bg-background`, `fill-accent text-accent` : chacun présent.
    - `grep -c 'text-sky-500' src/components/SpotDetail.tsx` retourne 0 (les 2 sites câblés ; aucun autre text-sky-500 dans le fichier).
    - `grep -c 'layoutId' src/components/SpotDetail.tsx` retourne exactement 1 (le titre partagé intact) et `grep -q 'spot-name-'` réussit.
    - `grep -q 'text-slate-900' src/components/SpotDetail.tsx` réussit (choix conservateur : L226/L453 restent littéraux).
    - `grep -q 'Drawer.Content'` et `grep -q 'snapPoints'` réussissent (coque vaul intacte).
    - `grep -c "from '../ui/" src/components/SpotDetail.tsx` retourne 0 (aucun composant maître importé/migré).
    - `grep -q 'URL.createObjectURL' src/components/SpotDetail.tsx` réussit (bug L622 NON corrigé, préservé pour Phase 4 — le SUMMARY doit le re-signaler comme dette Phase 4).
    - Revue du `git diff src/components/SpotDetail.tsx` : aucune ligne de l'overlay d'édition (L526-664), des handlers touch (L680-682), ni du portail lightbox (L668-670, 769-770) n'est modifiée. Aucune classe hors token (`slate-100/200/…`, dégradés, ombres) n'est changée.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 : Remplacer les rayons arbitraires par l'échelle canonique (rounded-3xl / rounded-t-4xl)</name>
  <files>src/components/SpotDetail.tsx</files>
  <read_first>
    - src/components/SpotDetail.tsx (repérer les rounded-[24px] hors overlay : L218 et L741 ; le rounded-t-[32px] du Drawer.Content L763 ; et le rounded-[24px] de l'overlay L532 à NE PAS toucher)
    - .planning/phases/03-fiches-d-taill-es-profils/03-UI-SPEC.md (§ Contrat de wiring — note « Drawer.Content : wiring du rayon autorisé » ; § Gain valeur arbitraire gratuit)
    - .planning/phases/03-fiches-d-taill-es-profils/03-RESEARCH.md (Open Q4 : rounded-t-[32px] sur Drawer.Content autorisé — D-01 gèle structure/comportement, pas les chaînes de classe)
  </read_first>
  <action>
    Remplacer les valeurs de rayon arbitraires byte-identiques (défauts Tailwind v4 :
    `--radius-3xl=1.5rem`=24px, `--radius-4xl=2rem`=32px) :

    - `rounded-[24px]` -> `rounded-3xl` aux deux sites du périmètre : L218 et L741.
      NE PAS toucher au `rounded-[24px]` de l'overlay d'édition (≈L532, hors scope).
    - `rounded-t-[32px]` -> `rounded-t-4xl` sur `Drawer.Content` (L763). Ce wiring est
      EXPLICITEMENT autorisé : D-01 gèle la structure et le comportement gestuel de la coque
      `vaul`, pas ses chaînes de classe ; changer un rayon n'affecte aucune gestuelle
      (`03-UI-SPEC.md` § note Drawer.Content).

    Ne toucher à rien d'autre. Aucune migration de composant. Ce retrait de valeurs arbitraires
    sert directement DS-01 (« aucune valeur de design en dur ») sans risque de rendu (24px=24px,
    32px=32px).

    Lancer `npm run build` et corriger toute erreur avant de terminer.
  </action>
  <verify>
    <automated>npm run build && grep -q 'rounded-3xl' src/components/SpotDetail.tsx && grep -q 'rounded-t-4xl' src/components/SpotDetail.tsx && test "$(grep -c 'rounded-\[24px\]' src/components/SpotDetail.tsx)" = "1" && test "$(grep -c 'rounded-t-\[32px\]' src/components/SpotDetail.tsx)" = "0" && grep -q 'Drawer.Content' src/components/SpotDetail.tsx</automated>
  </verify>
  <done>Les 2 rounded-[24px] du périmètre (218, 741) sont rounded-3xl ; le rounded-t-[32px] du Drawer.Content (763) est rounded-t-4xl ; le rounded-[24px] de l'overlay (532) est intact ; build vert.</done>
  <acceptance_criteria>
    - `npm run build` termine sans erreur.
    - `grep -c 'rounded-\[24px\]' src/components/SpotDetail.tsx` retourne exactement 1 (seul le rayon de l'overlay d'édition L532, hors périmètre, subsiste).
    - `grep -c 'rounded-t-\[32px\]' src/components/SpotDetail.tsx` retourne 0.
    - `grep -q 'rounded-3xl'` et `grep -q 'rounded-t-4xl'` réussissent.
    - `grep -q 'Drawer.Content'` réussit : la structure de la coque `vaul` est intacte, seule la classe de rayon a changé.
    - Revue du `git diff` : le diff de cette tâche ne touche que des chaînes `rounded-*` ; aucune modification de `snapPoints`, `modal={false}`, ni d'un handler `onTouch*`/`onPointerDown`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Refactor de présentation pur : aucun nouveau flux d'auth, aucune nouvelle entrée serveur. `SpotDetail` lit `spot`/`reviews`/`sessions` à l'identique. |
| Gardes d'accès côté client -> réorganisation JSX | Frontière de non-régression : `!user -> onOpenAuth()` sur le cœur favori (L288) et l'ouverture de la lightbox (L397) ; `blur-sm`+cadenas sur la vignette pour les non-connectés (L412-421) ; droit d'édition `user?.id === spot.user_id || user?.email === 'updock.app@gmail.com'` (L253). Contrôles côté client (UX) ; l'autorité reste les RLS Supabase. |
| Coque `vaul` / portail lightbox -> gestuelle tactile | Frontière comportementale : déplacer la lightbox hors de `Drawer.Portal` ou modifier les handlers ferait remonter les touch events au gesture handler de `vaul` (régression drag-to-dismiss). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Elevation of Privilege | Gardes `!user` (favori/lightbox/blur) + droit d'édition (`SpotDetail.tsx`) | mitigate | Le wiring ne touche que des classes de couleur/rayon ; ne PAS déplacer/retirer les gardes en réorganisant le JSX. Recette explicitement non authentifiée en plan 03-06 (vignette floutée + cadenas, cœur favori déclenchant l'auth). Autorité réelle = RLS Supabase. |
| T-03-04-02 | Tampering | Coque `vaul` + portail lightbox + handlers touch | mitigate | Zone gelée (D-01, Pitfall 8). Seul le rayon de `Drawer.Content` change. `<acceptance_criteria>` asserte `Drawer.Content`/`snapPoints` présents et le `git diff` limité aux classes. Recette device (drag 0.35<->0.95, swipe-to-dismiss, lightbox next/prev) en plan 03-06. |
| T-03-04-03 | Denial of Service (local) | `URL.createObjectURL` en rendu, jamais révoqué (L622, overlay d'édition) | accept | Bug préexistant, HORS PÉRIMÈTRE (Phase 4 / UI-03). Ne pas régresser, ne pas corriger ici (mélangerait fix de bug et migration DS). Consigné pour la Phase 4 dans le SUMMARY. |
| T-03-04-04 | Tampering | Image distante `image_urls`/`avatar_url` | accept | URLs Supabase Storage préexistantes ; ce plan ne change ni origine ni validation (le `loading="lazy"` est le plan 03-05). Aucune surface d'attaque nouvelle. |
| T-03-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | Aucun `npm install` (`03-RESEARCH.md` § Package Legitimacy Audit : aucune cible). `vaul`/`framer-motion`/`lucide-react` déjà présents et vérifiés. |
</threat_model>

<verification>
- `npm run build` passe après chaque tâche (typecheck `tsc -b` inclus).
- Tokens de couleur 1:1 câblés (hors overlay et zones gelées) ; `text-slate-900` littéral conservé.
- Rayons arbitraires du périmètre remplacés (218, 741, 763) ; celui de l'overlay (532) intact.
- `layoutId` intact (count=1), coque `vaul` intacte, aucun import `../ui/` ajouté.
- Byte-identité CSS + visuelle (captures avant/après, snap 0.35 et 0.95, 3 onglets) : déléguée au plan 03-06.
- Recette device (drag-to-dismiss, layoutId liste<->fiche, lightbox) + recette non authentifiée : déléguée au plan 03-06.
</verification>

<success_criteria>
- Toute couleur/rayon de `SpotDetail` ayant une correspondance 1:1 avec un token est câblée, et aucune migration non byte-identique n'est forcée : UI-01 honoré selon le critère d'override documenté, inventaire de `03-RESEARCH.md` joint comme preuve d'exhaustivité.
- Zéro régression de gestuelle : coque `vaul`, snap points, portail lightbox, handlers touch et `layoutId` liste<->fiche intacts.
- L'overlay d'édition (Phase 4) et les gardes d'accès ne sont pas touchés.
</success_criteria>

<output>
Créer `.planning/phases/03-fiches-d-taill-es-profils/03-04-SUMMARY.md` une fois terminé, en y
consignant : la liste des wirings de token appliqués, la confirmation que `text-slate-900` reste
littéral (choix conservateur), la confirmation que `layoutId` et la coque `vaul` sont intacts, et
le rappel de la dette Phase 4 (`URL.createObjectURL` L622 non corrigé).
</output>
